# Make crossing-aware endpoint correction sound: one checked strategy per port-facing segment, no unchecked fallback cascade

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This document must be maintained in accordance with `.agent/PLANS.md`, which is checked into this repository at that path and describes the required structure and editing discipline for ExecPlans in general.

## Purpose / Big Picture

This repository, TUMPhotonicRouter, routes optical waveguides for photonic chips on a discrete grid. After the A* search (see `.agent/execplans/2026-08-24-crossing-cost-function-soundness.md` for search-kernel background this plan does not repeat) produces a route as a sequence of grid-aligned "primitives" (short pre-defined path segments), a separate pipeline stage called "endpoint correction" runs afterward to nudge the route's two ends so they land exactly on the real port position and angle, instead of merely close to it (the grid is coarser than the exact sub-micrometer port geometry, so a route built purely from grid-aligned primitives is only approximately aligned with a port until this stage runs). A "net" is one logical point-to-point connection to route, identified by an integer `net_id` and a name like `n_67`. A "crossing" is a point where two different nets' waveguide paths physically cross on the chip, legal only under certain geometric conditions when the router's `enable_crossings` option is on.

Today, endpoint correction for a net that has one or more crossings on its route runs through a long, multi-tier fallback cascade (all in `translation/route_rust_endpoint_correction.py`, primarily inside `_apply_crossing_aware_endpoint_correction_to_record`, starting at line 1088 as of this writing): a first, "checked" tier that treats the port-to-first-crossing and last-crossing-to-port segments independently (much like a crossing-free net's own correction already does), and, only if that does not produce an accepted result, a second, materially weaker tier that computes a single "rich" correction across the *entire* route while ignoring the crossing entirely, then splices the crossing-safe middle section back in -- and that second tier has its own three further fallback levels for each end (the spliced rich correction itself, a second, "guard-window-limited" solver called `_absorbed_terminal_centerline`, and, as an absolute last resort, the route's raw, uncorrected geometry for that end).

This session (2026-08-24, immediately before this plan was written, working through `.agent/execplans/2026-08-24-crossing-cost-function-soundness.md`) found concrete, reproducible evidence that this cascade is not sound: a real bug in the second tier's acceptance check let it silently commit a route whose corrected geometry did not actually touch its own source port (`n_67` in `multiportmmi_8x8`, fixed in commit `2755a22`, but the underlying net is still left unable to route at all -- see Context below); a second net (`n_68`, same benchmark) falls through to the same second tier's weakest, "raw baseline" level for one end, which is a documented, accepted warning rather than an error but is still a materially worse-quality correction than the first tier would have given; a geometric scan found 11 of 111 routed nets in that same benchmark overshoot their own source/target port position by more than 5 micrometers, in a pattern consistent with an uncorrected or under-corrected terminal segment; and the repository owner independently spotted and screenshotted two visible geometry defects in the routed GDS output that plausibly share this same root cause (a bowtie/hourglass-shaped pinch in a route's width where it crosses a component footprint, and a small spurious loop in a route near a cluster of heater components) but that link has not yet been confirmed net-by-net.

The repository owner's own proposed design, stated directly in that session: a net with no crossings already gets one, consistent, checked correction; a net with one or more crossings should treat its port-to-first-crossing and last-crossing-to-port segments the same way -- as two independent, ordinary correction problems, using the exact same checked logic, not a separate, weaker mechanism. The first tier of today's cascade (`_checked_terminal_segment`, inside the same function, using the native `router.centerline_port_corrected_checked` call) already implements close to this design, but it does not fully cover every case today, and this plan's first job is finding out exactly why, with real evidence, before deciding what to change.

After this plan: every crossing-aware endpoint correction either succeeds via one clearly-understood, checked strategy applied uniformly to a net's two terminal segments (matching how a crossing-free net is corrected), or fails with a real, honest, geometrically-explained error -- never by silently accepting a fallback whose own quality was never actually verified against the port it claims to reach. A person can see this working by re-running `multiportmmi_8x8` under both its bare-defaults and stable-baseline (`--crossings true --crossing-mode lidar-pure --fanout-access-mode static-stubs --routing-window-scale 0.35 --foreign-port-keepout-cells 0`) configurations (exact commands in Concrete Steps) and observing that the `endpoint_correction_fallback_used` warning this plan starts from is gone (or replaced by a different, better-understood outcome with a recorded reason), that the geometric overshoot measurement described in Context returns zero or a much smaller, explained count, and that the two screenshotted visual defects are either resolved or conclusively shown to be unrelated to this cascade (with evidence either way, not a guess).

## Progress

- [ ] Milestone 1: root-cause, with direct evidence, every open question from the prior session's investigation -- see Context and Orientation's "Exactly what is still unknown" subsection for the precise list. Read-only; no code changes.
- [ ] Milestone 2: present the repository owner with a small number of concrete design options for the cascade, informed entirely by Milestone 1's findings (not decided in advance by this plan), and record their choice.
- [ ] Milestone 3: implement the chosen design.
- [ ] Milestone 4: validate against the full benchmark ladder, confirm the specific symptoms named in Purpose are resolved or conclusively explained, and update `.agent/REPOSITORY_STATE.md`.

## Surprises & Discoveries

(carried over from the prior session's investigation, recorded here so this plan is self-contained; add new entries below as this plan's own milestones proceed)

- Observation: endpoint correction is not a single pipeline phase. It is invoked from three separately-evolved call sites in `translation/route_rust.py`, all after the *entire* batch of nets has already been routed (never per-net, mid-routing): `_finalize_routing_results` (line 7184 as of this writing, the primary pass, called immediately after `_dispatch_native_routing` finishes routing and repairing every net in `run()`), `_repair_final_illegal_crossings` (line 5640, triggered only for nets touched while repairing an illegal realized crossing, later in the pipeline), and `_repair_final_photonic_issues` (line 6068, triggered only for nets touched while repairing a general photonic-verification issue, later still). A net's final corrected geometry can therefore be the product of up to three separate endpoint-correction invocations at different pipeline stages, against whatever the obstacle/repair state happened to be at each point.
  Evidence: `grep -n "_apply_all_endpoint_corrections_for_net_ids(\|_apply_checked_endpoint_corrections_for_net_ids(\|_apply_checked_fanout_stub_endpoint_corrections_for_net_ids(\|_apply_unrestricted_and_fanout_stub_endpoint_corrections_for_net_ids(" translation/route_rust.py` and matching each call site's line number to its enclosing method.

- Observation: `_classify_net_for_endpoint_correction` (`translation/route_rust.py:4537`) decides, per net, which of three correction paths applies: `UNRESTRICTED` (a crossing-free net, corrected by the native `self.router.apply_checked_endpoint_corrections` call inside `_apply_checked_endpoint_corrections_for_net_ids`, `translation/route_rust.py:4619`), `FANOUT_STUB_*` (a net anchored to a multi-port fanout instance, not otherwise discussed in this plan), or `CROSSING_AWARE` (any net with `self.enable_crossings and net_id in crossing_net_ids`, corrected by `_apply_crossing_aware_endpoint_correction_to_record`, `translation/route_rust_endpoint_correction.py:1088`). This plan is entirely about the `CROSSING_AWARE` path; the `UNRESTRICTED` path is the "already sound, already checked" reference behavior the repository owner's proposed design wants the crossing-aware terminal segments to match.
  Evidence: `translation/route_rust.py:4537-4617`.

- Observation: `_apply_crossing_aware_endpoint_correction_to_record` has a zero-crossing early branch (`translation/route_rust_endpoint_correction.py:1124-1221`) that, for a net classified `CROSSING_AWARE` but whose specific route happens not to have any crossing points on it after all, defers to `apply_port_endpoint_corrections` -- a Python-level function, not the same native call the `UNRESTRICTED` path uses, but conceptually the same "one checked correction over the whole route" shape. The interesting case this plan is about is the non-empty-`crossing_points` branch starting at `translation/route_rust_endpoint_correction.py:1223`.
  Evidence: `translation/route_rust_endpoint_correction.py:1124-1236`.

- Observation: for the non-empty-crossing case, the function first computes a three-way split of the route's raw, as-routed centerline (`baseline`) via `_crossing_endpoint_splice_parts` (`translation/route_rust_endpoint_correction.py:183-264`): a `prefix` (port to just past the first crossing, cut at most 4 length-units into the crossing's protected zone -- see `_guarded_cut`, same file, lines 214-245), a `middle` (the crossing-safe zone itself, always left as raw, uncorrected geometry, never touched by any correction tier), and a `suffix` (just before the last crossing to the target port, same 4-unit guard). If either `prefix` or `suffix` ends up with fewer than 2 points after this cut, `_crossing_endpoint_splice_parts` itself returns `None` for the *entire* split (`translation/route_rust_endpoint_correction.py:262-263`), not just the short side -- meaning tier 1 (below) is skipped entirely for both ends whenever either one is too short, not only the short one.
  Evidence: `translation/route_rust_endpoint_correction.py:183-264`.

- Observation: when the split succeeds, tier 1 (`_checked_terminal_segment`, a nested function at `translation/route_rust_endpoint_correction.py:1266-1334`, called separately for `prefix` at line ~1342 and `suffix` at line ~1351) calls the native `router.centerline_port_corrected_checked`, passing the *segment itself* as the route to correct and the real port plus the crossing-cut point as its two endpoints. This is architecturally the closest thing today's code has to the repository owner's proposed design. If both `_checked_terminal_segment` calls fail (or the initial split failed), or the combined checked candidate does not anchor-match the real ports (`_terminal_anchor_matches`, `translation/route_rust_endpoint_correction.py:668-680`, tolerance approximately 1e-6 units), the function falls through to tier 2.
  Evidence: `translation/route_rust_endpoint_correction.py:1266-1422`.

- Observation: tier 2 (the `_candidate`/`candidate_modes` loop, `translation/route_rust_endpoint_correction.py:1424-1501`) tries up to 4 combinations of `(use_source, use_target)` -- `(True,True)`, `(True,False)`, `(False,True)`, `(False,False)` -- each computing a single, *unchecked* correction over the entire route (`route_port_corrected_centerline`, note the caller's own `allow_unchecked_bumps` parameter name) and splicing it onto the crossing-safe `middle` via `_spliced_crossing_endpoint_centerline` (`translation/route_rust_endpoint_correction.py:682-868`), which has its own further 3-level fallback per end: the spliced rich correction itself, then `_absorbed_terminal_centerline` (a second, independent, "weaker, guard-window-limited" solver, same file lines 439-...), then, if that also fails, the raw uncorrected `baseline_prefix`/`baseline_suffix` unchanged. Until 2026-08-24 (commit `2755a22`), this tier's acceptance check (`_realization_accepts`) verified only that the resulting shape realizes as a valid waveguide polygon -- never that it actually reaches the real port, unlike tier 1's `_terminal_anchor_matches` check. This let a candidate identical to one tier 1 had already correctly rejected be silently accepted.
  Evidence: `translation/route_rust_endpoint_correction.py:1424-1501` (post-fix version, with the `source_ok`/`target_ok` checks added); the pre-fix trace and the fix itself are documented in full in `.agent/execplans/2026-08-24-crossing-cost-function-soundness.md`'s Surprises & Discoveries and the commit `2755a22` message.

- Observation: `n_67` in `multiportmmi_8x8` bare-defaults is a fully traced, concrete example of tier 1 failing for two different reasons on its two ends, both still not root-caused to the level this plan requires. Full trace (captured via the existing `PHOTONIC_ROUTER_TRACE_ENDPOINT_CORRECTION_NETS=n_67` diagnostic hook, `translation/route_rust_endpoint_correction.py:1108-1122`), before the tier-2 anchor-match fix:
      endpoint_trace net=n_67 id=68 mode=checked_terminal_segment segment_len=18 segment_start=(2501.5, 993.125) segment_end=(2517.5, 987.125) source_port=(2499.4, 992.5) target_port=(2517.5, 987.125) status=reject error=No port endpoint correction candidates found for centerline segment
      endpoint_trace net=n_67 id=68 mode=checked_terminal_segment segment_len=22 segment_start=(2537.5, 705.125) segment_end=(2607.5, 649.125) source_port=(2537.5, 705.125) target_port=(2609.4, 650.0) status=accept candidate_label=normal_segment corrected_len=22
  The *suffix* segment (18 points, a real, non-trivially-short segment) was actively rejected by the native `centerline_port_corrected_checked` call with the message `"No port endpoint correction candidates found for centerline segment"` -- this is a native-side (Rust) rejection whose exact cause (what "candidates" means, what geometric condition made none available) was not investigated at all in the prior session; it could be a real geometric impossibility, a native-side bug, or a mismatch between what this segment call expects and what it was given. The *prefix* segment produced **zero** trace output. The prior session guessed this meant the prefix was too short (`len(segment) < 2`), but a closer reading of `_checked_terminal_segment`'s own guard clauses (`translation/route_rust_endpoint_correction.py:1277-1280`) shows at least one other silent-return condition -- `source_port_um is None or target_port_um is None` -- and, more importantly, that the call to `_checked_terminal_segment` for the prefix only happens *at all* if `correct_source` (the whole function's own parameter, not a per-mode flag) is true (`translation/route_rust_endpoint_correction.py:1342-1349`: `checked_prefix = (_checked_terminal_segment(...) if correct_source else ())`). The prior session's "too short" theory was never actually confirmed against either of these alternative explanations. **This plan's Milestone 1 must resolve this with direct evidence, not carry the guess forward as fact.**
  Evidence: `/tmp/n67_trace.log`, this session's own scratch output (not preserved in the repository -- Milestone 1 must reproduce it, not assume its numbers are still current, since Milestone 1 changes this session already changed `n_67`'s downstream candidate outcome, though not its tier-1 rejection reasons, which are upstream of that fix).

- Observation: a geometric scan (scratch script, this session, not preserved in the repository) of `multiportmmi_8x8` stable-baseline (`--crossings true --crossing-mode lidar-pure --fanout-access-mode static-stubs --routing-window-scale 0.35 --foreign-port-keepout-cells 0`) found 11 of 111 routed nets whose corrected centerline's y-coordinate range extends more than 5 micrometers beyond the wider of its own source/target port y-range: `n_69` (128.88um), `n_110` (55.62um), `n_31` (46.38um), `n_68` (38.88um), `n_54` (29.62um), `n_35` (29.62um), `n_65` (12.00um), `n_4`/`n_3`/`n_11`/`n_10` (12.00um each). `n_68` appears in both this list and the endpoint-correction-fallback-warning list from the same run, which is circumstantial evidence, not proof, that the two phenomena share a cause -- Milestone 1 must check each of the other 10 nets' endpoint-correction classification and tier outcome directly, not assume the whole list shares `n_68`'s specific path.
  Evidence: this session's own scratch script and its printed output (not preserved in the repository -- Milestone 1 must reproduce this measurement, ideally as a real, reusable diagnostic rather than a disposable script this time, given this plan's own Purpose wants it as a repeatable acceptance check).

- Observation: the repository owner independently found and screenshotted two visible geometry defects in `build/routed_multiportmmi_8x8.gds` during this session, described but not yet mapped to specific net IDs or a confirmed root cause: (1) a route's width pinches into a bowtie/hourglass shape exactly where it overlaps a component's crossing footprint (the component footprint itself rendered as a blue-outlined lens/cross shape, the route rendered in pink pinching to near-zero width at the footprint's center before widening again on the far side); (2) a route makes a small loop -- overshooting past a point and looping back -- near a cluster of what appear to be heater/coupler components, visible as a small closed or near-closed hook shape immediately before the route reaches one of several parallel component rows. The repository owner also separately observed, in the same area, a route taking a long detour around the right side of a component cluster instead of a shorter path on the left, which they found suspicious but which may be an unrelated, legitimate congestion-driven routing decision rather than an endpoint-correction defect -- Milestone 1 should check this specifically, not assume it belongs to this plan's scope.
  Evidence: two screenshots shared directly in this session's chat (not saved as files in this repository or its scratch space); described here in enough detail to identify the same visual pattern when re-inspecting a fresh GDS render.

### Exactly what is still unknown (Milestone 1's job)

1. Why did the native `centerline_port_corrected_checked` call reject `n_67`'s 18-point suffix segment with `"No port endpoint correction candidates found for centerline segment"`? Is this a genuine geometric impossibility (no valid corrected path exists within whatever search space that native function considers), or a bug/mismatch in how the segment or its port arguments are constructed before being passed in?
2. Why did `n_67`'s prefix segment produce zero trace output -- was `_checked_terminal_segment` never called at all because `correct_source` was false for this invocation (and if so, why, and is that itself correct or a bug), or did it return silently from one of its own early guards (`len(segment) < 2`, or a `None` port)? This requires either adding a temporary diagnostic print inside `_checked_terminal_segment`'s early-return branches, or tracing `correct_source`'s value at the call site, not guessing from the trace format alone.
3. Of the 11 nets found by the y-overshoot scan, which are `CROSSING_AWARE`-classified, which tier of the cascade actually produced their final geometry, and does that tier's choice explain the specific overshoot shape in each case (a genuine geometric necessity given the crossing's position, versus an artifact of a weak fallback tier)?
4. Do the repository owner's two screenshotted defects (the bowtie/hourglass pinch, the spurious loop near heaters) correspond to specific, identifiable nets, and if so, which endpoint-correction tier produced their geometry? This requires reproducing a fresh `multiportmmi_8x8` GDS render under whichever configuration was used for the original screenshots (most likely the stable-baseline crossing configuration, since the defects appear crossing-adjacent, but this must be confirmed, not assumed -- the original screenshots did not record the exact command used) and visually or programmatically locating the same defect, then cross-referencing its net ID against the endpoint-correction trace for that net.
5. Is the "route detours right instead of left around a component cluster" observation actually related to endpoint correction at all, or is it a separate, legitimate (or separately-buggy) routing/repair decision? A quick check: does disabling endpoint correction entirely for that specific net (or comparing its pre-correction and post-correction centerlines) change that detour, or was the detour already present in the raw A* output before any correction ran?

## Decision Log

(none yet -- Milestone 2 is where the repository owner chooses among options Milestone 1's findings will produce; do not pre-decide the design here)

## Outcomes & Retrospective

Not started.

## Context and Orientation

### Key terms, defined for a reader new to this repository

"Net": one logical point-to-point waveguide connection to be routed, identified by an integer `net_id` and a human-readable name like `n_67`.

"Centerline": the sequence of `(x, y)` coordinate points (in micrometers) tracing the middle of a routed waveguide's path. A `RoutedNetRecord` (defined in `translation/route_rust_types.py`) carries both an uncorrected, grid-derived centerline (reconstructed from the route's primitive sequence via `_primitive_centerline_for_record`) and, after endpoint correction runs, a `corrected_centerline_um` field.

"Endpoint correction": the pipeline stage, entirely in Python (`translation/route_rust_endpoint_correction.py` and its callers in `translation/route_rust.py`), that adjusts a route's centerline near its two ends so they land exactly on the real port position and angle rather than merely the nearest grid-aligned point.

"Crossing": a point where two different nets' waveguide centerlines physically cross on the chip, legal under specific geometric conditions when `enable_crossings` is on. A net can have zero, one, or several crossings along its route.

"Tier" (this plan's own shorthand, not an existing repository term): one of the several distinct correction strategies `_apply_crossing_aware_endpoint_correction_to_record` tries in sequence for a `CROSSING_AWARE`-classified net -- "tier 1" for the checked, per-segment strategy (`_checked_terminal_segment`), "tier 2" for the unchecked, whole-route-then-spliced strategy (the `_candidate`/`candidate_modes` loop), and "tier 2's own three levels" for the further fallback inside `_spliced_crossing_endpoint_centerline` (rich-corrected, then `_absorbed_terminal_centerline`, then raw baseline).

### Files this plan concerns

`translation/route_rust_endpoint_correction.py` -- contains essentially all of the logic this plan is about: `_apply_crossing_aware_endpoint_correction_to_record` (line 1088), `_checked_terminal_segment` (nested inside it, line 1266), the `_candidate`/`candidate_modes` loop (line 1424), `_spliced_crossing_endpoint_centerline` (line 682), `_absorbed_terminal_centerline` (line 439), `_crossing_endpoint_splice_parts` (line 183), `_terminal_anchor_matches` (line 668), and the existing `PHOTONIC_ROUTER_TRACE_ENDPOINT_CORRECTION_NETS` diagnostic hook (lines 1108-1122 inside the main function, gating `print` calls scattered through the tiers).

`translation/route_rust.py` -- contains the three call sites (`_finalize_routing_results` line 7184, `_repair_final_illegal_crossings` line 5640, `_repair_final_photonic_issues` line 6068) and the per-net classification (`_classify_net_for_endpoint_correction`, line 4537).

`translation/photonic_verification.py` -- where `endpoint_correction_fallback_used` (a warning) and `endpoint_correction_error`/`missing_corrected_centerline`/`source_port_not_connected`/`source_endpoint_mismatch` (all errors) are actually checked and reported, around lines 380-425 as of the prior session's reading; re-confirm line numbers before citing them, since this file was not edited this session and may have shifted only slightly relative to other files that were.

### Toolchain and safety notes (identical to every other ExecPlan this session)

Any `src/*.rs` change (unlikely for this plan, since the native `centerline_port_corrected_checked` function is the one native call involved, and Milestone 1 is investigation-only) requires `RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu PYO3_PYTHON="$PWD/.venv/bin/python" cargo check --lib` followed by `.venv/bin/maturin develop --release` before Python-level testing sees it.

Do not run the `--debug-svgs` CLI flag with no value (or `all`) on `multiportmmi_8x8` or `multiportmmi_16x16` -- always pass a specific numeric net selector.

`multiportmmi_8x8` bare-defaults currently fails outright (`n_67`, documented in `.agent/execplans/2026-08-24-crossing-cost-function-soundness.md`) -- this is expected and correct given that plan's own fix; do not treat it as a new regression introduced by this plan unless this plan's own changes make a *different* net newly fail, or fail to make `n_67` succeed once this plan's fix is in place (which is one of the plausible, but not guaranteed, outcomes of Milestone 3 -- see Purpose).

`benes_8x8` and `benes_16x16` require `--crossings true --crossing-mode lidar-pure` to be representative -- their own benchmark files (`benchmarks/benes_8x8.py:23-25`, `benchmarks/benes_16x16.py:23-25`) document this as their "Stable crossing-router baseline." Bare CLI defaults are not representative for these two benchmarks specifically; this was a real mistake made twice in the prior session before being caught by the repository owner. Check every benchmark's own file for a similar comment before assuming bare defaults are representative, rather than repeating that mistake a third time.

This plan is independent of `.agent/execplans/2026-08-24-stabilize-16x16-benchmarks.md` (paused mid-Milestone-1, investigating `multiportmmi_16x16`'s repair-exhaustion failure, whose failure signature moved from `n_50` to `n_49` during the prior session -- see that plan's own corrected Milestone 0 note). They may plausibly interact, since `multiportmmi_16x16` also uses the crossing-aware endpoint-correction cascade this plan is about, but resolving one is not a prerequisite for the other; note anything discovered about that interaction in this plan's own Surprises & Discoveries rather than assuming it belongs to the other plan.

## Plan of Work

**Milestone 1** answers, with direct evidence (trace output, a small temporary diagnostic print statement if the existing trace hook does not cover a needed branch, or a geometric measurement), every item in Context and Orientation's "Exactly what is still unknown" list above. This milestone is read-only with respect to production code -- temporary diagnostic prints added to investigate item 2 must be reverted (or converted into a permanent, reviewed addition to the existing `PHOTONIC_ROUTER_TRACE_ENDPOINT_CORRECTION_NETS` hook, which is an acceptable, small, permanent improvement if it turns out the existing hook has a real blind spot) before this milestone is considered complete. For item 4, reproduce a fresh GDS render (`--debug-svgs <net_id>` with a specific net once a candidate net is suspected, or a full, non-debug GDS write if the defect's net is not yet known) and describe, in this plan's own Surprises & Discoveries, exactly how each screenshot's visual pattern was matched to a specific net ID and trace outcome -- or, if a defect cannot be matched to a specific net after a genuine, described attempt, record that explicitly rather than silently dropping the question.

**Milestone 2** is deliberately not pre-specified past "decide based on Milestone 1's findings, present options to the repository owner if a real design decision is needed" -- per `.agent/PLANS.md`'s guidance against over-specifying a design before the investigation that justifies it has happened, and matching this repository's own established practice for repair-strategy and cost-function design decisions this session (`.agent/execplans/2026-08-24-crossing-cost-function-soundness.md`'s own Decision Log is a model to follow: quote the repository owner's actual words, do not paraphrase a decision into something more convenient). Plausible candidate directions Milestone 1's findings might support -- none pre-committed to, all contingent on what is actually found -- include: extending tier 1 to correctly handle whatever case is actually blocking it (if item 1 or 2 above turns out to be a fixable gap rather than a genuine geometric impossibility); removing or substantially simplifying tier 2's multi-level internal fallback if it turns out to rarely or never produce a materially better result than an honest failure would; or unifying the three separate call sites in `translation/route_rust.py` into one consistent invocation pattern if Milestone 1's investigation of item 4 finds evidence that the three sites' different timing/context is itself contributing to a defect.

**Milestone 3** implements whichever design Milestone 2 settled on. Follow this repository's established Claude+Codex flow (`.agent/CLAUDE_CODEX_FLOW.md`) for any slice that is fully specified and bounded once Milestone 2's decision is recorded; keep diagnosis, design, and review with Claude, matching this session's own established division of labor.

**Milestone 4** validates the implemented change against the full ladder used throughout this session (`cargo test --lib`, `pytest -q`, `benes_4x4`, `multiportmmi_8x8` both configurations, `benes_8x8`/`benes_16x16` with `--crossings true --crossing-mode lidar-pure`, `multiportmmi_16x16` stable-baseline), re-runs the geometric y-overshoot measurement from Milestone 1 (ideally now a permanent, reusable diagnostic rather than a disposable script) to confirm the count drops to zero or a much smaller, individually-explained number, and re-inspects the GDS for the two specific screenshotted defects to confirm they are resolved (or, if a defect turns out to be unrelated to this cascade per Milestone 1's finding, explicitly re-confirms that conclusion rather than silently dropping it). Updates `.agent/REPOSITORY_STATE.md` with the plan's outcome, following the same pattern used by every other ExecPlan completed this session.

## Concrete Steps

The exact commands for Milestone 1's investigation reuse this session's own established patterns -- do not invent a new diagnostic style where an existing one already works:

    cd /home/benjamin/Documents/Repositories/working/TUMPhotonicRouter
    PHOTONIC_ROUTER_TRACE_ENDPOINT_CORRECTION_NETS="n_67" \
    PYTHONPATH=. .venv/bin/python -u routing_flow.py multiportmmi_8x8 2>&1 | grep endpoint_trace

For the stable-baseline configuration (needed for items 3 and 4, and for `n_68`):

    cd /home/benjamin/Documents/Repositories/working/TUMPhotonicRouter
    PHOTONIC_ROUTER_LONG_STRAIGHT_CONGESTION_WEIGHT="0.05" PHOTONIC_ROUTER_FANOUT_STUB_BEND_DEGREES="90" \
    PHOTONIC_ROUTER_TRACE_ENDPOINT_CORRECTION_NETS="n_68,n_69,n_31,n_110,n_54,n_35,n_65,n_4,n_3,n_11,n_10" \
    PYTHONPATH=. .venv/bin/python -u routing_flow.py multiportmmi_8x8 \
      --crossings true --crossing-mode lidar-pure --fanout-access-mode static-stubs \
      --routing-window-scale 0.35 --foreign-port-keepout-cells 0 2>&1 | grep endpoint_trace

For item 2 (the prefix-silence question), reading `_checked_terminal_segment`'s own source (`translation/route_rust_endpoint_correction.py:1266-1334`) alongside its two call sites (lines ~1342 and ~1351) to see the actual `correct_source`/`correct_target` values passed into the enclosing function for `n_67`'s specific invocation is the more direct approach than adding new prints -- trace which of `_finalize_routing_results`, `_repair_final_illegal_crossings`, or `_repair_final_photonic_issues` actually produced `n_67`'s final correction (only one of the three call sites' invocations matters for the final committed result, but more than one may have run against it over the pipeline's lifetime; determine which one is authoritative for `n_67`'s case with direct evidence, e.g. a print inside `_apply_crossing_aware_endpoint_correction_to_record` itself recording its own `correct_source`/`correct_target` arguments whenever `trace_endpoint` is true, which is a natural, minimal extension of the existing trace hook).

## Validation and Acceptance

Milestone 1: every item in "Exactly what is still unknown" has a recorded answer in Surprises & Discoveries, with direct evidence (a trace line, a measurement, a described reproduction) for each -- not a restatement of the question, and not a guess presented as a conclusion.

Milestone 2: a recorded decision in the Decision Log, quoting the repository owner's own words for the chosen direction, matching this session's own established citation discipline.

Milestone 3: the chosen design is implemented; `cargo test --lib` and `pytest -q` both pass at or better than this plan's own starting baseline (`394 passed` / `11 failed, 335 passed, 1 skipped` respectively, as of `.agent/execplans/2026-08-24-crossing-cost-function-soundness.md`'s own close-out).

Milestone 4: the full validation ladder passes (or fails only for already-documented, unrelated reasons -- `multiportmmi_16x16`'s `n_49`/`n_50` repair-exhaustion failure, tracked separately); the y-overshoot re-measurement and the two screenshotted-defect re-inspections each have a recorded, evidence-backed outcome (resolved, or conclusively unrelated) rather than an unverified assumption either way.

## Idempotence and Recovery

All of Milestone 1's investigation is read-only with respect to the repository (diagnostic env vars and scratch scripts only; any temporary print statement added to `translation/route_rust_endpoint_correction.py` for item 2 must be reverted or turned into a reviewed, permanent addition before Milestone 1 is marked done, not left as an uncommitted stray edit). If Milestone 3's implementation causes an unexpected validation-ladder regression, do not proceed to Milestone 4 until it is understood and fixed, following this session's own established practice of isolating a regression's cause (a temporary `git worktree` at the pre-change commit, as used repeatedly in `.agent/execplans/2026-08-24-crossing-cost-function-soundness.md`, is the proven technique for this) rather than guessing.

## Artifacts and Notes

(to be filled in as Milestone 1 produces trace output, measurements, and GDS-inspection evidence worth preserving -- consider whether the y-overshoot scan script should be promoted from a disposable scratch script into a real, checked-in diagnostic this time, given this plan's own Purpose treats it as a repeatable acceptance check, not a one-off)

## Interfaces and Dependencies

None prescribed yet -- Milestone 1 is read-only investigation; Milestone 2's scope, and therefore Milestone 3's actual interface changes (if any), depend entirely on what Milestone 1 finds. Do not assume in advance that the three call sites in `translation/route_rust.py` need unifying, that tier 2 needs removing, or that tier 1 needs extending -- any of these, none of these, or something else entirely could be the right answer, and Milestone 1's job is finding out which.
