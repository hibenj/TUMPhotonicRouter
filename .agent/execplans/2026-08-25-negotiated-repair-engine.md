# Replace the repair-dispatch chain with a general, negotiated-congestion repair engine

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This document must be maintained in accordance with `.agent/PLANS.md`, which is checked into this repository at that path and describes the required structure and editing discipline for ExecPlans in general.

## Purpose / Big Picture

This repository, TUMPhotonicRouter, routes optical waveguides for photonic chips on a discrete grid. When a net's own search fails to find a route (because another, already-routed net is in the way), a repair process has to "negotiate": decide which net yields, clear space, and try again. Today that negotiation is implemented as `route_many_with_repair_and_commit` (`src/py_router.rs`, currently starting around line 11555), a per-net dispatch chain that tries up to 17 separately-named `try_*` methods in a fixed sequence, each hand-built to handle a specific situation somebody hit while debugging a specific benchmark (`benes_4x4`, `multiportmmi_8x8`, etc.). None of these 17 methods are dead code -- `.agent/execplans/2026-08-25-unify-astar-kernel-and-clean-repair-baseline.md`'s Milestone 5 already verified that precisely, by tracing every call site -- but "not dead" is not the same as "a coherent algorithm." The repository owner's own words, said directly in the conversation that produced this plan: *"just becaus[e] all these strategies are somewhat called doesnt mean the[y] are really necessary or that the behaviour is kinda 'wild'."* Several benchmark files (`benchmarks/multiportmmi_8x8.py`, `benchmarks/multiportmmi_16x16.py`, `benchmarks/benes_8x8.py`, `benchmarks/benes_16x16.py`) even hardcode their own `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` bundle of specific environment-variable values (for example `PHOTONIC_ROUTER_LONG_STRAIGHT_CONGESTION_WEIGHT=0.05`) empirically found to make that specific benchmark route cleanly -- a visible symptom of an algorithm that needs per-benchmark tuning rather than working generally.

This plan's purpose is to replace that 17-method dispatch chain with a small number of general, composable repair mechanisms built around a real, principled algorithm: negotiated-congestion rip-up/reroute, the same family of algorithm real router literature (and a sibling reference implementation this plan studies directly, see Context and Orientation) uses. After this plan: a net's repair path is a short, readable loop a person can describe in a few sentences (route nets in a principled order; when a net fails, mark every net it conflicted with as needing a retry; apply a real, accumulating cost penalty to cells that caused conflicts so the next attempt avoids them; iterate until every net routes or a round limit is hit), not a sequence of 17 named special cases. A person can see this working by reading `route_many_with_repair_and_commit` and finding that short loop instead of the current dispatch chain, and by running the benchmark ladder (exact commands in Concrete Steps) with each benchmark's own *plain* configuration and observing it route as cleanly as it does today with the hand-tuned `STABLE_ROUTING_ENV` bundle -- the concrete, falsifiable signal that the new algorithm is actually general rather than differently benchmark-specific.

## Progress

- [x] (2026-08-25) Milestone 1: complete. Investigated (a) the current 17-method repair dispatch chain's real call structure and (b) a sibling reference implementation's repair algorithm, in detail, as the basis for design. Full findings in Surprises & Discoveries below.
- [ ] Milestone 2: design the target algorithm precisely for this codebase (not a literal port -- this repository's language, A* kernel, and crossing-legality machinery are different and already validated) and validate the design's feasibility with a small, bounded prototype before committing to full implementation.
- [ ] Milestone 3: implement topological net ordering, replacing today's raw netlist declaration order, as an isolated, independently-validated first step.
- [ ] Milestone 4: make history-cost application systematic (every successful commit penalizes the cells it used) instead of today's 3 narrow call sites, as an isolated, independently-validated step.
- [ ] Milestone 5: implement the global iterate-until-converged repair loop (mark conflicting nets, rip them up together, requeue, retry with accumulated cost) and migrate `route_many_with_repair_and_commit` to use it, keeping the old dispatch chain available for direct comparison until the new loop is validated across the full benchmark ladder.
- [ ] Milestone 6: implement principled, cost/distance-based conflict resolution (which of two conflicting nets should yield) to replace the remaining hand-tuned decision points, and remove the old 17-method dispatch chain plus every helper that existed only to serve it.
- [ ] Milestone 7: attempt to remove or shrink the per-benchmark `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` bundles now that the general algorithm should need less tuning; record honestly whatever the outcome is, including if some tuning remains genuinely necessary.
- [ ] Milestone 8: full validation ladder, `.agent/REPOSITORY_STATE.md` update, retrospective comparing the final state against this plan's own Purpose.

## Surprises & Discoveries

### Milestone 1: the current repair dispatch chain's real structure

`route_many_with_repair_and_commit` (`src/py_router.rs`) dispatches, per net, in order: `try_plain_normal_route` -> (if lidar-pure crossing routing is enabled and a `pending_straight_victim_hint_for` hint exists) `try_source_layer_center_out_repair` then `try_pending_straight_victim_repair` -> `try_lidar_direct_crossing_subset` -> `probe_net_for_repair` (builds a trial route and a **hand-assigned integer priority score**: realized-violation partners get 0, dynamic blockers get 0, static-blocker-event partners get 1, expected-pairs uncrossed partners get 2) -> `try_localized_crossing_keepout_retry` -> `try_commit_clean_probe` -> a **round-based loop** that brute-force-enumerates 4 orderings (`victim_first` x `reverse_victim_order`) per victim set, restoring a full obstacle-map/committed-routes snapshot between attempts, advancing rounds by **cumulatively adding more victims to the same fixed priority list** (never re-scoring based on what happened last round) -> `try_final_repair_fallback`.

Of the (as of Milestone 5) 17 `try_*` methods, a call-graph investigation (this same session, `.agent/execplans/2026-08-25-unify-astar-kernel-and-clean-repair-baseline.md` Milestone 5's own Surprises & Discoveries) found that 4 are not repair-dispatch-scoped at all (they are called from `route_single_net_and_commit_native`, the core per-net routing primitive, and must not be touched by this plan either), and the remaining 13 cluster into 5 groups by actual mechanism, not by name: bulk column-wide reroute (2 methods), crossing-partner search primitives that all wrap the same underlying crossing search with different partner subsets (3 repair-scoped methods, after excluding the 4 core-routing ones), single-victim ripup/reroute (4 methods, of which one -- `try_ripup_single_victim_and_reroute` -- is the only real primitive; the other 3 are thin wrappers or ordering variants of it, and the ordering variation they encode is *also* separately brute-forced by the outer round loop), a localized keepout-window retry (1 method), and terminal bookkeeping (commit-clean-probe, plain-first, final-fallback -- not really "repair").

**Verdict, in the repository owner's own words, confirmed by direct evidence, not assumed**: this is priority-computed-once, then brute-force enumeration of orderings/reservations with full snapshot/restore as the undo mechanism -- not a negotiation loop. Evidence: `prepare_repair_attempt` (`src/py_router.rs`, computes `repair_victim_sets` once via `compute_repair_victim_sets`); the round loop's own `for victim_first in [false, true] { for reverse_victim_order in [false, true] { ... } }` structure; `enqueue_targeted_illegal_crossing_repair_set`, which reactively appends victim IDs parsed out of *error message strings* mid-loop -- a string-parsing-driven adaptive mechanism, not a cost signal.

### Milestone 1: a sibling reference implementation with a real, principled algorithm

`/home/benjamin/Documents/Repositories/working/LiDAR` is a local checkout of the official implementation of "LiDAR: Automated Curvy Waveguide Detailed Routing for Large-Scale Photonic Integrated Circuits" (ISPD 2025, Zhou/Zhu/Gu). This is very likely the direct research-paper ancestor of this repository's own "lidar-pure" crossing-mode terminology -- its own benchmark generator at `src/picroute/benchmarks/MMIports.py` literally produces netlists named `f"multiportmmi_{N}x{N}"`, matching this repository's own `multiportmmi_8x8`/`multiportmmi_16x16` benchmarks by name. Its repair engine (`src/picroute/routing/drgridroute.py`, function `runNRR`, "route with ripup and reroute") is a real, working example of the algorithm family this plan wants to adopt the principles of (not literally port -- LiDAR is Python-only with its own A* kernel and data model, this repository has a Rust kernel already validated and staying in place):

- **Net ordering is topological, computed once.** `process_net_order` (`drgridroute.py:74`) walks the schematic's topology levels (`self._cirdb.topology_orders`) and assigns each net a `comp_dist` (topology depth). `Nets.__lt__` (`database/schematic.py:267`) sorts by `comp_dist` first, then declaration order as a tiebreak -- so nets closer to the signal source route before nets further downstream. This repository already computes equivalent topology metadata (`node_depths`, used by `translation/route_rust_crossing_plan.py`'s topology analysis) but `route_many_with_repair_and_commit` routes nets in raw netlist declaration order, not topological order -- already flagged, unaddressed, earlier this session.
- **History cost is applied systematically, on every successful commit, not from 3 narrow call sites.** `registar_net` (`drgridroute.py:443`), called every time any net successfully routes, unconditionally calls `updateHistoryMap` (`drgridroute.py:703`), which increments a per-cell-per-orientation history cost (`historyMap[x][y][ori_index] += historyCost`) for every cell the just-committed route used. This repository's own `add_history_for_native_route`/`add_history_cells` mechanism (`src/py_router.rs`) does the identical *kind* of thing (confirmed: `history_weight` is threaded through nearly every A*-config builder in the file, and a nonzero value does discourage future searches from reusing penalized cells) but is only invoked from 3 specific call sites inside specific repair strategies, not systematically on every commit.
- **Failure handling is one global loop with real negotiation, not per-net special cases.** `mark_conflict_nets` (`drgridroute.py:690`) adds *both* the failing net and every net it conflicted with to a single `global_failedNet` set. At the end of every iteration, `ripupfailedNets` (`drgridroute.py:455`) clears every net in that set and requeues it for the next iteration's pass through the priority queue -- now discouraged from repeating the same conflict by the history cost the previous attempt already accumulated. `runNRR` (`drgridroute.py:117`) is the outer loop: for up to `maxIteration` rounds, drain the whole net priority queue (routing each net with strict design-rule checking first, falling back to relaxed checking), then rip up and requeue every net that ended up in the failed set, repeat.
- **A genuine escape hatch exists for non-convergence.** `ripupfailedNets` resets the *entire* history-cost map to zero (`clearHistoryMap`, `drgridroute.py:685`) after the 2nd global ripup round (`if self._ripup_times == 2`) -- an explicit, deliberate "give the search a clean slate" mechanism if accumulated history cost itself has become a local-optimum trap, not an accident.
- **Escalation is failure-count-driven, not net-ID-driven.** `Nets.clearNet` (`database/schematic.py:216`) increments `failed_count` on every clear; once `failed_count > 4`, `enable_45` (whether the net's search may use 45-degree diagonal primitives) turns off, widening the effective search space for a net that keeps failing -- a general relaxation rule, not a special case naming a specific net or benchmark.
- **Conflict resolution between two nets contesting the same crossing uses a real distance-based heuristic**, not a hardcoded rule: `routeSingleNet`'s `clear` condition (`drgridroute.py:279`) compares `cur_net.failed_count == 0` against whether the crossing partner has significantly more Euclidean slack (`net.distance["Euler"] > cur_net.distance["Euler"] + 1000`) to decide whether the *partner*, not the current net, should be the one ripped up and rerouted around.

**Comparison, stated plainly**: this repository's repair engine has none of these five properties as a general mechanism. It has narrow, hand-built approximations of some of them (the `history_weight` machinery is real but underused; `probe_net_for_repair`'s hand-assigned integer priorities are a crude, non-cost-based stand-in for a real priority signal), assembled ad hoc, one bug at a time, rather than designed as one coherent loop. This is the concrete, evidence-backed answer to why 17 special-cased methods exist: without a real negotiation loop underneath, every new failure pattern needed its own bespoke method instead of being handled by the general mechanism.

### Milestone 1: known counter-examples outside the repair dispatch chain

The repository owner asked directly, in the conversation that produced this plan, whether other parts of the codebase also follow benchmark-specific rules rather than general principles. Two concrete, already-identified instances exist outside `route_many_with_repair_and_commit` itself, both evidence of the same underlying pattern this plan's redesign targets (a knob or magic value substituting for a general mechanism), and both are folded into this plan's later milestones rather than left as loose ends:

- **`routing_flow_config.py`'s `SCRIPT_*` constants are not neutral defaults -- they are `benes_16x16`'s own tuned configuration, applied blanket to every benchmark that does not explicitly override it.** Evidence: `SCRIPT_BENCHMARK = "benes_16x16"` (`routing_flow_config.py:22`) is the literal first constant in the block, followed immediately by `SCRIPT_ALLOW_45_DEGREE_TURNS`, `SCRIPT_BEND_RADIUS_UM`, `SCRIPT_MIN_STRAIGHT_CELLS_PER_CROSSING`, `SCRIPT_FOREIGN_PORT_KEEPOUT_CELLS`, `SCRIPT_FANOUT_ACCESS_MODE`, `SCRIPT_ROUTING_WINDOW_SCALE`, and more (`routing_flow_config.py:22-41`) -- a config block named and shaped around one specific benchmark's needs, silently inherited by every other benchmark's CLI invocation unless that benchmark's own file documents an override (only `benes_8x8`, `benes_16x16`, `multiportmmi_8x8`, `multiportmmi_16x16` currently do, via their own `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS`). Milestone 7 (below) already targets removing the need for per-benchmark tuning bundles; this finding means Milestone 7 must also check whether the `SCRIPT_*` defaults themselves stop needing to be one specific benchmark's tuned values once the general algorithm lands, not only whether the separate `STABLE_ROUTING_ENV` overrides become unnecessary.
- **`RipupRerouteConfig.enabled` (`translation/route_rust_types.py:295-300`) is a user-toggleable boolean (`enabled: bool = True`, alongside `max_rounds: int = 4`, `max_victims_per_failure: int = 8`, `history_weight: float = 2.0`, `history_increment: int = 1`) for whether repair may run at all.** This was already flagged, unaddressed, earlier this session: the router structurally needs repair whenever a plain search fails (confirmed: even `benes_4x4`/`benes_8x8` fail immediately without it), so "should repair be allowed to run" is not really a user choice, only "should repair be *isolated out* for testing/diagnostic purposes" is (the one legitimate use this repository already has for disabling it, e.g. `test_benchmarks_route_with_astar_only`). Once this plan's negotiated-congestion loop makes repair an integral part of routing rather than a fallback phase bolted on after a separate dispatch chain, this toggle stops making conceptual sense as a general-purpose config knob. Resolving it (either removing the toggle in favor of an explicit test-only bypass, or leaving it with a corrected doc comment explaining the one legitimate use) is folded into Milestone 6, where the old dispatch chain's config surface is being cleaned up anyway.

## Decision Log

- Decision (2026-08-25, made by the repository owner in conversation, recorded here verbatim in spirit): do not treat "each of the 17 methods is called from somewhere" as evidence the repair engine is well-designed. Reachability is a correctness bar, not a design bar. This plan exists because of that distinction.
- Decision (2026-08-25, made by the repository owner in conversation): the target is a genuinely general repair strategy, explicitly *not* one shaped by getting today's specific benchmarks to pass. Benchmark-passing remains supporting evidence used along the way (per this repository's own standing priority, see `.agent/PLANS.md`-adjacent project memory), not the design driver. Milestone 7's attempt to shrink the `STABLE_ROUTING_ENV` bundles exists specifically to falsify (or confirm) that the new algorithm is actually general, not just differently benchmark-shaped.
- Decision (2026-08-25, made by the assistant, approved by the repository owner): adopt LiDAR's *principles* (topological ordering, systematic history cost, one global negotiation loop, failure-count-driven relaxation, distance-based conflict resolution), not a literal port. Rationale: this repository's Rust A* kernel (`src/astar.rs`, unified this same session) and crossing-legality machinery (`src/crossings.rs`, `crossing_move_outcome_with_segments` and friends) are already validated and different in specifics from LiDAR's own Python A* kernel (`src/picroute/routing/astarsearch.py`) -- reusing this repository's own kernel and porting only the repair *strategy* is far lower-risk than replacing the kernel too, and the kernel was not identified as a problem anywhere in this investigation.

## Outcomes & Retrospective

Not started beyond Milestone 1.

## Context and Orientation

### Key terms, defined for a reader new to this repository

"Repair" / "rip-up-reroute": when a net's own search attempt fails because another, already-committed net is in the way, the process of removing ("ripping up") that other net's route, then re-routing both nets, possibly across several rounds, until an arrangement is found where both succeed (or a round limit is reached and the net is reported as failed).

"Negotiated congestion": a repair strategy where, instead of a fixed sequence of special-cased attempts, every routing decision consults a shared, accumulating cost signal (here: a per-cell "history cost" that goes up every time a cell is used by a committed route) so that repeated conflicts over the same physical space become progressively more expensive to keep repeating, naturally pushing the search toward alternatives -- this is the general shape of the well-known "PathFinder" family of algorithms from FPGA/PCB routing literature, and it is what this plan's sibling reference implementation (LiDAR, see below) actually does.

"Topological order" / "topology depth": in this repository's netlist model, nets connect instance ports that form a directed graph (signal flows from inputs toward outputs through a chain of components). "Topology depth" is how many hops a net's source is from the netlist's true inputs; routing nets in increasing depth order means routing signal-source-side nets before the nets that depend on them being placed, matching the netlist's own natural dependency order instead of an arbitrary declaration order.

"The 17 `try_*` methods": every method in `src/py_router.rs` whose name starts with `try_`, as of the end of `.agent/execplans/2026-08-25-unify-astar-kernel-and-clean-repair-baseline.md`'s Milestone 5 (`grep -n "    fn try_[a-z_]*(" src/py_router.rs`; re-run this before trusting the exact count, since it may have changed since this plan started). 4 of these (`try_route_with_collision_crossings` and its 2 default-argument-chain siblings, plus `try_route_through_expected_crossing_partner`) are load-bearing core per-net routing infrastructure, not repair-dispatch strategies, and are out of scope for removal in this plan -- confirmed by direct call-graph tracing in the sibling plan's Milestone 5, not assumed.

### Files this plan concerns

`src/py_router.rs` -- `route_many_with_repair_and_commit` (the per-net dispatch loop), `prepare_repair_attempt` (the round-based loop), `probe_net_for_repair` (today's hand-assigned priority scoring), `compute_repair_victim_sets`, `add_history_for_native_route`/`add_history_cells` (today's narrow history-cost application), and all 13 repair-scoped `try_*` methods this plan's later milestones will consolidate or remove.

`src/astar.rs` -- the unified A* kernel (`.agent/execplans/2026-08-25-unify-astar-kernel-and-clean-repair-baseline.md`, complete through Milestone 5) and `AStarConfig.history_weight`/`long_straight_congestion_weight`, the existing cost-penalty machinery this plan's Milestone 4 will invoke more systematically. This plan does not need to modify `src/astar.rs` itself -- the kernel already supports history-cost-driven search; the gap is entirely in when and how often `src/py_router.rs` applies it.

`translation/route_rust_crossing_plan.py` -- `analyze_schematic_topology`/`build_crossing_plan`, which already compute topology metadata (`node_depths`, `node_ranks`, `edge_ranks`) this plan's Milestone 3 needs for topological net ordering. This metadata already exists and is already passed into the Rust router for crossing-plan purposes (see the `lidar-pure` topology-plan-leak fix earlier this session, `.agent/execplans/2026-08-24-endpoint-correction-cascade-soundness.md`'s sibling work, which is about *not* leaking this same metadata into crossing legality -- reusing it for net *ordering* is a different, legitimate use this plan must not confuse with that fixed leak).

`benchmarks/multiportmmi_8x8.py`, `benchmarks/multiportmmi_16x16.py`, `benchmarks/benes_8x8.py`, `benchmarks/benes_16x16.py` -- each defines its own `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` bundle (specific environment-variable values found empirically to make that benchmark route cleanly). Milestone 7 attempts to reduce or eliminate the need for these once the general algorithm is in place.

`routing_flow_config.py` -- the `SCRIPT_*` constant block (lines 22-41 as of this writing), which is `benes_16x16`'s own tuned CLI-default configuration applied blanket to every benchmark that does not override it. Milestone 7 checks whether these can become genuinely benchmark-neutral defaults once the general algorithm is in place.

`translation/route_rust_types.py` -- `RipupRerouteConfig` (lines 295-300 as of this writing), whose `enabled: bool = True` field Milestone 6 resolves (see Surprises & Discoveries and Milestone 6's own description above).

`/home/benjamin/Documents/Repositories/working/LiDAR` -- the sibling reference implementation studied in Milestone 1 (see Surprises & Discoveries above for the exact files and line numbers read). Read-only reference for this plan; nothing in this plan modifies that repository. Key files, if re-reading is needed: `src/picroute/routing/drgridroute.py` (the repair engine itself, `runNRR`/`ripupfailedNets`/`registar_net`/`updateHistoryMap`/`mark_conflict_nets`), `src/picroute/database/schematic.py` (`Nets` class, `__lt__` net-priority ordering, `failed_count`/`crossing_budget` fields), `src/picroute/queue/pqueue.py` (`AdvancedPriorityQueue`, a thin `heapq` wrapper -- the ordering logic lives in `Nets.__lt__`, not here).

### Related, already-completed work this plan builds on

`.agent/execplans/2026-08-25-unify-astar-kernel-and-clean-repair-baseline.md` (Milestones 1-5 complete, Milestone 6 -- final validation and retrospective -- still open) unified the two previously-duplicated A* search kernels into one, and its own Milestone 5 investigation is the direct origin of this plan: it found that most of the 17 `try_*` methods are individually reachable and behaviorally necessary in the narrow sense, which is what prompted the repository owner's sharper question this plan exists to answer. This plan does not require that sibling plan's Milestone 6 to be run first, but touches the same file (`src/py_router.rs`) -- avoid concurrent uncommitted edits to it.

### Toolchain and safety notes (identical to every other ExecPlan this session)

`RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu PYO3_PYTHON="$PWD/.venv/bin/python"` override for every `cargo` command; `.venv/bin/maturin develop --release` after any `src/*.rs` change before Python-level testing sees it. `benes_8x8`/`benes_16x16` need `--crossings true --crossing-mode lidar-pure` to be representative; do not assume bare CLI defaults are representative for any benchmark without checking that benchmark's own file first. This plan's whole later purpose is to make the `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` bundles unnecessary (Milestone 7) -- until that milestone actually lands, keep using them for `multiportmmi_8x8`/`multiportmmi_16x16` validation runs exactly as documented in each benchmark file, the same as every other plan this session has.

This plan changes real routing *behavior*, not just code structure -- unlike the kernel-unification plan, whose whole design point was byte-identical output. Do not expect or require byte-identical route costs before/after Milestones 3, 5, or 6; expect and require that every benchmark that passes today still passes (or newly passes where it previously failed for a reason this algorithm class is expected to fix), validated via each benchmark's own `build/verification/*_crossing_verification.json` per `.agent/WORKFLOW.md`'s Routing Verification Gate, not via byte-for-byte route comparison.

## Plan of Work

**Milestone 1** (complete) is read-only. Characterize the current repair dispatch chain's real structure and a sibling reference implementation's repair algorithm in enough detail to design from. See Surprises & Discoveries.

**Milestone 2** designs the target algorithm precisely for this codebase. Produce, in this plan's own text, the exact shape of: the net priority queue and what determines a net's position in it (topology depth from `node_depths`, tiebroken how); exactly which existing Rust structures need a new field or two to carry per-net state analogous to LiDAR's `failed_count` (this repository's `NativeRouteJob`/`RepairBatchState` types in `src/py_router.rs` are the likely home -- confirm by reading them fresh, do not assume); the exact trigger and effect of the history-cost systematic-application change (Milestone 4) and how it composes with today's `history_weight` config value (does the default need to change once application becomes systematic, since a value tuned for 3 narrow call sites may be wrong at every-commit frequency -- this needs the bounded prototype below to answer, not a guess); the exact shape of the global iterate-until-converged loop (Milestone 5) including its round limit and what "converged" means (every net routed, vs. a fixed-point where `global_failedNet` stops shrinking); and the exact distance/cost comparison Milestone 6's conflict-resolution heuristic uses, translated from LiDAR's Euclidean-distance-slack comparison into whatever equivalent this repository's own net/port geometry already exposes. Per `.agent/PLANS.md`'s guidance for designs with significant unknowns, include a small, bounded prototype: implement the systematic history-cost change and the global iterate loop against a narrow, synthetic multi-net conflict scenario (not a full benchmark) and confirm it actually converges to a legal routing where today's dispatch chain would need multiple special-cased strategies to do the same.

**Milestone 3** implements topological net ordering as an isolated first step, independently validated against the full benchmark ladder before Milestone 4 begins, so any regression can be attributed to this specific change.

**Milestone 4** makes history-cost application systematic, independently validated the same way.

**Milestone 5** implements the global iterate-until-converged loop and migrates `route_many_with_repair_and_commit` to it, keeping the old dispatch chain compiled and reachable (not deleted yet) so a direct A/B comparison stays possible while this milestone's own validation runs.

**Milestone 6** implements the distance/cost-based conflict-resolution heuristic, then deletes the old 17-method dispatch chain (minus the 4 confirmed-core-routing methods that stay) and every helper that existed only to serve it, following the same "delete, recompile, follow the compiler's own dead-code warnings" discipline the sibling kernel-unification plan used successfully in its own Milestones 4-5. Also resolves `RipupRerouteConfig.enabled` (`translation/route_rust_types.py:295-300`, see Surprises & Discoveries): once repair is integral to the negotiated-congestion loop rather than a separate fallback phase, decide and record whether this becomes a corrected doc comment explaining its one legitimate use (isolating repair out for tests like `test_benchmarks_route_with_astar_only`) or is replaced by an explicit test-only bypass instead of a general config toggle.

**Milestone 7** attempts to remove or shrink each benchmark's `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` bundle now that the general algorithm should need less benchmark-specific tuning, and separately checks whether `routing_flow_config.py`'s `SCRIPT_*` defaults (see Surprises & Discoveries: these are `benes_16x16`'s own tuned configuration, not neutral defaults) can be replaced with values that are not any one specific benchmark's tuning -- these are two related but distinct questions (per-benchmark override bundles vs. the CLI's own baseline defaults) and both need an honest answer, not just one. Record the honest outcome either way: if a benchmark still needs some tuning, or the `SCRIPT_*` defaults still need to favor one benchmark's needs, say exactly what and why, rather than declaring victory prematurely or hiding a remaining gap.

**Milestone 8** runs the complete validation ladder one final time, updates `.agent/REPOSITORY_STATE.md`, and writes the Outcomes & Retrospective section comparing the final state against this plan's own Purpose.

## Concrete Steps

Milestone 1's commands (already run; re-run before trusting exact line numbers, since other work may shift them):

    cd /home/benjamin/Documents/Repositories/working/TUMPhotonicRouter
    grep -n "    fn try_[a-z_]*(" src/py_router.rs
    cd /home/benjamin/Documents/Repositories/working/LiDAR
    sed -n '1,220p' src/picroute/routing/drgridroute.py
    sed -n '160,290p' src/picroute/database/schematic.py

Milestones 2 onward's exact commands are not prescribed further here, per `.agent/PLANS.md`'s guidance against over-specifying implementation details before the design milestone that justifies them has happened. The toolchain commands are the same ones every other ExecPlan this session has used, listed in Context and Orientation above.

## Validation and Acceptance

Milestone 1: a complete, evidence-backed characterization of both the current repair chain and the reference algorithm, with exact file/line citations for every claim (see Surprises & Discoveries -- every claim there cites a specific function and line).

Milestone 2: the design is written down with enough precision that Milestone 3 does not need to make further design decisions on its own; the bounded prototype demonstrates the systematic-history-cost-plus-iterate-loop combination actually converges on a synthetic multi-conflict scenario.

Milestones 3-6: each independently passes the full benchmark ladder (`cargo test --lib`, `pytest -q`, `benes_4x4`, `multiportmmi_8x8` both configurations, `benes_8x8`/`benes_16x16` with their documented stable-baseline flags, `multiportmmi_16x16` stable-baseline), verified via `build/verification/*.json` per the Routing Verification Gate, not merely exit codes. Milestone 6 additionally requires `RipupRerouteConfig.enabled` to have an explicit, recorded resolution (corrected doc comment, or removed in favor of an explicit test-only bypass) -- not left exactly as-is.

Milestone 7: for each benchmark, either its `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` bundle is removed and the benchmark still passes with plain defaults (the strong success signal), or it is kept with an honest, specific explanation of what remaining tuning is genuinely necessary and why; separately, the `SCRIPT_*` defaults in `routing_flow_config.py` either stop being `benes_16x16`-specific values or keep an honest, specific explanation of why they still need to be.

Milestone 8: `.agent/REPOSITORY_STATE.md` reflects this plan's completion; the Outcomes & Retrospective section states plainly whether the final state matches this plan's own Purpose.

## Idempotence and Recovery

Milestone 1 is read-only. Milestones 3-7 each produce their own commit (one-commit-per-milestone, matching this repository's established discipline); if a validation ladder run after any of them finds a real regression, `git revert` that specific milestone's commit rather than hand-patching forward, and do not proceed to the next milestone until the regression is understood. Keep the old dispatch chain compiled and reachable through Milestone 5's validation specifically so an A/B comparison against the new loop remains possible without reconstructing the old implementation from git history under time pressure.

## Artifacts and Notes

(to be filled in as Milestone 2's bounded prototype produces its own convergence evidence, and as later milestones' benchmark-ladder runs produce real before/after numbers)

## Interfaces and Dependencies

This plan reuses `src/astar.rs`'s existing `AStarConfig.history_weight`/`long_straight_congestion_weight` fields unchanged -- no new kernel-level interface is needed, only more systematic *use* of what already exists. `translation/route_rust_crossing_plan.py`'s already-computed topology metadata (`node_depths`) is the dependency Milestone 3 needs; if that metadata is not already threaded into `src/py_router.rs` at the point `route_many_with_repair_and_commit` builds its job list, Milestone 3 must add that plumbing and document the new interface precisely at that time, not assume it exists.
