# Full restructuring of route_many_with_repair_and_commit

## Purpose / Big Picture

This is the last major routing-pipeline component without an explicit structure or real test coverage. Every other stage (obstacle map/grid snapping, A* single-net search, geometry realization, path-length matching) has an explicit `Protocol`/`trait` interface as of the just-closed Future Architecture Initiative; `route_many_with_repair_and_commit` (`src/py_router.rs`, ~3,035 lines) was excluded from that initiative at every stage specifically because it needed its own restructuring pass first. Three real bugs were found in or adjacent to it this session alone (zero-event-acceptance, silent partial-restore data loss, the `n_67`/`n_70`/`n_71` victim-set-expansion gap, the last now fixed -- see `.agent/execplans/2026-08-20-ripup-repair-orchestration-restructuring.md`).

The repository owner's stated goal is a readable, tested codebase as the prerequisite for algorithm optimization work, not an end in itself. This function is the single remaining blocker to that goal, and -- per the structural survey below -- also contains at least one concrete, measurable optimization opportunity (repeated full-state cloning) that a restructuring would make safe to address, tying the readability goal directly to the eventual optimization goal.

## Progress

- [x] Milestone 0 (structural survey) done, see Surprises & Discoveries. Dispatched to a fork for the read-only investigation (report only, no design/code -- matching this session's established discipline for characterization work on this exact function, after an earlier fork correctly followed the same instruction on a related task). Produced a 13-phase control-flow breakdown and a full local-variable lifetime map across three natural scope tiers (whole-batch, per-net, per-repair-attempt).
- [x] Milestone 1 (`RepairBatchState` promotion, Codex) done (2026-08-20). All 9 whole-batch-scope locals (`final_routes`, `attempts`, `repair_trace`, `repair_count`, `failed_net_id`, `failed_error`, `retried_source_layers`, `timings`, `trace_last_route_start`) grouped into one struct, constructed once, every usage rewritten to `batch.<field>`. **Reviewed by Claude**: full diff read (464 lines), confirmed via targeted greps that every one of the 9 old bare-name declarations was removed (not left as dead/duplicate code) and that every remaining bare occurrence of the 9 names in the function is either the struct-literal's own field name or an unrelated PyO3 dict-key string literal -- no missed usages, no accidental over-matching. Codex reported finding no 10th whole-function-scope variable and confirmed the moved initializers don't depend on intervening code. Validation independently re-run, not just taken from Codex's report: `cargo test --lib` `389 passed, 0 failed` (unchanged count, as expected for zero-behavior-change), `tests/test_rust_batch_repair.py` unchanged. Given the user's chosen pacing (validate the full ladder after each milestone), also rebuilt the Python extension and ran the real benchmarks: `multiportmmi_8x8` bare defaults and `benes_4x4` both clean (`error_count: 0`; `multiportmmi_8x8` photonic verification's 2 warnings are the same pre-existing, unrelated `endpoint_correction_fallback_used` notices seen after today's `n_70` fix, confirming zero behavior change), full `pytest -q` byte-identical to the 11-failure baseline.
- [x] Milestone 2a (`RepairAttemptState` promotion, Codex) done (2026-08-20). 12 Phase-10 repair-setup locals grouped into a second struct. See Plan of Work's Milestone 2 entry for the full story, including the scoping split (2a mechanical cluster now, 2b closure-entangled cluster deferred) and validation detail. Same review rigor as Milestone 1: full diff read, zero missed usages confirmed, full benchmark ladder re-run, byte-identical to baseline.
- [x] Milestone 2b (`ProbeState` promotion, Codex) done (2026-08-20). The 10 probe-phase outputs were grouped into `ProbeState`, constructed once immediately after `candidate_blockers.sort_unstable_by_key(...)` using field-init shorthand, with the prior closure/intermediate computation left unchanged. Codex found the task's transcribed "two `candidate_blockers` mutation sites" was stale against current source -- the real number is 6 -- and correctly fixed all 6, verifying against real source rather than trusting the task file (this repository's own established Codex-reliability pattern, a repeated instance now). **Reviewed by Claude**: full diff read (230 lines), independently re-confirmed all 6 `enqueue_targeted_illegal_crossing_repair_set` call sites correctly use `&mut probe.candidate_blockers`, and a targeted grep across the whole function found zero missed bare-name usages of the 10 fields (every remaining match is either the struct-literal's own field-init shorthand, an unrelated format-string/dict-key text literal, or a same-named-but-different field on `NativeRouteAttempt`, not this function's local). Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline. This completes Milestone 2 (both 2a and 2b) -- all per-net-scope locals are now grouped into named structs, matching Milestone 1's whole-batch-scope grouping.
- [x] Milestone 3.2 (`prepare_repair_attempt`, Codex) implementation pass done (2026-08-21). Phase 10's history update plus `RepairAttemptState` construction now lives in a plain inherent `impl PyPhotonicRouter` helper next to `probe_net_for_repair`; the call site still performs the trivial `.max(1)` clamps before calling it, and `prefer_orthogonal_repair` plus `repair_set_index` remain immediately after the call unchanged. Pre-change baseline was `cargo test --lib` `389 passed, 0 failed` and `tests/test_rust_batch_repair.py` `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with identical counts (`389 passed, 0 failed`, `1 passed`). No bare, non-`probe.` references to the moved `ProbeState` fields appeared inside the extracted block, and the helper returns the `RepairAttemptState` struct literal directly with no local `mut` binding because no in-method mutation is needed after construction.
- [x] Milestone 3.3 (`try_guided_collision_crossing`, Codex) done (2026-08-21). Phase 7 extracted, new `GuidedCrossingOutcome` enum, `?`-operator propagation preserved through the new method boundary. Reviewed, independently validated, full benchmark ladder clean. See Plan of Work's Milestone 3 entry for full detail (design, implementation, and review notes).
- [x] Milestone 3.4 (`try_localized_crossing_keepout_retry`, Codex) done (2026-08-21). Phase 8 extracted, new `LocalizedKeepoutOutcome` enum (no `PyResult` needed -- no `?` in this phase). Reviewed, independently validated, full benchmark ladder clean. See Plan of Work's Milestone 3 entry for full detail.
- [x] Milestone 3.5 (`try_commit_clean_probe`, Codex) done (2026-08-21). Phase 9 extracted -- the most complex extraction so far: 3-way outcome (`Result<CommitIfCleanOutcome, ()>`), 7 exit points (4 breaks, 2 continues, 1 fall-through), 3 deliberate ownership-preserving clones (2 planned, 1 found by Codex during implementation). Reviewed, independently validated including both `multiportmmi_8x8` configs, full benchmark ladder clean. See Plan of Work's Milestone 3 entry for full detail.
- [x] Milestone 3.6 (`try_plain_normal_route`, Codex) done (2026-08-21). Phase 3 ("plain normal-route attempt") extracted -- the simplest extraction so far: no `?`, no `break`, no ownership/move issues, single tail-expression `match` with no explicit `return`. New `PlainRouteOutcome { Routed, NotResolved }` enum, plain return (no `Result` wrapper). **Reviewed by Claude**: full diff read (117 lines), confirmed the method's whole body is exactly the original `match route_result { ... }` with each arm's trailing control-flow keyword replaced by the corresponding enum variant as its tail expression, confirmed `try_plain_normal_route`/`PlainRouteOutcome` each defined exactly once, and confirmed the call site is immediately followed by Phase 4's `if self.lidar_pure_crossing_enabled() && self.use_collision_crossing_routing {` unchanged. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` bare defaults (`error_count: 0`, photonic `warning_count: 2` -- the same pre-existing `endpoint_correction_fallback_used` notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`).
- [x] Milestone 3.7 (`reset_repair_attempt_state_from_round_base`, Codex) done (2026-08-21). Sub-phase 11.3 ("reset-from-snapshot") extracted -- the first sub-extraction inside the ~1,300-line Phase 11 adaptive repair loop, per the structural re-survey fork's own recommendation. Pure side-effecting reset, no outcome enum, `repair: &RepairAttemptState` (read-only), `batch: &mut RepairBatchState`, verbatim 9-statement move with zero control-flow-shape change. **Reviewed by Claude**: full diff read (32 lines: 20 insertions, 12 deletions), confirmed the new method is a byte-for-byte move of the original 9 statements (same order, same expressions), confirmed the call site is a single line inside the `victim_first`/`reverse_victim_order` nesting immediately after the `reverse_victim_order && ripup_ids.len() <= 1` guard and immediately followed by the unchanged `let mut victim_reroute_ids = ripup_ids.clone();`, and confirmed `reset_repair_attempt_state_from_round_base` is defined exactly once. Given this is the first extraction to touch code physically inside Phase 11 (the pipeline's highest-risk, most complex remaining region), validation was run at full rigor, none skipped: independently re-ran `cargo test --lib` (`389 passed, 0 failed`, unchanged), rebuilt the Python extension, re-ran `tests/test_rust_batch_repair.py` (unchanged, 1 passed), and re-ran the full benchmark ladder: `multiportmmi_8x8` (`error_count: 0` on crossing verification, photonic `error_count: 0`/`warning_count: 2` -- the same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.8 (`ripup_repair_set_victims`, Codex) done (2026-08-21). Sub-phase 11.9 ("rip-up loop") extracted -- the second Phase-11 sub-extraction. Pure side-effecting loop over victim nets, no outcome enum. `lidar_pure_crossing_repair`'s `let` binding correctly kept at the call site (not moved into the method) and passed in as an explicit `bool` parameter, since it is also read later, further down in the same enclosing loop iteration, at code outside this extraction's scope -- mirroring the `guided_collision_crossing_enabled` accommodation from Milestone 3.3. **Reviewed by Claude**: full diff read (39 insertions, 15 deletions), confirmed `ripup_repair_set_victims` defined exactly once, confirmed the moved loop body is a verbatim, unreordered statement-for-statement move, confirmed the correct one of three `for old_id in &ripup_ids`-shaped loops in the file was extracted (the one immediately preceded by the `lidar_pure_crossing_repair` binding and immediately followed by `temporary_probe_reservation_added`), and confirmed via `grep -n lidar_pure_crossing_repair` that the flag's later, out-of-scope read (inside sub-phase 11.14.2, further down the same function) still refers to the same unmoved call-site binding, unchanged. Validation independently re-run at full rigor (this milestone also touches Phase 11): `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.9 (`label_and_trace_repair_mode_start`, Codex) done (2026-08-21). Sub-phases 11.4-11.5 ("labeling + repair_mode_start trace") extracted -- the third Phase-11 sub-extraction, and the first that returns a value rather than being purely side-effecting: `(route_order, victim_reroute_ids)`. Codex's implementation matched the design exactly, with the two anticipated mechanical adjustments (`ripup_ids.clone()` -> `ripup_ids.to_vec()`, dropped `&` on the trace call's slice argument) and no others needed. **Reviewed by Claude**: full diff read (46 insertions, 16 deletions), confirmed `label_and_trace_repair_mode_start` defined exactly once, confirmed the moved body matches the original 3-statement-plus-trace-call sequence with no reordering, and confirmed the call site's later code (all downstream reads of `route_order`/`victim_reroute_ids` throughout the rest of the mode-order iteration, confirmed via `grep -n victim_reroute_ids` -- 20 usages, every one after the call site is a read, never a mutation) needed no changes. **Found and fixed one real, if cosmetic, gap in my own task design**: the task file told Codex to keep the call site's destructured binding as `mut victim_reroute_ids` on the (wrong) assumption that later code might still mutate it; `cargo check` after the build surfaced an `unused_mut` warning proving the assumption false -- the reversal now happens entirely inside the new method, and every one of the 20 downstream usages is read-only. Removed the unnecessary `mut` directly (one-word change, `src/py_router.rs:10474`), re-verified `cargo check --lib` is now warning-free. Validation independently re-run at full rigor (this milestone also touches Phase 11), including after the `mut` fix: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.20 (`try_final_repair_fallback`, Codex) done (2026-08-21). Phase 12 extracted -- the last phase inside the per-net loop body, and the first extraction to take `repair`/`probe` by value rather than by reference. All 6 `repair.round_base_*` field moves and the `probe.probe_route` move preserved verbatim (no accidental `.clone()` insertions where the original genuinely moved). **Reviewed by Claude**: full diff read (444 lines), confirmed all 3 real translations (`if repair.repaired` early-return-guard, 2x `continue 'route_jobs` -> `return Ok(())`) plus the final `Err(())` tail expression are correct, confirmed the internal 12-iteration retry loop's own 2 unrelated `break;`s were left untouched, confirmed `try_final_repair_fallback` is defined exactly once, and confirmed the call site passes `repair`/`probe` bare (moved, not `&repair`/`&probe`), immediately followed by the unchanged `if trace_native_progress { ... }` block starting Phase 13. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free on the first pass -- strong confirmation the by-value ownership design was exactly right (a move-after-use error would have failed to compile, not just warned). Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.** Every phase inside the per-net loop body is now a named method or the deliberately-inline Phase 0/1 setup code.
- [x] **Plan complete (2026-08-21).** Phase 13 (result assembly, pure sequential `PyDict` bookkeeping after the loop) reviewed and deliberately left inline, same judgment as Phases 0/1 -- no genuine branching complexity to name. `route_many_with_repair_and_commit`: ~3,035 lines -> 663 lines (HEAD `a8dac59`). See Outcomes & Retrospective for the full summary.
- [x] Milestone 3.19 (`try_lidar_direct_crossing_subset`, Codex) done (2026-08-21). Phase 5 extracted -- the last remaining "lidar-specific" phase. `PyResult`-wrapped two-variant outcome. Codex folded the outer `lidar_pure_crossing_enabled() && use_collision_crossing_routing` gate into the method's own early-return guard too (not just the inner `is_empty()` check my task file called out explicitly) -- a clean, consistent extension of the same pattern, not a deviation; the call site now has no wrapping `if` at all, just the `match`. **Reviewed by Claude**: full diff read (274 lines), confirmed the one real translation (`continue 'route_jobs;` -> `return Ok(Routed)`) is correct and the only one, confirmed both bare `continue;` inside the loop and both `.map_err(PyRuntimeError::new_err)?` uses needed no change, confirmed `try_lidar_direct_crossing_subset`/`LidarDirectCrossingOutcome` each defined exactly once, and confirmed the call site is immediately followed by the unchanged `probe_net_for_repair` call (Phase 6). Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.18 (`try_pending_straight_victim_repair`, Codex) done (2026-08-21). Second of Phase 4's two sub-mechanisms extracted, completing Phase 4's decomposition. Labeled block wrapper (`'pending_straight_repair_attempt: { ... }`) removed entirely; plain two-variant outcome (`Routed`/`NotResolved`). **Reviewed by Claude**: full diff read (370 lines), confirmed all 3 exit-point translations (2x `break 'pending_straight_repair_attempt` -> `return NotResolved`, 1x `continue 'route_jobs` -> `return Routed`) present and correct, confirmed the labeled block wrapper survives only in the diff's removed lines (gone from the new method), confirmed `try_pending_straight_victim_repair`/`PendingStraightRepairOutcome` each defined exactly once, and confirmed the call site's closing braces correctly return control to Phase 4's outer gate nesting, unchanged. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.** Phase 4 is now fully decomposed.
- [x] Milestone 3.17 (`try_source_layer_center_out_repair`, Codex) done (2026-08-21). First of Phase 4's two sequential sub-mechanisms extracted -- the highest-stakes extraction this session, given part of its failure-recovery path (the `still_missing_net_ids` fresh-reroute-fallback loop) generalizes a fix for a real, previously-reproduced production bug. Three-way `Result`-wrapped outcome (`Routed`/`NotAttempted`, `Err(())` for the stop-the-batch case), the guard condition moved inside the method as an early-return. **Reviewed by Claude with extra care given the historical stakes**: full diff read (436 lines), independently confirmed via direct `grep` that the bug-fix-reference comment (`.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md`) survived the move byte-for-byte, unedited, unreformatted; confirmed both exit-point translations (`continue 'route_jobs` -> `return Ok(Routed)`, `break 'route_jobs` -> `return Err(())`) are correct and the only two; confirmed the inner `for &unrestored_net_id in &unrestored_net_ids` loop's own `continue;` needed no translation and stayed unchanged; confirmed `try_source_layer_center_out_repair`/`SourceLayerCenterOutOutcome` each defined exactly once; confirmed the call site is immediately followed by the unchanged `'pending_straight_repair_attempt` labeled block (the second sub-mechanism, a separate future milestone). Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). Neither benchmark is confirmed to exercise the 8+-shared-source-layer branch itself, but the byte-for-byte diff verification is the stronger evidence here regardless. **Verdict: PASS.**
- [x] Milestone 3.16 (`try_preemptive_crossing_ripup`, Codex) done (2026-08-21). Phase 2 ("preemptive crossing ripup") extracted -- the first top-level-phase extraction since Phase 3 (Milestone 3.6). Plain two-variant outcome (`Routed`/`NotResolved`, matching `PlainRouteOutcome`'s shape); the block's success-path `continue 'route_jobs;` becomes `return PreemptiveCrossingRipupOutcome::Routed;`. **Task-design gap found, same pattern as prior milestones**: my own task file undercounted the block's bare `continue;` statements as 2 when there are actually 3 (job-lookup failure, current-net route failure, victim-repair-fallback failure) -- Codex verified against real source and correctly left all 3 unchanged (they still target the still-present inner per-victim loop), rather than trusting the task file's count. **Reviewed by Claude**: full diff read (451 lines), independently re-confirmed all 3 `continue;` statements present and unchanged in the new method (not 2), confirmed the one real translation (`continue 'route_jobs;` -> `return PreemptiveCrossingRipupOutcome::Routed;`) is correct and the only one, confirmed `try_preemptive_crossing_ripup`/`PreemptiveCrossingRipupOutcome` each defined exactly once, and confirmed the call site is immediately followed by the unchanged `try_plain_normal_route` call. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.15 (`build_repair_mode_reservation`, Codex) done (2026-08-21). Sub-phase 11.6 extracted -- the first extraction needing only `&self`, confirmed correct (both helper calls it makes already take `&self`, nothing else in the block mutates state). Returns `(victim_first_probe_reservation, temporary_probe_reservation)` as a tuple, matching Milestone 2b's closure-entangled-construction precedent. Correctly used `.get(ripup_ids)` (no `.to_vec()`) for the two lookup-only `FxHashMap<Vec<u64>, _>` calls, exploiting `Vec<u64>: Borrow<[u64]>` -- the first extraction to need this narrower adjustment instead of every prior extraction's `.entry(ripup_ids.to_vec())` insertion pattern. **Reviewed by Claude**: full diff read (64 insertions, 42 deletions), confirmed the moved body is a verbatim, unreordered copy with exactly the two designed adjustments (dropped `&` on `ripup_ids` arguments, `.get(ripup_ids)` with no `.to_vec()`) and no others, confirmed `build_repair_mode_reservation` is defined exactly once and takes `&self` not `&mut self`, and confirmed the call site's tuple destructuring is immediately followed by the unchanged `if trace_native_repair && !temporary_probe_reservation.is_empty() { ... }` block. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free (the `&self`/`.get()` prediction compiled clean on the first pass, no drift found). Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.14 (`try_reroute_current_net_after_victims`, Codex) done (2026-08-21). Sub-phase 11.16 extracted -- the `victim_first`-gated counterpart to Milestone 3.11's block, completing the extraction of both large near-duplicate reroute blocks. New `RerouteCurrentNetAfterVictimsOutcome { Routed, Failed }` enum. Deliberately not merged with `try_reroute_current_net_before_victims`: this block's real differences (tail-bound `let route = match ...`, unconditional post-match success bookkeeping, and an inline reservation cleanup immediately before a bare `continue` on total failure rather than `mode.mode_failed = true` falling through) were preserved exactly. **Reviewed by Claude**: full diff read (531 lines), confirmed the inline reservation-cleanup `if mode.temporary_probe_reservation_added { ... }` stays immediately before `return RerouteCurrentNetAfterVictimsOutcome::Failed` unchanged (not altered to match 11.11's `mode.mode_failed = true` shape), confirmed the unconditional success bookkeeping after the `match` (attempts.push, final_routes.insert, `mode.repaired_route = Some(route)`) is preserved verbatim, confirmed `try_reroute_current_net_after_victims`/`RerouteCurrentNetAfterVictimsOutcome` each defined exactly once, and confirmed the call site's `continue` is bare/unlabeled (targeting the same `reverse_victim_order` loop the original inline `continue;` targeted), wrapped in the unchanged `if !mode.mode_failed && victim_first { ... }` guard, immediately followed by sub-phase 11.17's unchanged reservation-release code. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.13 (`try_crossing_aware_victim_reroute`, Codex) done (2026-08-21). Sub-phases 11.14.2-11.14.5 extracted together (lidar-seeded/lidar-direct attempts, guided-collision attempt, resolution fast-exit, lidar-blocked guard), grouped because they share `guided_victim_route`/`lidar_crossing_partners_available` state that never escapes the new method. Three-way `PyResult`-wrapped outcome (`Routed`/`Blocked`/`NotAttempted`); `probe`/`repair` correctly threaded as immutable borrows (confirmed by direct grep, not assumed, that neither is mutated in this sub-range -- the first extraction in Phase 11 that does *not* need `&mut ProbeState`/`&mut RepairAttemptState`). **Reviewed by Claude**: full diff read (793 lines -- the largest so far, consistent with ~368 moved lines plus 6 relocated `?`-propagating call chains), confirmed exactly 6 `.map_err(PyRuntimeError::new_err)?` uses moved (12 diff lines = 6 removed + 6 added, none missed, none added), confirmed both control-flow translations (`continue` -> `return Ok(Routed)`, `break` -> `return Ok(Blocked)`, with `mode.mode_failed = true` staying immediately before the latter) match exactly, confirmed `try_crossing_aware_victim_reroute`/`CrossingAwareVictimRerouteOutcome` each defined exactly once, and confirmed the call site's `?` propagation and its match are immediately followed by the unchanged, already-extracted `reroute_victim_with_plain_fallback` call. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.12 (`reroute_victim_with_plain_fallback`, Codex) done (2026-08-21). Sub-phases 11.14.6-11.14.7 extracted -- the first sub-extraction from inside the 615-line per-victim loop, deliberately narrower than 11.14 as a whole. New `VictimPlainRerouteOutcome { Routed, Failed }` enum; the block's one `break` becomes `return VictimPlainRerouteOutcome::Failed`. Correctly used the 4-argument `remember_victim_repair_error_keepout`/`"reroute_victims"` bucket (not Milestone 3.11's 2-argument current-net pair). Applied the required borrow-checker fix exactly as designed: the enclosing loop now iterates `for old_id in &victim_reroute_ids_snapshot` (a one-time clone of `mode.victim_reroute_ids` taken immediately before the loop) instead of borrowing `mode.victim_reroute_ids` directly, since the new method needs `mode: &mut RepairModeAttemptState`. **Reviewed by Claude**: full diff read (565 lines), confirmed the moved method body is a verbatim, unreordered copy of the original nested-match block (every trace-event argument, every `NativeRouteAttempt` field including the correct `"reroute_victims"` bucket, both `enqueue_targeted_illegal_crossing_repair_set` calls, the 4-arg `remember_victim_repair_error_keepout` call with `job.net_id` as its 4th argument, and the final `return VictimPlainRerouteOutcome::Failed` all present and unchanged except for the mechanical `ripup_ids`/`ripup_ids.to_vec()` slice adaptations), confirmed `reroute_victim_with_plain_fallback`/`VictimPlainRerouteOutcome` each defined exactly once, and confirmed the call site's loop header, match, and closing braces exactly match the design with sub-phases 11.14.1-11.14.5 completely untouched above it. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor -- this is the first extraction from inside the highest-risk remaining block (11.14) and the first requiring the loop-snapshot borrow-checker workaround: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**
- [x] Milestone 3.11 (`try_reroute_current_net_before_victims`, Codex) done (2026-08-21). Sub-phase 11.11 ("current-net-reroute (A)") extracted -- the largest single extraction this session (~238 lines), the first to use `RepairModeAttemptState` and the first needing `probe: &mut ProbeState`. See Plan of Work's Milestone 3 entry for full design/implementation/review detail. **Verdict: PASS.**
- [x] Milestone 3.10 (`RepairModeAttemptState` promotion, Codex) done (2026-08-21). Added the 9-field `RepairModeAttemptState` beside `RepairAttemptState`/`ProbeState`, constructed it once immediately after `let repaired_route: Option<RouteResult> = None;`, and rewrote only post-construction usages in the mode-order iteration to `mode.<field>`. Rewritten usage counts: `route_order` 16, `victim_reroute_ids` 17, `victim_first_probe_reservation` 3, `temporary_probe_reservation` 12, `temporary_probe_reservation_added` 6, `victim_reroute_only_reservation` 4, `victim_reroute_only_reservation_added` 2, `mode_failed` 12, `repaired_route` 8. The only remaining bare `mode_failed` in the post-construction range is the intentional trace string literal `"mode_failed"`. `cargo check` initially confirmed the known `victim_first_probe_reservation`/`temporary_probe_reservation` two-field borrow point compiled (disjoint field projections through one `&mut mode`), then reported 6 stale `unused_mut` warnings on now-moved locals (`temporary_probe_reservation`, `temporary_probe_reservation_added`, `victim_reroute_only_reservation`, `victim_reroute_only_reservation_added`, `mode_failed`, `repaired_route`); Codex removed exactly those `mut`s and re-ran warning-free. **Reviewed by Claude**: full diff read (589 lines -- the largest single diff this session, consistent with rewriting ~80 usage sites across the remaining ~1,150 lines of Phase 11). Independently confirmed via `awk`+`grep` scanning every line after the construction point that no bare (non-`mode.`) usage of any of the 9 field names survives, other than the struct literal's own field-init shorthand and one confirmed-unrelated match (`event.route_order` on `NativeRepairTraceEvent`, a different struct entirely -- the same class of false positive seen in Milestone 2b's review). Confirmed `lidar_pure_crossing_repair`, `round_idx`, `ripup_ids`, `active_repair_set_index`, `victim_first`, `reverse_victim_order` were correctly left untouched, bare, exactly as designed. Forced a clean recompile (`touch` + `cargo check --lib`) and independently confirmed zero warnings, not just trusting Codex's own report. Validation independently re-run at full rigor -- this is the single highest-risk milestone so far, since it touches both of Phase 11's large near-duplicate reroute blocks (11.11, 11.16) and the 615-line victim-reroute loop (11.14), not just a small self-contained sub-phase: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**

**Reviewed by Claude**: full diff read (254 lines). Confirmed `try_guided_collision_crossing` defined exactly once, `GuidedCrossingOutcome` defined exactly once, and `let guided_start = native_batch_timer(...)` appears twice in the whole file -- one inside the new method (this extraction's own instance), one deep in Phase 11's separate `lidar_seeded_collision_crossing` branch (pre-existing, unrelated code, not a duplicate). One accepted, harmless imperfection worth recording: `guided_collision_crossing_enabled`'s two `std::env::var_os` checks are now evaluated twice per net -- once inlined into the new method's own `if` condition, once still at the call site (kept there because Phase 11, much further down, reads the same value) -- a gap in this task's own design (it assumed the flag was Phase-7-only). Confirmed this does not change observable behavior: process environment variables are stable for the duration of a run in this codebase (nothing here calls `std::env::set_var` mid-routing), so both evaluations always agree; the cost is one redundant, cheap lookup per net, not a correctness issue. Not worth a follow-up milestone on its own. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline.

**Milestone 3.4 (Phase 8, "localized crossing keepout retry"), dispatched (2026-08-21)**: the same two-outcome shape as Phase 7, but confirmed to contain no `?` operator, so the new method returns a plain `LocalizedKeepoutOutcome { Routed, NotResolved }` (no `PyResult` wrapper), matching Milestone 3.2's simpler shape rather than 3.3's. A separate, phase-specific enum rather than reusing `GuidedCrossingOutcome`, matching this plan's established one-named-type-per-phase pattern even where the shape is structurally identical.

**Milestone 3.4 implementation pass done (2026-08-21, Codex)**: clean run, no complications -- confirmed the block genuinely has no `?` operator and no moved reference needed an extra parameter. **Reviewed by Claude**: full diff read (317 lines). Confirmed `try_localized_crossing_keepout_retry`/`LocalizedKeepoutOutcome` each defined exactly once, `let invalid_collision_crossing_candidate = ...` appears exactly once in the whole file (moved cleanly, no stray duplicate left at the old call site), and Phase 9's `if probe.candidate_blockers.is_empty() { ...` immediately follows the new call site unchanged. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline.

All of Phases 6-10 are now extracted -- the entire "probe through repair-setup" span of the function is a clean chain of named-method calls. Continuing autonomously into the remaining phases per the repository owner's direction (2026-08-21): proceed through Phase 11 (the large adaptive repair loop) and the rest without stopping for per-milestone confirmation, only for a genuine blocker or open design question. Given Phase 11's size (~1,300 lines) and internal complexity (flagged in Milestone 0's survey as containing two near-duplicated ~250-line blocks), dispatched a fresh read-only fork to re-survey its current exact structure before designing its extraction (line numbers have drifted after 5 completed extractions) -- see the new Surprises & Discoveries entry once that returns. While that runs, picked up a smaller, independent, already-well-understood phase in parallel.

**Milestone 3.6 (Phase 3, "plain normal-route attempt"), dispatched (2026-08-21)**: one of the simplest remaining phases -- no `?`, no `break`, no ownership/move issues (the committed route is a freshly-owned local, not borrowed from any shared struct), and runs before `ProbeState` even exists, so the new method needs only `batch`/`job`/scalar parameters. `PlainRouteOutcome { Routed, NotResolved }`, no `Result` wrapper. The whole extracted body is a single `match` that can be the method's own tail expression, no explicit `return` needed.

**Milestone 3.5 (Phase 9, "commit-if-clean fast path"), dispatched (2026-08-21)**: the most structurally complex extraction so far. Three-way outcome (routed / stop-the-batch / fall-through), combining Milestone 3.1's `Result<T, ()>` shape with 3.3/3.4's two-variant enum: `Result<CommitIfCleanOutcome, ()>`, `CommitIfCleanOutcome { Routed, NotResolved }`. 4 separate `break;` statements (not 1) all become `return Err(());`; 2 separate `continue;` statements become `return Ok(CommitIfCleanOutcome::Routed);`. One deliberate, allowed exception to pure code motion: today's code *moves* `probe.probe_route` into `batch.final_routes` at both success points, which won't compile once `probe` is a borrowed `&ProbeState` (matching every other extracted phase) -- both sites get an added `.clone()`, matching an already-established pattern elsewhere in this file, with zero change to observable behavior.

**Milestone 3.5 implementation pass done (2026-08-21, Codex)**: Phase 9 now lives in `try_commit_clean_probe`, placed immediately after `try_localized_crossing_keepout_retry` in the plain inherent `impl PyPhotonicRouter` block, with the requested `Result<CommitIfCleanOutcome, ()>` shape. The original call site is now only the match translating `Routed` to `continue 'route_jobs`, `NotResolved` to fall-through, and `Err(())` to `break 'route_jobs`, immediately followed by `let max_rounds = max_rounds.max(1);`. Validation passed: pre-change `cargo test --lib` was `389 passed, 0 failed` and `tests/test_rust_batch_repair.py` was `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with the same counts. Source drift from the dispatch note: the moved block also moved `probe.probe_crossing_events` into `add_crossing_events`; because the helper takes `probe: &ProbeState`, the implementation added `probe.probe_crossing_events.clone()` at that call to preserve the existing success-path semantics while compiling. The two requested `probe.probe_route.clone()` additions are exactly the two `batch.final_routes.insert(job.net_id, probe.probe_route.clone())` success sites.

**Reviewed by Claude**: full diff read (538 lines -- the largest so far, consistent with 3 clone-accommodation edits plus 7 exit-point translations across the largest single block extracted this session). Confirmed all 7 exit points translated exactly as designed (4x `return Err(())`, 2x `return Ok(CommitIfCleanOutcome::Routed)`, 1x fall-through default), and confirmed the 3rd clone (`probe.probe_crossing_events.clone()`, Codex's own find) is real and necessary -- `add_crossing_events` takes its argument by value, so this would not have compiled with a borrowed `probe` otherwise. Confirmed `try_commit_clean_probe`/`CommitIfCleanOutcome` each defined exactly once, `if probe.candidate_blockers.is_empty()` appears exactly once in the whole file (no stray duplicate at the old call site), and the new call site is immediately followed by Milestone 3.2's already-extracted `prepare_repair_attempt` call, unchanged. Given this milestone's higher risk (3 ownership-semantics changes, not just control-flow), validation was more thorough than prior milestones: independently re-ran `cargo test --lib` (`389 passed, 0 failed`, unchanged) and the batch-repair integration test (unchanged); full benchmark ladder re-run (Python extension rebuilt) covered **both** `multiportmmi_8x8` configs (bare defaults *and* stable-baseline, not just bare defaults as in prior milestones) plus `benes_4x4` -- all clean, `error_count: 0`, warnings identical to every prior run today (stable-baseline: 0 warnings; bare defaults: the same 2 pre-existing unrelated notices); full pytest suite byte-identical to the 11-failure baseline.

## Surprises & Discoveries

- **The function decomposes cleanly into 13 named phases**, each with a clear line range and single responsibility, confirmed by a full line-by-line read (not sampled): (0) job unpacking/indexing, (1) loop entry, (2) preemptive crossing ripup, (3) plain normal-route attempt, (4) lidar pending-straight-victim-hint (source-layer center-out + single-victim ripup), (5) lidar direct-crossing subset attempts, (6) probe + `candidate_blockers` construction, (7) guided collision-crossing, (8) localized crossing keepout retry, (9) commit-if-clean fast path, (10) repair setup (snapshot + `compute_repair_victim_sets`), (11) the adaptive repair-attempt loop (itself sub-phased: reset-from-snapshot, victim reroute, current-net reroute, result tracing), (12) total-failure fallback, (13) result assembly. This is a genuinely different entanglement shape than `route_nets_rust`'s pre-refactor closures -- it is already sequential, already delegates to ~40 named helper methods -- the restructuring problem here is "give this sequential procedure named phase boundaries and explicit state," not "untangle hidden closure captures."
- **Local variables split cleanly into three lifetime tiers, which is the actual design input for how to group them**: whole-batch scope (survives across every net in the loop: `final_routes`, `attempts`, `repair_trace`, `repair_count`, `failed_net_id`, `failed_error`, `retried_source_layers`, `timings`, plus read-only indices built once), per-net scope (fresh each net, but read/written across multiple of the 13 phases within that net: `probe_route`, `candidate_blockers`, the 7 `round_base_*` snapshot fields, `repair_victim_sets`, 3 learned-keepout maps, `repaired`), and per-repair-attempt scope (fresh each inner-loop iteration, no cross-attempt leakage observed: `victim_reroute_ids`, `route_order`, temporary reservations, `mode_failed`, `repaired_route`).
- **Two variables have the exact cross-phase mutability trap that correctly blocked the first Codex dispatch on `compute_repair_victim_sets` earlier today** -- `candidate_blockers` (built read-only-looking in Phase 6, but mutated again deep inside Phase 11 via `&mut candidate_blockers` passed to `enqueue_targeted_illegal_crossing_repair_set`) and `repair_victim_sets` itself (already known). Any phase-extraction task touching Phase 6 or Phase 11 must account for this explicitly, or repeat today's exact near-miss.
- **`retried_source_layers` has a genuinely easy-to-miss lifetime**: it's only touched within Phase 4a's logic, but must persist *across different nets'* loop iterations (a source layer retried while routing one net must not be retried again for a later net sharing that x-coordinate). It reads as phase-local; it is not. A naive "extract Phase 4 into a per-net method" pass would silently break this unless the variable is explicitly threaded from whole-batch scope.
- **A concrete, measurable optimization target, found as a side effect of the structural read, not gone looking for**: the 7 `round_base_*` fields are re-cloned in full at the top of *every single repair attempt* (Phase 11a), not once per net -- so a net needing 4 repair rounds x 2 victim orderings x 2 reverse-order combinations clones the entire obstacle map, committed-route maps, and crossing-event list up to 16 times. This session's own `multiportmmi_8x8` timing output (from today's `n_70` fix validation) shows `victims=123.6s` dominating a 142s total route phase -- consistent with this being a real, not theoretical, cost. Restructuring this function safely is the prerequisite for changing this to restore-by-diff or clone-on-write, which is itself a first concrete algorithm-optimization win once the structure exists to change it safely.
- **Phases 11b (victim reroute) and 11c (current-net reroute) are near-duplicated, symmetric code** -- same shape (attempt reroute, on error call the same two `enqueue_*` helpers, same keepout-learning), differing only in which net goes first. A strong candidate for collapsing into one method parameterized by role, rather than maintaining two ~250-line copies -- itself a readability win independent of the state-management restructuring.
- **No new top-level "session" wrapper is needed the way `_RouteNetsRustSession` was needed for `route_nets_rust`.** That precedent existed because `route_nets_rust` was a free function with ~120 closures needing somewhere to live; this function is already a method on `PyPhotonicRouter` (`&mut self`), which already carries the 20 session-state fields. The entanglement here is in this method's own ~20 call-scoped locals across the three lifetime tiers above, not in a missing `self`.
- **Milestone 2b found current-source drift from the point-in-time dispatch note, without changing the design.** The current function has six post-construction `enqueue_targeted_illegal_crossing_repair_set` call sites mutating `candidate_blockers` through `&mut probe.candidate_blockers`, not two. The six excluded helper locals stayed excluded, but `victim_selection_start` is still read once after the new `ProbeState` construction to charge `repair_probe_victim_selection_us`; it remains a plain local and is not part of `ProbeState`.
- **Milestone 3.2 found the Phase 10 extraction assumptions still matched current source.** The block starting at `let history_start = native_batch_timer(collect_native_timing);` already used `probe.probe_route`, `probe.crossing_repair_enabled`, `probe.probe_realized_crossing_violations`, and `probe.candidate_blockers`; no bare pre-Milestone-2b names were present. The constructed `RepairAttemptState` was not mutated inside the extracted block, so the new helper could return the struct literal directly and leave `mut` only at the call site where later phases mutate `repair`.
- **Milestone 3.3 found one downstream use outside the planned Phase 7 block.** The local `guided_collision_crossing_enabled` is referenced again later in Phase 11's victim-reroute guided branch (`src/py_router.rs`, around the `guided_victim_route` fallback). To keep that later branch compiling without widening the Phase 7 helper's requested signature, the call site still binds `guided_collision_crossing_enabled` before calling `try_guided_collision_crossing`; the helper also evaluates the same environment flag for its own top-level `if`.
- **Milestone 3.5 found a second ownership move inside Phase 9 that the dispatch text did not mention.** In addition to the two `probe.probe_route` moves into `batch.final_routes`, the crossing-compliant success path moved `probe.probe_crossing_events` into `self.add_crossing_events(...)`. Once Phase 9 is extracted with `probe: &ProbeState`, that vector cannot be moved out of the shared reference, so the extracted method clones the crossing events at that one call site.
- **Phase 11 structural re-survey (fork, read-only, 2026-08-21) -- current exact boundary is `src/py_router.rs:10372-11697`, 1,326 lines** (line numbers drifted after 5 extractions; the corrected outer boundary is `while repair_set_index < repair.repair_victim_sets.len() { ... }`, immediately preceded by `prefer_orthogonal_repair`/`repair_set_index` init at `10368-10371` which stay at the call site, immediately followed by Phase 12's `if !repair.repaired {` at `11699`). Decomposes into 20 numbered sub-phases (11.0 setup through 11.20 cascading break-out); full breakdown, size table, and exit-point inventory preserved in this fork's own transcript (agent id `ab751c89f2e812c17`), condensed here to what changes the extraction design:
  - **`probe` must become `&mut ProbeState` inside this loop**, not the `&ProbeState` every extraction since Milestone 3.3 has used -- `enqueue_targeted_illegal_crossing_repair_set` is called with `&mut probe.candidate_blockers` at 5 separate sites inside Phase 11 (not just the 6 already found in Phase 6/Milestone 2b). Any Phase-11 task file must state this explicitly so Codex doesn't default to copying the immutable-reference pattern established everywhere else this session.
  - **The two "near-duplicated ~250-line blocks" flagged by Milestone 0's original survey (current-net-reroute at 11.11/`10519-10756` and 11.16/`11418-11649`, plus the victim-reroute plain-fallback at 11.14.6/`11161-11388`) are structurally similar but not safely collapsible as-is**: they call different "remember error" helpers with different arities (`remember_local_repair_error_keepout`, 2 args, for current-net vs. `remember_victim_repair_error_keepout`, 4 args, for victim reroute -- the latter needs the *current job's* `net_id`, not the victim's), use different `bucket_name` trace labels, and read from `job` vs. `victim_job` (via `job_by_id`). 11.11 and 11.16 additionally differ in their own total-failure control flow (11.11 falls through via `mode_failed = true`; 11.16 does `continue`, skipping 11.17's reservation cleanup -- confirmed harmless because 11.3's next-iteration reset-from-snapshot fully overwrites `self.obstacle_map` regardless, but this must be preserved as an explicit "known safe due to snapshot restore" note if this code is ever touched, not silently "fixed" to match 11.11's shape). Any future collapse is a real design task on its own, not a free byproduct of extraction.
  - **Unlabeled multi-level `continue`/`break` target at least 3 different loop depths** depending on position inside the 1,326 lines (innermost `for old_id in &victim_reroute_ids`, the `reverse_victim_order`/`victim_first` mode-order loops, and the outer `while`). A naive single-method extraction of the whole loop would need a new outcome type distinct from every enum used so far; extracting sub-phase-by-sub-phase avoids this.
  - **Recommended first sub-extraction: 11.3 (reset-from-snapshot, `10381-10392`, ~12 lines)** -- idempotent, no ordering dependency on the previous mode-order iteration, takes `repair: &RepairAttemptState` read-only and mutates only `self`/`batch`, needs no new struct fields and no `&mut ProbeState`. This is the safest possible entry point into Phase 11 and is picked as the next milestone (3.7).
  - Confirms `job_by_id`'s exact type is not yet nailed down (needed once 11.14's victim-reroute lookup is extracted) -- resolve by direct read at that milestone's design time, not assumed from this survey.

## Decision Log

- Decision: proceed through the milestones autonomously (2026-08-20, via `AskUserQuestion`), validating the full benchmark ladder after each step, stopping to report only on a break or a genuine design fork -- rather than checking in after every milestone or stopping after Milestone 1 alone.
  Rationale: repository owner's explicit choice, matching the pace of today's smaller ripup/repair work.
- Decision: in Milestone 3.3, keep the requested `try_guided_collision_crossing` parameter list unchanged and preserve the call-site `guided_collision_crossing_enabled` binding for Phase 11's later use.
  Rationale: `cargo check` showed the dispatch's extracted block boundary missed a downstream reference to that local; keeping the call-site binding is the smallest compile-preserving adjustment while leaving the extracted helper's return type and routing outcome shape as designed.
  Date/Author: 2026-08-21 / Codex
- Decision: in Milestone 3.5, keep the requested `try_commit_clean_probe` signature with `probe: &ProbeState` and clone `probe.probe_crossing_events` when adding the committed crossing events.
  Rationale: the current source moved that vector in the inline success path, but the extracted helper only borrows `ProbeState`; cloning is the smallest behavior-preserving compile fix and avoids widening the method boundary or taking `probe` by value.
  Date/Author: 2026-08-21 / Codex

## Outcomes & Retrospective

**Structural restructuring complete (2026-08-21).** `route_many_with_repair_and_commit` went from ~3,035 lines to 663 lines (HEAD `a8dac59`). Every phase from the original 13-phase survey with genuine branching complexity, borrow-checker subtlety, or historical-bug lineage is now a named method with an explicit outcome type: 4 new state-carrier structs (`RepairBatchState`, `RepairAttemptState`, `ProbeState`, `RepairModeAttemptState`, one per natural scope tier -- whole-batch, per-net, per-repair-attempt, per-mode-order-iteration) and 12 new phase-outcome enums, backing ~30 new named methods. Phases 0, 1, and 13 (job unpacking/indexing, the skip-if-already-routed loop entry, and PyDict result assembly) were deliberately left as inline code at every stage -- each is straightforward, sequential, self-explanatory via variable names, and extracting them would add indirection without a real readability gain, consistent with this project's stated preference against premature abstraction. This judgment call was made explicitly at each point it came up (see the Progress entries around Milestones 3.15-3.16), not by default.

Every one of the 20 implementation milestones (3.6-3.20, plus the earlier state-promotion Milestones 1/2a/2b and the Phase-6-10 extractions 3.1-3.5) followed the same discipline: design by direct source read (never trusting stale line-number transcriptions), dispatch to Codex via `.agent/scripts/codex_task.sh`, independently review the full diff (not a summary), independently re-run the full validation ladder (`cargo test --lib`, the batch-repair integration test, `multiportmmi_8x8`, `benes_4x4`, full `pytest -q`), and only then commit. Every single validation pass came back byte-identical to the pre-restructuring baseline (`11 failed, 335 passed, 1 skipped`; both benchmarks' verification JSONs unchanged) -- zero behavior change across the entire restructuring, confirmed independently at every step rather than assumed from Codex's own reports.

Real traps this restructuring's design work found and correctly handled, not just anticipated in the abstract:
- The `&ProbeState`/`&mut ProbeState` and `&RepairAttemptState`/`&mut RepairAttemptState` distinction had to be re-derived per extraction by direct grep, never assumed from a prior extraction's signature -- roughly half needed mutable access, half didn't, and guessing wrong would have meant a wasted Codex dispatch.
- A genuine new borrow-checker class of trap (Milestone 3.12): a `for x in &struct.field` loop's iterator holds a live borrow of that field for the whole loop, which conflicts with any `&mut struct` needed later in the same loop body -- fixed by snapshotting the field into a loop-scoped clone before the loop, not by any of the more invasive alternatives (index-based iteration, `RefCell`, etc.).
- A genuine ownership-move requirement (Milestone 3.20), the mirror image of every other extraction: the final phase's `repair`/`probe` parameters had to be taken *by value*, not by reference, because both are demonstrably consumed (fields moved out, not cloned) as their last use in the function -- confirmed by direct grep before writing the task, and confirmed correct by a clean first-pass compile with zero warnings.
- Two "near-duplicate" blocks (Milestones 3.11/3.14) that looked collapsible were extracted as two separate methods once their real behavioral differences were found (different helper arity, different bucket names, different total-failure control-flow shape) -- deliberately not merged, preserving each one's exact original behavior rather than "cleaning up" the duplication into a wrong abstraction.
- My own task files undercounted exit points twice (Milestone 3.9's `mut` assumption, Milestone 3.16's `continue` count) -- both caught by Codex verifying against live source rather than trusting the task file, and both caught again independently in review before commit.

Not done, and deliberately not treated as part of this plan's scope: the concrete performance-optimization opportunity flagged in Surprises & Discoveries (the 7 `round_base_*` fields being re-cloned in full on every repair attempt, up to 16x per net) is now safe to address because the structure exists to change it -- but doing so is future work, per this plan's own stated Purpose ("readability/testing first... a first concrete algorithm-optimization win once the structure exists to change it safely"), not something to fold in here. Collapsing the two near-duplicate reroute blocks once their real differences are reconciled by design (not just extracted separately) is a similar, explicitly-deferred future candidate.

## Context and Orientation

`route_many_with_repair_and_commit` (`src/py_router.rs:8723-11758`) is a method on `PyPhotonicRouter` handling batch net routing with rip-up/repair: for each net in a job batch, try to route it cleanly; if blocked, rip up one or more already-routed "victim" nets, reroute the blocked net, then try to reroute the victims elsewhere, with several fallback strategies and a bounded adaptive expansion of which nets to rip up together (`compute_repair_victim_sets`, `enqueue_targeted_illegal_crossing_repair_set`, `enqueue_learned_keepout_repair_retry` -- all already-extracted, already-tested free functions from earlier work this session and 2026-07-02). See the 13-phase breakdown in Surprises & Discoveries for the full control flow, and `.agent/execplans/2026-08-20-ripup-repair-orchestration-restructuring.md` for the session-state field inventory, test-coverage findings, and the three prior bugs' exact locations -- all still accurate, not repeated here.

## Plan of Work

### Milestone 0: structural survey (done, see Surprises & Discoveries)

### Milestone 1 (proposed): introduce `RepairBatchState`, pure state-promotion, zero behavior change

Mirrors the single highest-leverage decision from the `route_nets_rust` precedent (`.agent/REPOSITORY_STATE.md`'s own recorded lesson: "the mid-plan restructuring after Milestone 1 -- inserting a dedicated shared-state-promotion milestone before any closure conversion -- was the single highest-leverage decision in this plan"). Introduce a plain struct, `RepairBatchState`, holding exactly the whole-batch-scope locals identified in the survey (`final_routes`, `attempts`, `repair_trace`, `repair_count`, `failed_net_id`, `failed_error`, `retried_source_layers`, `timings`). Construct it once at the top of the function, thread `&mut RepairBatchState` through the existing inline code with zero logic changes -- this milestone does not extract any phase into a separate method yet, it only proves the state grouping is correct by making the existing code compile and behave identically with the new struct in place. Validate against the full benchmark ladder (same rigor as today's `n_70` fix: `cargo test --lib`, the batch-repair integration test, `benes_4x4`, `multiportmmi_8x8` both bare and stable-baseline) before proceeding.

### Milestone 2 (in progress): introduce a per-net scratch struct, same discipline -- split into two, not one

Same pattern one tier down: group the per-net-scope locals into a struct, constructed fresh each loop iteration, threaded through unchanged. **Scoping adjustment found while designing this milestone precisely** (not part of the original survey): the per-net cluster is not uniform the way Milestone 1's whole-batch cluster was. `probe_route`/`candidate_blockers` and several `probe_*` fields are constructed in the probe phase (Phase 6) alongside a closure, `add_candidate_blocker`, that captures some of these locals by mutable reference to build `candidate_blocker_priority` -- a genuinely more entangled case than Milestone 1's plain defaults. The other 12 locals (`repaired`, the 7 `round_base_*` fields, `repair_victim_sets`, the 3 learned-keepout maps) are declared together in the repair-setup phase (Phase 10) with no closures involved, exactly as mechanical as Milestone 1 was, just with real initializer expressions (mostly `.clone()` calls) instead of plain defaults.

Splitting Milestone 2 into two sub-passes rather than attempting both clusters at once: **Milestone 2a** (this dispatch) covers the 12-field Phase 10 cluster only, as `RepairAttemptState`. **Milestone 2b** (not yet started) will cover the smaller, closure-entangled Phase 6 cluster once 2a validates cleanly -- deferred deliberately, not because it's out of scope, but because mixing the two in one pass would conflate a purely mechanical change with a genuinely trickier one in the same review.

**Milestone 2a done (2026-08-20, Codex)**: `RepairAttemptState` added (`src/py_router.rs:1019`, next to `RepairBatchState`), all 12 fields, constructed once with every initializer expression preserved verbatim, every usage rewritten to `repair.<field>`. The one flagged subtlety (the total-failure fallback moves 6 `round_base_*` fields out of the struct into `self`, not clones) compiled clean with no error -- confirmed by direct check that `repair.round_base_routes` at the fallback's last line is genuinely the last usage of the struct in that net's iteration, so the partial move is safe. **Reviewed by Claude**: full diff read (176 lines), targeted greps confirmed zero missed bare-name usages of the 12 fields anywhere in the function (only the struct-literal's own field names remain unprefixed). Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Real benchmark ladder also re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, `multiportmmi_8x8`'s 2 warnings identical to every prior run today (same pre-existing, unrelated notices); full pytest suite byte-identical to the 11-failure baseline.

**Milestone 2b design (2026-08-20)**: read the probe-phase computation directly rather than delegating the design sight-unseen, given the closure entanglement flagged in Milestone 2's scoping note. Finding: the closure (`add_candidate_blocker`) and its captured local (`candidate_blocker_priority`) are both fully consumed *before* any of the values that need to escape this phase reach their final form -- they never need to coexist with a struct field. This means Milestone 2b does not need to touch the ~140-line intermediate computation at all: construct a new struct, `ProbeState`, once at the very end of that computation (right after `candidate_blockers` is sorted), using field-init shorthand on the already-computed locals, and rewrite only the *later* usages (from that point through the rest of the function) to `probe.<field>`. 10 fields (one more than the original survey's estimate): `probe_route`, `crossing_repair_enabled`, `allowed_crossing_partners`, `probe_crossing_events`, `strict_expected_crossing_probe` (a field the original survey didn't call out explicitly, confirmed by direct trace to be read as far downstream as the commit-if-clean phase), `probe_crossing_compliant`, `probe_realized_crossing_violations`, `probe_grid_crossing_violations`, `probe_repair_keepout_keys`, `candidate_blockers` (carries the same later-mutated-via-`&mut` trap as Milestones 1/2a's sibling variables, already flagged explicitly for this dispatch).

### Milestone 3.10 (done): promote Phase 11's mode-iteration-scoped locals into `RepairModeAttemptState`

The structural re-survey's own note flagged this cluster ("Mode-order-iteration-scoped 'attempt state'... candidate for its own struct, analogous to `ProbeState`/`RepairAttemptState`") as the natural next step before sub-extracting 11.6 or any of 11.10-11.20 -- those blocks are large (11.11/11.16 ~230 lines each, 11.14 ~615 lines) and each reads/writes several of these same 9 locals, so extracting them as methods first would mean threading 8-9 separate `&mut` parameters through each one. Following the exact precedent Milestone 1 (`RepairBatchState`) and Milestone 2a (`RepairAttemptState`) set: pure state-promotion, zero behavior change, no new method extracted yet. Independently confirmed via whole-word `grep -n` that all 9 candidate fields (`route_order`, `victim_reroute_ids`, `victim_first_probe_reservation`, `temporary_probe_reservation` + `_added`, `victim_reroute_only_reservation` + `_added`, `mode_failed`, `repaired_route`) are confined to one mode-order iteration inside `route_many_with_repair_and_commit`, with no name collisions elsewhere in the file. Deliberately excluded: `lidar_pure_crossing_repair` (only 2 usages after declaration, already correctly handled as an explicit `bool` parameter since Milestone 3.8, matching the `guided_collision_crossing_enabled` precedent -- adding a 10th field would be unnecessary). Mirrors Milestone 2b's closure-entangled construction handling: the construction sequence itself (sub-phases 11.6-11.8, plus 11.10's own reservation-commit and remaining inits) stays untouched inline code; the new struct is constructed once, via field-init shorthand, right after the last of the 9 values reaches its final initial form (immediately after `let mut repaired_route: Option<RouteResult> = None;`), and only usages *after* that point are rewritten to `mode.<field>`.

**Milestone 3.11 (Phase 11, sub-phase 11.11 "current-net-reroute (A)"), done (2026-08-21, Codex)**: extracted `try_reroute_current_net_before_victims` immediately after `label_and_trace_repair_mode_start` in the same plain inherent `impl PyPhotonicRouter` block. The call-site `if !victim_first { ... }` guard remains inline and now wraps only the method call; the following sub-phase 11.12-11.13 guard (`if !mode.mode_failed && !victim_first && mode.temporary_probe_reservation_added && ...`) is unchanged and still immediately follows. The helper is pure side effect (mutates `mode.repaired_route`/`mode.mode_failed`, plus the same batch/repair/probe/obstacle-map state the inline block already mutated), with no new outcome enum. `probe` is correctly threaded as `&mut ProbeState`; both `enqueue_targeted_illegal_crossing_repair_set` call sites in the helper pass `&mut probe.candidate_blockers`. Confirmed `active_repair_set_index` and `repair_set_index` are genuinely distinct at the call site (`active_repair_set_index` is captured before `repair_set_index += 1`, while `repair_set_index` is the already-incremented next-queue position used by learned-keepout retry enqueueing), so both parameters stay. `repair` and `probe` were already `let mut` bindings, no upstream mutability changes were needed. The only mechanical adaptations from moving `ripup_ids` behind a slice parameter were `ripup_ids` where callees expect `&[u64]` and `ripup_ids.to_vec()` where the old inline block stored/cloned a `Vec<u64>`. **Reviewed by Claude**: full diff read (503 lines -- the largest single-milestone diff this session, matching the ~238-line block's size plus the new method's own boilerplate), confirmed line-by-line that the moved method body is a verbatim, unreordered copy of the original nested-match block (every trace-event argument, every `NativeRouteAttempt` field, both `enqueue_targeted_illegal_crossing_repair_set` calls, the learned-keepout bookkeeping, and the final `mode.mode_failed = true` all present and unchanged except for the mechanical `&ripup_ids` -> `ripup_ids`/`ripup_ids.clone()` -> `ripup_ids.to_vec()` slice adaptations), and confirmed `try_reroute_current_net_before_victims` is defined exactly once. Forced a clean recompile and independently confirmed `cargo check --lib` is warning-free. Validation independently re-run at full rigor -- the largest extraction so far and the first with `probe: &mut ProbeState`: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged, full benchmark ladder re-run (Python extension rebuilt) -- `multiportmmi_8x8` (`error_count: 0`, photonic `warning_count: 2` -- same pre-existing notices) and `benes_4x4` (`error_count: 0`, `warning_count: 0` on both) both clean; full pytest suite byte-identical to the 11-failure baseline (`11 failed, 335 passed, 1 skipped`). **Verdict: PASS.**

**Milestone 3.12 (Phase 11, sub-phases 11.14.6-11.14.7 "victim plain reroute with fallback + bookkeeping"), dispatched (2026-08-21)**: the first sub-extraction from inside the 615-line per-victim loop (sub-phase 11.14 as a whole), rather than the whole loop at once -- deliberately scoped narrower than Milestone 3.11's block, per the repository owner's own guidance to decompose 11.14 into its established sub-pieces rather than one big-bang extraction. New two-variant outcome (`VictimPlainRerouteOutcome { Routed, Failed }`), the one `break` inside the block's innermost failure arm becomes `return VictimPlainRerouteOutcome::Failed`. Uses the 4-argument `remember_victim_repair_error_keepout`/`"reroute_victims"` bucket (not the 2-argument current-net pair Milestone 3.11 used) -- the exact divergence the structural re-survey flagged between the two shapes. **A new borrow-checker requirement, found by direct read, not by trial-and-error**: the enclosing `for old_id in &mode.victim_reroute_ids` loop borrows `mode.victim_reroute_ids` for the loop's whole duration; once the new method needs `mode: &mut RepairModeAttemptState`, calling it from inside that loop conflicts with the loop's own borrow. Fix: clone `mode.victim_reroute_ids` into a loop-scoped snapshot immediately before the loop and iterate over that instead -- confirmed via `awk`+`grep` that `mode.victim_reroute_ids` is never mutated anywhere in this loop (only ever read via `&mode.victim_reroute_ids` in trace calls), so the snapshot and the live field always agree, zero behavior change.

**Milestone 3.13 (Phase 11, sub-phases 11.14.2-11.14.5 "crossing-aware victim reroute attempt"), dispatched (2026-08-21)**: the second sub-extraction from inside the 615-line per-victim loop, grouping four sub-phases together (lidar-seeded/lidar-direct attempts, guided-collision attempt, resolution fast-exit, lidar-blocked guard) because they share two locals (`guided_victim_route`, `lidar_crossing_partners_available`) that would otherwise need an awkward return-tuple to split apart -- grouped, both stay fully internal to the new method, mirroring Milestone 2b's precedent. Three-way `PyResult`-wrapped outcome (`Routed`/`Blocked`/`NotAttempted`), 6 confirmed `?` uses (2 per each of 3 route-attempt calls). Confirmed by direct grep, not assumed, that neither `probe` nor `repair` is mutated anywhere in this specific sub-range -- both stay immutable borrows here, unlike Milestones 3.11/3.12's `&mut` requirement. Reuses Milestone 3.12's loop-snapshot fix (the per-victim loop already iterates a decoupled clone), so no new borrow-checker workaround is needed for `&mut mode` itself. Falls through to the already-extracted `reroute_victim_with_plain_fallback` call on `NotAttempted`, left completely unchanged at the call site.

**Milestone 3.14 (Phase 11, sub-phase 11.16 "current-net-reroute (B)"), dispatched (2026-08-21)**: completes the whole 615-line sub-phase-11.14 victim loop's decomposition begun in Milestones 3.12-3.13 by extracting its counterpart, 11.16 -- the block gated `victim_first` (runs after the victim loop, not before it). Deliberately not merged with Milestone 3.11's `try_reroute_current_net_before_victims`, per the structural re-survey's own caution: this block's real differences (tail-bound `let route = match ...`, unconditional post-match bookkeeping, and -- critically -- an inline reservation cleanup immediately before a bare `continue` on total failure, rather than `mode.mode_failed = true` falling through) are preserved exactly, not "fixed" to match 11.11's shape. New two-variant `RerouteCurrentNetAfterVictimsOutcome` outcome; the call site's `continue` must stay unlabeled, targeting the same `reverse_victim_order` loop the original inline `continue;` targeted.

**Milestone 3.15 (Phase 11, sub-phase 11.6 "temporary_probe_reservation construction"), dispatched (2026-08-21)**: with both large near-duplicate reroute blocks and the whole victim loop now extracted, moving to the remaining small Phase-11 fragments. Confirmed via direct read that both helper calls this block makes (`crossing_physical_violation_repair_keepout_keys`, `crossing_grid_violation_repair_keepout_keys`) already take `&self` and nothing else in the block mutates state -- the first extraction this session needing only `&self`, not `&mut self`. Returns `(victim_first_probe_reservation, temporary_probe_reservation)` as a tuple, mirroring Milestone 2b's closure-entangled-construction precedent (the two values are computed together, `victim_first_probe_reservation` mutated inside `temporary_probe_reservation`'s own construction). One new Rust nuance flagged explicitly for this dispatch: the two `.get(&ripup_ids)` lookups against `FxHashMap<Vec<u64>, _>` become `.get(ripup_ids)` once `ripup_ids: &[u64]` -- no `.to_vec()` needed, since a lookup (unlike every prior extraction's `.entry(...)` insertion pattern) only needs `Vec<u64>: Borrow<[u64]>`, not ownership.

**Phase 11 sub-extraction is now substantially complete (2026-08-21)**: with Milestone 3.15, every genuinely complex or high-risk Phase-11 sub-phase (11.3, 11.4-11.5, 11.6, 11.9, 11.11, the whole 615-line 11.14, 11.16) is now a named method. The remaining Phase-11 fragments (11.7-11.8, 11.10's leftover init lines, 11.12-11.13, 11.15, 11.17-11.20) are left as inline code -- each is small (a handful of lines), sequential, and already self-explanatory via the `mode.*`/`repair.*` field names introduced by Milestones 2a/2b/3.10; extracting them further would add indirection without a real readability gain, consistent with this project's stated preference against premature abstraction. Presented this checkpoint to the repository owner (via `AskUserQuestion`): continue restructuring the remaining top-level phases (0, 1, 2, 4, 5, 12, 13) vs. pivot to the two parked Current Findings items vs. stop. **Decision: continue restructuring** (2026-08-21).

**Milestone 3.16 (Phase 2, "preemptive crossing ripup"), dispatched (2026-08-21)**: the first top-level-phase extraction since Milestone 3.6 (Phase 3). Phases 0 (job unpacking/indexing) and 1 (loop entry / skip-if-already-routed) deliberately left inline -- straightforward setup/trace code, already clear via self-descriptive variable names, matching the same judgment applied to Phase 11's small leftover fragments. Phase 2 itself (~189 lines: a nested per-victim-candidate loop, each iteration running a full two-tier route+fallback flow for *both* the current net and the victim, with a base-state snapshot/restore around the whole attempt) gets a plain two-variant outcome matching `PlainRouteOutcome`'s exact shape (`Routed`/`NotResolved`, no `Result` wrapper -- this phase never stops the whole batch). Only 1 of the block's 3 `continue`-family statements needs translation (the `continue 'route_jobs;` on full success, which becomes `return PreemptiveCrossingRipupOutcome::Routed;`); the other 2 bare `continue;`s target the still-present inner per-victim loop and need no change.

**Milestone 3.17 (Phase 4, first sub-mechanism "source-layer center-out repair"), dispatched (2026-08-21)**: Phase 4 ("lidar pending-straight-victim-hint") turns out to combine two sequential, independent sub-mechanisms under one shared gate -- source-layer center-out repair, then (unconditionally after it) pending-straight-victim repair -- rather than being one cohesive block, so it is being split into two milestones like Phase 11's sub-phases were. This is the highest-stakes extraction yet: part of the first sub-mechanism's failure-recovery path (a fresh-reroute-fallback loop for nets a best-effort restore couldn't recommit) has an inline comment explaining it generalizes a fix for a real, previously-reproduced production bug (`.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md`) -- the task explicitly instructs moving this code with maximum care, no reordering, no simplification. Three-way `Result`-wrapped outcome (`Routed`/`NotAttempted`, `Err(())` for the one `break 'route_jobs` stop-the-batch exit), mirroring Milestone 3.5's `try_commit_clean_probe` shape. The block's own guard condition moves inside the method as an early-return, matching Milestone 3.16's precedent.

**Milestone 3.18 (Phase 4, second sub-mechanism "pending-straight victim repair"), dispatched (2026-08-21)**: completes Phase 4's decomposition begun in Milestone 3.17. Much simpler than its sibling -- a labeled block (`'pending_straight_repair_attempt: { ... }`), no nested fallback loop, no historical-bug comment, a plain two-net rip-up+reroute+restore-on-failure pattern. Plain two-variant outcome (`Routed`/`NotResolved`, no `Result` wrapper -- never stops the batch). The labeled block wrapper itself disappears; both of its `break`s become `return NotResolved`, its one `continue 'route_jobs` becomes `return Routed`.

**Milestone 3.19 (Phase 5, "lidar direct-crossing subset attempts"), dispatched (2026-08-21)**: simpler than Phase 4 -- single gate, single loop, no nested labeled blocks, no historical-bug lineage. `PyResult`-wrapped two-variant outcome (2 confirmed `?` uses), matching Milestone 3.3's shape. The `if !local_partner_ids.is_empty() { ... }` wrapper becomes an early-return guard inside the method, matching Milestones 3.16/3.17's precedent.

**Milestone 3.20 (Phase 12, "total-failure fallback"), dispatched (2026-08-21)**: the last phase inside the per-net loop body. A genuinely new ownership requirement, confirmed by direct read: `repair`/`probe` are both fully *consumed* here (`self.obstacle_map = repair.round_base_map;` and `batch.final_routes.insert(job.net_id, probe.probe_route);` are real moves, not clones, and confirmed to be each struct's last use in the function), so the new method takes `repair: RepairAttemptState`/`probe: ProbeState` by value, not by reference -- different from every prior extraction this session, which all used borrows. Plain `Result<(), ()>` outcome (no named enum -- no third "fall through" case exists here, this is a terminal phase). The 2 `continue 'route_jobs` sites become `return Ok(())`; the one final `break;` becomes `Err(())`; a 12-iteration internal retry loop's own 2 unrelated `break;`s stay untouched.

### Milestone 3 (in progress): extract phases into named methods, one at a time

Now that Milestones 1-2 have landed and validated cleanly, this milestone extracts the 13 identified phases into named methods, one at a time, each independently validated against the full ladder before moving to the next -- not a single big-bang extraction.

**Milestone 3.1 (Phase 6, "probe + `candidate_blockers` construction"), implementation pass done (2026-08-20, Codex)**: chosen first because `ProbeState` (Milestone 2b) already exactly matches this phase's output shape. Design: a new method, `probe_net_for_repair(&mut self, batch: &mut RepairBatchState, job: &NativeRouteJob, order_by_id: &FxHashMap<u64, usize>, block_radius_cells: i32, commit_radius_cells: Option<i32>, collect_native_timing: bool, trace_native_repair: bool) -> Result<ProbeState, ()>`, with the one real control-flow-shape change this task allows: the block's existing `break;` (today directly breaking `'route_jobs` on total probe failure) becomes `return Err(());`, and the call site does `Err(()) => break 'route_jobs`. Every other line moves unchanged except for Rust-required placement and binding details: the helper lives in the existing private inherent `impl PyPhotonicRouter` above the `#[pymethods]` impl because PyO3 rejected a Rust-only helper inside the PyO3 methods impl and also rejected splitting `PyPhotonicRouter` into multiple `#[pymethods]` impl blocks; the call-site binding remains `let mut probe` because later repair phases still mutate `probe.candidate_blockers`. The extracted body referenced no names outside the planned parameter list. Validation for this implementation pass: pre-change `cargo test --lib` was `389 passed; 0 failed`; pre-change `tests/test_rust_batch_repair.py` was `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with the same post-change counts (`389 passed; 0 failed`, `1 passed`). This establishes the `Result<PhaseOutput, ()>` outcome pattern the remaining 12 phases will reuse where they also need to signal "stop the whole batch," and the plain-value-return pattern (`-> SomeStruct` with no `Result` wrapper) for phases that can't reach that outcome.

**Reviewed by Claude**: full diff read (460 lines -- consistent with one ~220-line block moving location, which git shows as remove+add). Confirmed `probe_net_for_repair` is defined exactly once, `let probe_start = ...` (the block's original first line) appears exactly once in the whole file (no duplicate left at the old inline site), and `ProbeState {` appears exactly twice (the struct definition and the one construction site, now inside the new method) -- clean code motion, nothing duplicated or lost. Confirmed the placement rationale (a separate plain `impl PyPhotonicRouter` block at `:2883`, distinct from the `#[pymethods] impl PyPhotonicRouter` at `:8149-8150` that contains `route_many_with_repair_and_commit`) is a real, correct PyO3 constraint, not a workaround. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline.

**Milestone 3.2 (Phase 10, "repair setup"), implementation pass done (2026-08-21, Codex)**: chosen next for the same reason Phase 6 was first -- `RepairAttemptState` (Milestone 2a) already matches this phase's main output. Implemented as `prepare_repair_attempt(&mut self, batch: &mut RepairBatchState, probe: &ProbeState, block_radius_cells: i32, history_increment: u32, max_rounds: u32, max_victims: usize, collect_native_timing: bool) -> RepairAttemptState` (plain return, no `Result` wrapper needed -- this phase can't reach the "stop the whole batch" outcome), placed in the same plain inherent `impl PyPhotonicRouter` block as `probe_net_for_repair` because Milestone 3.1 proved Rust-only helpers cannot live inside the PyO3 methods block here. The extraction stayed narrower than the original 13-phase survey's Phase 10 boundary: `prefer_orthogonal_repair` (computed right after, from `probe.*` fields only) and the `max_rounds`/`max_victims` clamping (`.max(1)`, trivial parameter derivation) both stayed at the call site. Validation for this implementation pass: pre-change `cargo test --lib` was `389 passed, 0 failed` and pre-change `tests/test_rust_batch_repair.py` was `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with the same counts.

**Reviewed by Claude**: full diff read (76 lines -- a small, clean extraction as expected). Confirmed `prepare_repair_attempt` is defined exactly once; `let history_start = native_batch_timer(...)` appears 3 times in the whole file, but only the one inside the new method matches this extraction -- the other two are pre-existing, unrelated timing blocks elsewhere in the function (Phase 2's preemptive ripup, Phase 11's repair loop), not duplicates. `prefer_orthogonal_repair` and `repair_set_index` confirmed untouched immediately after the new call, exactly as designed. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline.

**Milestone 3.7 (Phase 11, sub-phase 11.3 "reset-from-snapshot"), dispatched (2026-08-21)**: the first sub-extraction inside the ~1,300-line Phase 11 loop, per the structural re-survey's own recommendation (see Surprises & Discoveries). A pure side-effecting reset -- no outcome enum needed, `repair` taken as `&RepairAttemptState` (read-only), `batch` as `&mut RepairBatchState`. Verbatim 9-statement move, no control-flow-shape change. This begins Phase 11's extraction sub-phase by sub-phase rather than as one big-bang pass, matching how Milestone 3 handled Phases 6-10.

**Milestone 3.8 (Phase 11, sub-phase 11.9 "rip-up loop"), dispatched (2026-08-21)**: the second Phase-11 sub-extraction. Self-contained `for old_id in &ripup_ids` loop, no closures, no `?`, no early exit. The one accommodation: `lidar_pure_crossing_repair` (computed immediately before this loop) is also read later, further down in the same enclosing iteration, at code well outside this extraction's scope -- so its `let` binding stays at the call site (not moved into the new method) and is passed in as an explicit `bool` parameter, mirroring the `guided_collision_crossing_enabled` accommodation from Milestone 3.3. New method: `ripup_repair_set_victims`, no outcome enum (pure side effect).

**Milestone 3.9 (Phase 11, sub-phases 11.4-11.5 "labeling + repair_mode_start trace"), dispatched (2026-08-21)**: the third Phase-11 sub-extraction, and the first inside Phase 11 that needs a real return value rather than being purely side-effecting. Produces `route_order: &'static str` and `victim_reroute_ids: Vec<u64>`, both read extensively by the rest of this mode-order iteration -- the new method, `label_and_trace_repair_mode_start`, returns both as a tuple, and the call site destructures them with the identical bindings the inline code used (`victim_reroute_ids` still `mut`, since later code reverses/mutates it further downstream in ways outside this extraction's scope -- confirmed the mutation itself, if any, happens after this block, not inside it). Verified `push_native_repair_trace`'s real signature (`src/py_router.rs:876`) takes `ripup_ids: &[u64]`/`candidate_blockers: &[u64]` already, so the only mechanical adjustments are `ripup_ids.clone()` -> `ripup_ids.to_vec()` and dropping the extra `&` on the trace call's `ripup_ids` argument, both confirmed correct against the real signature before dispatch rather than assumed.

**Milestone 3.3 (Phase 7, "guided collision-crossing"), dispatched (2026-08-21)**: the first phase extraction needing a genuinely new outcome shape -- Milestones 3.1/3.2 only needed `Result<T, ()>` ("stop the whole batch") and plain `T` ("always succeeds"). This phase can either resolve the net (commit and `continue 'route_jobs`) or fall through to Phase 8, so it needs a small two-variant enum, `GuidedCrossingOutcome { Routed, NotResolved }`, returned as `PyResult<GuidedCrossingOutcome>` since the block also contains a `?` on a `PyErr`-producing call that today propagates directly out of `route_many_with_repair_and_commit` itself -- the extracted method must preserve that propagation, and the call site needs its own `?` to keep it working. The one control-flow-shape change: the success path's `continue 'route_jobs;` becomes `return Ok(GuidedCrossingOutcome::Routed);`, with the call site's `match` translating it back.

## Concrete Steps

Milestone 0 was read-only source investigation -- no build or benchmark commands. See the geometry-realization plan's Toolchain notes for this machine's Rust toolchain override. Given this is the pipeline's highest-risk code by this session's own repeated characterization, every implementation milestone from here on must run the full validation ladder (`cargo test --lib`, `tests/test_rust_batch_repair.py`, `benes_4x4`, `multiportmmi_8x8` bare + stable-baseline) before being considered complete, per `.agent/WORKFLOW.md`'s Routing Verification Gate -- no milestone here should be self-certified by unit tests alone.

## Validation and Acceptance

Milestone 0 is complete when the phase breakdown and variable lifetime map are direct-evidence-backed (done above). Each later milestone's acceptance criteria is: compiles, passes the full validation ladder above with zero behavior change (byte-identical benchmark verification results, byte-identical pytest baseline), and does not regress on either of the two known traps.

## Idempotence and Recovery

Milestone 0 was entirely read-only and is safe to redo or resume freely; it made no code changes.

## Artifacts and Notes

Originating context: `.agent/execplans/2026-08-20-ripup-repair-orchestration-restructuring.md` (this function's first-pass characterization, test-harness fix, victim-set-expansion extraction, and the `n_67`/`n_70`/`n_71` fix -- all still in effect and not repeated here) and `.agent/REPOSITORY_STATE.md`'s Next Engineering Step.

## Interfaces and Dependencies

To be determined as Milestones 1-2 land; no public interface changes are anticipated (this is an internal restructuring of a `PyPhotonicRouter` method, not a new `Protocol`/`trait` boundary the way the Future Architecture Initiative's four stages were).

Revision note (2026-08-21 / Codex): recorded the Milestone 3.3 implementation pass, validation evidence, and the downstream `guided_collision_crossing_enabled` dependency discovered by `cargo check`; this keeps the plan restartable for the next extraction or review pass.

Revision note (2026-08-21 / Codex): recorded the Milestone 3.5 implementation pass, validation evidence, and the additional `probe.probe_crossing_events` ownership move discovered during extraction.

Revision note (2026-08-21 / Codex): recorded the Milestone 3.10 `RepairModeAttemptState` state-promotion pass, rewritten usage counts, the 6 stale local `mut`s removed after `cargo check`, and focused validation evidence.

Revision note (2026-08-21 / Codex): recorded the Milestone 3.11 `try_reroute_current_net_before_victims` extraction, the required mutable `ProbeState` threading, the distinct repair-set-index roles, and focused validation evidence.
