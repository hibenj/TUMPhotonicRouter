# Full restructuring of route_many_with_repair_and_commit

## Purpose / Big Picture

This is the last major routing-pipeline component without an explicit structure or real test coverage. Every other stage (obstacle map/grid snapping, A* single-net search, geometry realization, path-length matching) has an explicit `Protocol`/`trait` interface as of the just-closed Future Architecture Initiative; `route_many_with_repair_and_commit` (`src/py_router.rs`, ~3,035 lines) was excluded from that initiative at every stage specifically because it needed its own restructuring pass first. Three real bugs were found in or adjacent to it this session alone (zero-event-acceptance, silent partial-restore data loss, the `n_67`/`n_70`/`n_71` victim-set-expansion gap, the last now fixed -- see `.agent/execplans/2026-08-20-ripup-repair-orchestration-restructuring.md`).

The repository owner's stated goal is a readable, tested codebase as the prerequisite for algorithm optimization work, not an end in itself. This function is the single remaining blocker to that goal, and -- per the structural survey below -- also contains at least one concrete, measurable optimization opportunity (repeated full-state cloning) that a restructuring would make safe to address, tying the readability goal directly to the eventual optimization goal.

## Progress

- [x] Milestone 0 (structural survey) done, see Surprises & Discoveries. Dispatched to a fork for the read-only investigation (report only, no design/code -- matching this session's established discipline for characterization work on this exact function, after an earlier fork correctly followed the same instruction on a related task). Produced a 13-phase control-flow breakdown and a full local-variable lifetime map across three natural scope tiers (whole-batch, per-net, per-repair-attempt).
- [x] Milestone 1 (`RepairBatchState` promotion, Codex) done (2026-08-20). All 9 whole-batch-scope locals (`final_routes`, `attempts`, `repair_trace`, `repair_count`, `failed_net_id`, `failed_error`, `retried_source_layers`, `timings`, `trace_last_route_start`) grouped into one struct, constructed once, every usage rewritten to `batch.<field>`. **Reviewed by Claude**: full diff read (464 lines), confirmed via targeted greps that every one of the 9 old bare-name declarations was removed (not left as dead/duplicate code) and that every remaining bare occurrence of the 9 names in the function is either the struct-literal's own field name or an unrelated PyO3 dict-key string literal -- no missed usages, no accidental over-matching. Codex reported finding no 10th whole-function-scope variable and confirmed the moved initializers don't depend on intervening code. Validation independently re-run, not just taken from Codex's report: `cargo test --lib` `389 passed, 0 failed` (unchanged count, as expected for zero-behavior-change), `tests/test_rust_batch_repair.py` unchanged. Given the user's chosen pacing (validate the full ladder after each milestone), also rebuilt the Python extension and ran the real benchmarks: `multiportmmi_8x8` bare defaults and `benes_4x4` both clean (`error_count: 0`; `multiportmmi_8x8` photonic verification's 2 warnings are the same pre-existing, unrelated `endpoint_correction_fallback_used` notices seen after today's `n_70` fix, confirming zero behavior change), full `pytest -q` byte-identical to the 11-failure baseline.
- [x] Milestone 2a (`RepairAttemptState` promotion, Codex) done (2026-08-20). 12 Phase-10 repair-setup locals grouped into a second struct. See Plan of Work's Milestone 2 entry for the full story, including the scoping split (2a mechanical cluster now, 2b closure-entangled cluster deferred) and validation detail. Same review rigor as Milestone 1: full diff read, zero missed usages confirmed, full benchmark ladder re-run, byte-identical to baseline.
- [x] Milestone 2b (`ProbeState` promotion, Codex) done (2026-08-20). The 10 probe-phase outputs were grouped into `ProbeState`, constructed once immediately after `candidate_blockers.sort_unstable_by_key(...)` using field-init shorthand, with the prior closure/intermediate computation left unchanged. Codex found the task's transcribed "two `candidate_blockers` mutation sites" was stale against current source -- the real number is 6 -- and correctly fixed all 6, verifying against real source rather than trusting the task file (this repository's own established Codex-reliability pattern, a repeated instance now). **Reviewed by Claude**: full diff read (230 lines), independently re-confirmed all 6 `enqueue_targeted_illegal_crossing_repair_set` call sites correctly use `&mut probe.candidate_blockers`, and a targeted grep across the whole function found zero missed bare-name usages of the 10 fields (every remaining match is either the struct-literal's own field-init shorthand, an unrelated format-string/dict-key text literal, or a same-named-but-different field on `NativeRouteAttempt`, not this function's local). Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline. This completes Milestone 2 (both 2a and 2b) -- all per-net-scope locals are now grouped into named structs, matching Milestone 1's whole-batch-scope grouping.
- [x] Milestone 3.2 (`prepare_repair_attempt`, Codex) implementation pass done (2026-08-21). Phase 10's history update plus `RepairAttemptState` construction now lives in a plain inherent `impl PyPhotonicRouter` helper next to `probe_net_for_repair`; the call site still performs the trivial `.max(1)` clamps before calling it, and `prefer_orthogonal_repair` plus `repair_set_index` remain immediately after the call unchanged. Pre-change baseline was `cargo test --lib` `389 passed, 0 failed` and `tests/test_rust_batch_repair.py` `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with identical counts (`389 passed, 0 failed`, `1 passed`). No bare, non-`probe.` references to the moved `ProbeState` fields appeared inside the extracted block, and the helper returns the `RepairAttemptState` struct literal directly with no local `mut` binding because no in-method mutation is needed after construction.
- [x] Milestone 3.3 (`try_guided_collision_crossing`, Codex) implementation pass done (2026-08-21). Phase 7's guided collision-crossing body now lives in `try_guided_collision_crossing` in the same plain inherent `impl PyPhotonicRouter` block as `probe_net_for_repair` and `prepare_repair_attempt`, with a module-scope `GuidedCrossingOutcome { Routed, NotResolved }`. The success path's old `continue 'route_jobs;` is now `return Ok(GuidedCrossingOutcome::Routed);`, and the call site translates that variant back into `continue 'route_jobs`; no other control-flow-shape change was made. Pre-change baseline was `cargo test --lib` `389 passed, 0 failed` and `tests/test_rust_batch_repair.py` `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with identical counts (`389 passed, 0 failed`, `1 passed`). The moved body's `self.astar_config(None, None, None).map_err(PyRuntimeError::new_err)?` still compiles inside the new `PyResult<GuidedCrossingOutcome>` helper.

**Reviewed by Claude**: full diff read (254 lines). Confirmed `try_guided_collision_crossing` defined exactly once, `GuidedCrossingOutcome` defined exactly once, and `let guided_start = native_batch_timer(...)` appears twice in the whole file -- one inside the new method (this extraction's own instance), one deep in Phase 11's separate `lidar_seeded_collision_crossing` branch (pre-existing, unrelated code, not a duplicate). One accepted, harmless imperfection worth recording: `guided_collision_crossing_enabled`'s two `std::env::var_os` checks are now evaluated twice per net -- once inlined into the new method's own `if` condition, once still at the call site (kept there because Phase 11, much further down, reads the same value) -- a gap in this task's own design (it assumed the flag was Phase-7-only). Confirmed this does not change observable behavior: process environment variables are stable for the duration of a run in this codebase (nothing here calls `std::env::set_var` mid-routing), so both evaluations always agree; the cost is one redundant, cheap lookup per net, not a correctness issue. Not worth a follow-up milestone on its own. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline.

**Milestone 3.4 (Phase 8, "localized crossing keepout retry"), dispatched (2026-08-21)**: the same two-outcome shape as Phase 7, but confirmed to contain no `?` operator, so the new method returns a plain `LocalizedKeepoutOutcome { Routed, NotResolved }` (no `PyResult` wrapper), matching Milestone 3.2's simpler shape rather than 3.3's. A separate, phase-specific enum rather than reusing `GuidedCrossingOutcome`, matching this plan's established one-named-type-per-phase pattern even where the shape is structurally identical.

## Surprises & Discoveries

- **The function decomposes cleanly into 13 named phases**, each with a clear line range and single responsibility, confirmed by a full line-by-line read (not sampled): (0) job unpacking/indexing, (1) loop entry, (2) preemptive crossing ripup, (3) plain normal-route attempt, (4) lidar pending-straight-victim-hint (source-layer center-out + single-victim ripup), (5) lidar direct-crossing subset attempts, (6) probe + `candidate_blockers` construction, (7) guided collision-crossing, (8) localized crossing keepout retry, (9) commit-if-clean fast path, (10) repair setup (snapshot + `compute_repair_victim_sets`), (11) the adaptive repair-attempt loop (itself sub-phased: reset-from-snapshot, victim reroute, current-net reroute, result tracing), (12) total-failure fallback, (13) result assembly. This is a genuinely different entanglement shape than `route_nets_rust`'s pre-refactor closures -- it is already sequential, already delegates to ~40 named helper methods -- the restructuring problem here is "give this sequential procedure named phase boundaries and explicit state," not "untangle hidden closure captures."
- **Local variables split cleanly into three lifetime tiers, which is the actual design input for how to group them**: whole-batch scope (survives across every net in the loop: `final_routes`, `attempts`, `repair_trace`, `repair_count`, `failed_net_id`, `failed_error`, `retried_source_layers`, `timings`, plus read-only indices built once), per-net scope (fresh each net, but read/written across multiple of the 13 phases within that net: `probe_route`, `candidate_blockers`, the 7 `round_base_*` snapshot fields, `repair_victim_sets`, 3 learned-keepout maps, `repaired`), and per-repair-attempt scope (fresh each inner-loop iteration, no cross-attempt leakage observed: `victim_reroute_ids`, `route_order`, temporary reservations, `mode_failed`, `repaired_route`).
- **Two variables have the exact cross-phase mutability trap that correctly blocked the first Codex dispatch on `compute_repair_victim_sets` earlier today** -- `candidate_blockers` (built read-only-looking in Phase 6, but mutated again deep inside Phase 11 via `&mut candidate_blockers` passed to `enqueue_targeted_illegal_crossing_repair_set`) and `repair_victim_sets` itself (already known). Any phase-extraction task touching Phase 6 or Phase 11 must account for this explicitly, or repeat today's exact near-miss.
- **`retried_source_layers` has a genuinely easy-to-miss lifetime**: it's only touched within Phase 4a's logic, but must persist *across different nets'* loop iterations (a source layer retried while routing one net must not be retried again for a later net sharing that x-coordinate). It reads as phase-local; it is not. A naive "extract Phase 4 into a per-net method" pass would silently break this unless the variable is explicitly threaded from whole-batch scope.
- **A concrete, measurable optimization target, found as a side effect of the structural read, not gone looking for**: the 7 `round_base_*` fields are re-cloned in full at the top of *every single repair attempt* (Phase 11a), not once per net -- so a net needing 4 repair rounds x 2 victim orderings x 2 reverse-order combinations clones the entire obstacle map, committed-route maps, and crossing-event list up to 16 times. This session's own `multiportmmi_8x8` timing output (from today's `n_70` fix validation) shows `victims=123.6s` dominating a 142s total route phase -- consistent with this being a real, not theoretical, cost. Restructuring this function safely is the prerequisite for changing this to restore-by-diff or clone-on-write, which is itself a first concrete algorithm-optimization win once the structure exists to change it safely.
- **Phases 11b (victim reroute) and 11c (current-net reroute) are near-duplicated, symmetric code** -- same shape (attempt reroute, on error call the same two `enqueue_*` helpers, same keepout-learning), differing only in which net goes first. A strong candidate for collapsing into one method parameterized by role, rather than maintaining two ~250-line copies -- itself a readability win independent of the state-management restructuring.
- **No new top-level "session" wrapper is needed the way `_RouteNetsRustSession` was needed for `route_nets_rust`.** That precedent existed because `route_nets_rust` was a free function with ~120 closures needing somewhere to live; this function is already a method on `PyPhotonicRouter` (`&mut self`), which already carries the 20 session-state fields. The entanglement here is in this method's own ~20 call-scoped locals across the three lifetime tiers above, not in a missing `self`.
- **Milestone 2b found current-source drift from the point-in-time dispatch note, without changing the design.** The current function has six post-construction `enqueue_targeted_illegal_crossing_repair_set` call sites mutating `candidate_blockers` through `&mut probe.candidate_blockers`, not two. The six excluded helper locals stayed excluded, but `victim_selection_start` is still read once after the new `ProbeState` construction to charge `repair_probe_victim_selection_us`; it remains a plain local and is not part of `ProbeState`.
- **Milestone 3.2 found the Phase 10 extraction assumptions still matched current source.** The block starting at `let history_start = native_batch_timer(collect_native_timing);` already used `probe.probe_route`, `probe.crossing_repair_enabled`, `probe.probe_realized_crossing_violations`, and `probe.candidate_blockers`; no bare pre-Milestone-2b names were present. The constructed `RepairAttemptState` was not mutated inside the extracted block, so the new helper could return the struct literal directly and leave `mut` only at the call site where later phases mutate `repair`.
- **Milestone 3.3 found one downstream use outside the planned Phase 7 block.** The local `guided_collision_crossing_enabled` is referenced again later in Phase 11's victim-reroute guided branch (`src/py_router.rs`, around the `guided_victim_route` fallback). To keep that later branch compiling without widening the Phase 7 helper's requested signature, the call site still binds `guided_collision_crossing_enabled` before calling `try_guided_collision_crossing`; the helper also evaluates the same environment flag for its own top-level `if`.

## Decision Log

- Decision: proceed through the milestones autonomously (2026-08-20, via `AskUserQuestion`), validating the full benchmark ladder after each step, stopping to report only on a break or a genuine design fork -- rather than checking in after every milestone or stopping after Milestone 1 alone.
  Rationale: repository owner's explicit choice, matching the pace of today's smaller ripup/repair work.
- Decision: in Milestone 3.3, keep the requested `try_guided_collision_crossing` parameter list unchanged and preserve the call-site `guided_collision_crossing_enabled` binding for Phase 11's later use.
  Rationale: `cargo check` showed the dispatch's extracted block boundary missed a downstream reference to that local; keeping the call-site binding is the smallest compile-preserving adjustment while leaving the extracted helper's return type and routing outcome shape as designed.
  Date/Author: 2026-08-21 / Codex

## Outcomes & Retrospective

(To be filled in as this plan's milestones complete.)

## Context and Orientation

`route_many_with_repair_and_commit` (`src/py_router.rs:8723-11758`) is a method on `PyPhotonicRouter` handling batch net routing with rip-up/repair: for each net in a job batch, try to route it cleanly; if blocked, rip up one or more already-routed "victim" nets, reroute the blocked net, then try to reroute the victims elsewhere, with several fallback strategies and a bounded adaptive expansion of which nets to rip up together (`compute_repair_victim_sets`, `enqueue_targeted_illegal_crossing_repair_set`, `enqueue_learned_keepout_repair_retry` -- all already-extracted, already-tested free functions from earlier work this session and 2026-07-02). See the 13-phase breakdown in Surprises & Discoveries for the full control flow, and `.agent/execplans/2026-08-20-ripup-repair-orchestration-restructuring.md` for the session-state field inventory, test-coverage findings, and the three prior bugs' exact locations -- all still accurate, not repeated here.

## Plan of Work

### Milestone 0: structural survey (done, see Surprises & Discoveries)

### Milestone 1 (proposed): introduce `RepairBatchState`, pure state-promotion, zero behavior change

Mirrors the single highest-leverage decision from the `route_nets_rust` precedent (`.agent/REPOSITORY_STATE.md`'s own recorded lesson: "the mid-plan restructuring after Milestone 1 -- inserting a dedicated shared-state-promotion milestone before any closure conversion -- was the single highest-leverage decision in this plan"). Introduce a plain struct, `RepairBatchState`, holding exactly the whole-batch-scope locals identified in the survey (`final_routes`, `attempts`, `repair_trace`, `repair_count`, `failed_net_id`, `failed_error`, `retried_source_layers`, `timings`). Construct it once at the top of the function, thread `&mut RepairBatchState` through the existing inline code with zero logic changes -- this milestone does not extract any phase into a separate method yet, it only proves the state grouping is correct by making the existing code compile and behave identically with the new struct in place. Validate against the full benchmark ladder (same rigor as today's `n_70` fix: `cargo test --lib`, the batch-repair integration test, `benes_4x4`, `multiportmmi_8x8` both bare and stable-baseline) before proceeding.

### Milestone 2 (in progress): introduce a per-net scratch struct, same discipline -- split into two, not one

Same pattern one tier down: group the per-net-scope locals into a struct, constructed fresh each loop iteration, threaded through unchanged. **Scoping adjustment found while designing this milestone precisely** (not part of the original survey): the per-net cluster is not uniform the way Milestone 1's whole-batch cluster was. `probe_route`/`candidate_blockers` and several `probe_*` fields are constructed in the probe phase (Phase 6) alongside a closure, `add_candidate_blocker`, that captures some of these locals by mutable reference to build `candidate_blocker_priority` -- a genuinely more entangled case than Milestone 1's plain defaults. The other 12 locals (`repaired`, the 7 `round_base_*` fields, `repair_victim_sets`, the 3 learned-keepout maps) are declared together in the repair-setup phase (Phase 10) with no closures involved, exactly as mechanical as Milestone 1 was, just with real initializer expressions (mostly `.clone()` calls) instead of plain defaults.

Splitting Milestone 2 into two sub-passes rather than attempting both clusters at once: **Milestone 2a** (this dispatch) covers the 12-field Phase 10 cluster only, as `RepairAttemptState`. **Milestone 2b** (not yet started) will cover the smaller, closure-entangled Phase 6 cluster once 2a validates cleanly -- deferred deliberately, not because it's out of scope, but because mixing the two in one pass would conflate a purely mechanical change with a genuinely trickier one in the same review.

**Milestone 2a done (2026-08-20, Codex)**: `RepairAttemptState` added (`src/py_router.rs:1019`, next to `RepairBatchState`), all 12 fields, constructed once with every initializer expression preserved verbatim, every usage rewritten to `repair.<field>`. The one flagged subtlety (the total-failure fallback moves 6 `round_base_*` fields out of the struct into `self`, not clones) compiled clean with no error -- confirmed by direct check that `repair.round_base_routes` at the fallback's last line is genuinely the last usage of the struct in that net's iteration, so the partial move is safe. **Reviewed by Claude**: full diff read (176 lines), targeted greps confirmed zero missed bare-name usages of the 12 fields anywhere in the function (only the struct-literal's own field names remain unprefixed). Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Real benchmark ladder also re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, `multiportmmi_8x8`'s 2 warnings identical to every prior run today (same pre-existing, unrelated notices); full pytest suite byte-identical to the 11-failure baseline.

**Milestone 2b design (2026-08-20)**: read the probe-phase computation directly rather than delegating the design sight-unseen, given the closure entanglement flagged in Milestone 2's scoping note. Finding: the closure (`add_candidate_blocker`) and its captured local (`candidate_blocker_priority`) are both fully consumed *before* any of the values that need to escape this phase reach their final form -- they never need to coexist with a struct field. This means Milestone 2b does not need to touch the ~140-line intermediate computation at all: construct a new struct, `ProbeState`, once at the very end of that computation (right after `candidate_blockers` is sorted), using field-init shorthand on the already-computed locals, and rewrite only the *later* usages (from that point through the rest of the function) to `probe.<field>`. 10 fields (one more than the original survey's estimate): `probe_route`, `crossing_repair_enabled`, `allowed_crossing_partners`, `probe_crossing_events`, `strict_expected_crossing_probe` (a field the original survey didn't call out explicitly, confirmed by direct trace to be read as far downstream as the commit-if-clean phase), `probe_crossing_compliant`, `probe_realized_crossing_violations`, `probe_grid_crossing_violations`, `probe_repair_keepout_keys`, `candidate_blockers` (carries the same later-mutated-via-`&mut` trap as Milestones 1/2a's sibling variables, already flagged explicitly for this dispatch).

### Milestone 3 (in progress): extract phases into named methods, one at a time

Now that Milestones 1-2 have landed and validated cleanly, this milestone extracts the 13 identified phases into named methods, one at a time, each independently validated against the full ladder before moving to the next -- not a single big-bang extraction.

**Milestone 3.1 (Phase 6, "probe + `candidate_blockers` construction"), implementation pass done (2026-08-20, Codex)**: chosen first because `ProbeState` (Milestone 2b) already exactly matches this phase's output shape. Design: a new method, `probe_net_for_repair(&mut self, batch: &mut RepairBatchState, job: &NativeRouteJob, order_by_id: &FxHashMap<u64, usize>, block_radius_cells: i32, commit_radius_cells: Option<i32>, collect_native_timing: bool, trace_native_repair: bool) -> Result<ProbeState, ()>`, with the one real control-flow-shape change this task allows: the block's existing `break;` (today directly breaking `'route_jobs` on total probe failure) becomes `return Err(());`, and the call site does `Err(()) => break 'route_jobs`. Every other line moves unchanged except for Rust-required placement and binding details: the helper lives in the existing private inherent `impl PyPhotonicRouter` above the `#[pymethods]` impl because PyO3 rejected a Rust-only helper inside the PyO3 methods impl and also rejected splitting `PyPhotonicRouter` into multiple `#[pymethods]` impl blocks; the call-site binding remains `let mut probe` because later repair phases still mutate `probe.candidate_blockers`. The extracted body referenced no names outside the planned parameter list. Validation for this implementation pass: pre-change `cargo test --lib` was `389 passed; 0 failed`; pre-change `tests/test_rust_batch_repair.py` was `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with the same post-change counts (`389 passed; 0 failed`, `1 passed`). This establishes the `Result<PhaseOutput, ()>` outcome pattern the remaining 12 phases will reuse where they also need to signal "stop the whole batch," and the plain-value-return pattern (`-> SomeStruct` with no `Result` wrapper) for phases that can't reach that outcome.

**Reviewed by Claude**: full diff read (460 lines -- consistent with one ~220-line block moving location, which git shows as remove+add). Confirmed `probe_net_for_repair` is defined exactly once, `let probe_start = ...` (the block's original first line) appears exactly once in the whole file (no duplicate left at the old inline site), and `ProbeState {` appears exactly twice (the struct definition and the one construction site, now inside the new method) -- clean code motion, nothing duplicated or lost. Confirmed the placement rationale (a separate plain `impl PyPhotonicRouter` block at `:2883`, distinct from the `#[pymethods] impl PyPhotonicRouter` at `:8149-8150` that contains `route_many_with_repair_and_commit`) is a real, correct PyO3 constraint, not a workaround. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline.

**Milestone 3.2 (Phase 10, "repair setup"), implementation pass done (2026-08-21, Codex)**: chosen next for the same reason Phase 6 was first -- `RepairAttemptState` (Milestone 2a) already matches this phase's main output. Implemented as `prepare_repair_attempt(&mut self, batch: &mut RepairBatchState, probe: &ProbeState, block_radius_cells: i32, history_increment: u32, max_rounds: u32, max_victims: usize, collect_native_timing: bool) -> RepairAttemptState` (plain return, no `Result` wrapper needed -- this phase can't reach the "stop the whole batch" outcome), placed in the same plain inherent `impl PyPhotonicRouter` block as `probe_net_for_repair` because Milestone 3.1 proved Rust-only helpers cannot live inside the PyO3 methods block here. The extraction stayed narrower than the original 13-phase survey's Phase 10 boundary: `prefer_orthogonal_repair` (computed right after, from `probe.*` fields only) and the `max_rounds`/`max_victims` clamping (`.max(1)`, trivial parameter derivation) both stayed at the call site. Validation for this implementation pass: pre-change `cargo test --lib` was `389 passed, 0 failed` and pre-change `tests/test_rust_batch_repair.py` was `1 passed`; post-change `cargo check --lib`, `cargo test --lib`, and `tests/test_rust_batch_repair.py` all passed with the same counts.

**Reviewed by Claude**: full diff read (76 lines -- a small, clean extraction as expected). Confirmed `prepare_repair_attempt` is defined exactly once; `let history_start = native_batch_timer(...)` appears 3 times in the whole file, but only the one inside the new method matches this extraction -- the other two are pre-existing, unrelated timing blocks elsewhere in the function (Phase 2's preemptive ripup, Phase 11's repair loop), not duplicates. `prefer_orthogonal_repair` and `repair_set_index` confirmed untouched immediately after the new call, exactly as designed. Validation independently re-run: `cargo test --lib` `389 passed, 0 failed` (unchanged), batch-repair integration test unchanged. Full benchmark ladder re-run (Python extension rebuilt): `multiportmmi_8x8` and `benes_4x4` both clean, `error_count: 0`, warnings identical to every prior run today; full pytest suite byte-identical to the 11-failure baseline.

**Milestone 3.3 (Phase 7, "guided collision-crossing"), dispatched (2026-08-21)**: the first phase extraction needing a genuinely new outcome shape -- Milestones 3.1/3.2 only needed `Result<T, ()>` ("stop the whole batch") and plain `T` ("always succeeds"). This phase can either resolve the net (commit and `continue 'route_jobs`) or fall through to Phase 8, so it needs a small two-variant enum, `GuidedCrossingOutcome { Routed, NotResolved }`, returned as `PyResult<GuidedCrossingOutcome>` since the block also contains a `?` on a `PyErr`-producing call that today propagates directly out of `route_many_with_repair_and_commit` itself -- the extracted method must preserve that propagation, and the call site needs its own `?` to keep it working. The one control-flow-shape change: the success path's `continue 'route_jobs;` becomes `return Ok(GuidedCrossingOutcome::Routed);`, with the call site's `match` translating it back.

## Concrete Steps

Milestone 0 was read-only source investigation -- no build or benchmark commands. See the geometry-realization plan's Toolchain notes for this machine's Rust toolchain override. Given this is the pipeline's highest-risk code by this session's own repeated characterization, every implementation milestone from here on must run the full validation ladder (`cargo test --lib`, `tests/test_rust_batch_repair.py`, `benes_4x4`, `multiportmmi_8x8` bare + stable-baseline) before being considered complete, per `.agent/WORKFLOW.md`'s Routing Verification Gate -- no milestone here should be self-certified by unit tests alone.

## Validation and Acceptance

Milestone 0 is complete when the phase breakdown and variable lifetime map are direct-evidence-backed (done above). Each later milestone's acceptance criteria is: compiles, passes the full validation ladder above with zero behavior change (byte-identical benchmark verification results, byte-identical pytest baseline), and does not regress on either of the two known traps.

## Idempotence and Recovery

Milestone 0 was entirely read-only and is safe to redo or resume freely; it made no code changes.

## Artifacts and Notes

Originating context: `.agent/execplans/2026-08-20-ripup-repair-orchestration-restructuring.md` (this function's first-pass characterization, test-harness fix, victim-set-expansion extraction, and the `n_67`/`n_70`/`n_71` fix -- all still in effect and not repeated here) and `.agent/REPOSITORY_STATE.md`'s Next Engineering Step.

## Interfaces and Dependencies

To be determined as Milestones 1-2 land; no public interface changes are anticipated (this is an internal restructuring of a `PyPhotonicRouter` method, not a new `Protocol`/`trait` boundary the way the Future Architecture Initiative's four stages were).

Revision note (2026-08-21 / Codex): recorded the Milestone 3.3 implementation pass, validation evidence, and the downstream `guided_collision_crossing_enabled` dependency discovered by `cargo check`; this keeps the plan restartable for the next extraction or review pass.
