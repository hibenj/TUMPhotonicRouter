# Restructure crossing-partner discovery and collision-crossing routing

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This document must be maintained in accordance with `.agent/PLANS.md`, which is checked into this repository at that path and describes the required structure and editing discipline for ExecPlans in general.

## Purpose / Big Picture

This repository's crossing-legality logic (`src/py_router.rs`) supports two conceptually different modes: `allow_only_expected_pairs` (crossings are pre-planned from the schematic's topology -- a net is only allowed to cross specific, named partners decided before routing starts) and `lidar-pure` (the mode this repository's active benchmarks actually use -- a net's crossings are discovered by colliding with whatever is physically nearby during the search, not pre-decided). The repository owner's own mental model of `lidar-pure` is simple: A* just routes, and when it collides with another net's cell, it asks "which net is this and is crossing it legal here." Reading the real code this session (while diagnosing and fixing `.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md`'s bug) found that the actual implementation does not read that cleanly: there are at least seven distinctly-named "find the candidate partner nets" functions and at least three independent, hand-written, textually-divergent blocks of code (in three different top-level functions) that each separately decide "what partner set should I use for a collision-crossing attempt on this net" -- and two of those three disagree with each other about how `lidar-pure` mode should behave. The bug just fixed lived inside exactly this tangle, and its investigation is what surfaced the tangle in the first place, not a separate audit.

After this plan is complete, there will be one clear, well-tested answer to "how does this repository decide which other nets a route might legally cross, and where does that decision get made" -- matching the repository owner's own reactive, collision-driven mental model wherever `lidar-pure` mode is active, with whatever remains of the older `allow_only_expected_pairs`-specific plumbing clearly separated from it rather than sharing functions that silently branch between two different intents.

## Progress

- [x] (2026-08-19) Milestone 0, steps 1/3/4 substantially complete: full call-graph mapped for all seven partner-lookup functions and the three known decision sites via exhaustive `grep`, all seven function bodies read in full, dead-code check done for the four `_with_repair_keepout` wrapper variants, PyO3 boundary confirmed. See Surprises & Discoveries for the complete, evidenced findings, replacing the plan's original "hypothesis, not yet confirmed" framing. Step 2 (divergence audit) is partially done: the native-vs-repair two-stage-vs-single-stage divergence has a git-blame trace (see below) but its *rationale* is not fully confirmed, only that it was a same-commit, deliberate-looking choice rather than accidental drift across unrelated commits -- flagged as a question for the repository owner rather than guessed. The `route_many_with_repair_and_commit` divergence audit (git blame for its own three internal decision points) has not been done yet.
- [ ] Milestone 0 remaining: git-blame rationale confirmation for `route_many_with_repair_and_commit`'s own three internal partner-set usages; Milestone 1's target-shape lock-in given the terrain is now known to be *larger* than the plan's original draft anticipated (five distinct usage contexts inside `route_many_with_repair_and_commit` alone, not the single "third decision site" the plan's Surprises & Discoveries originally described).

## Surprises & Discoveries

- Observation: there are at least seven distinctly-named "candidate partner set" functions in `src/py_router.rs`, found while tracing the collision-crossing zero-event-acceptance bug: `crossing_allowed_partner_set` (line ~3201 as of this writing -- line numbers will drift as this plan's own work proceeds, always re-grep rather than trust a cited number), `lidar_pure_owner_lookup_partner_set` (~3220), `lidar_route_window_partner_lookup_set` (~3234), `lidar_probe_partner_lookup_set` (~3264), `lidar_route_result_partner_lookup_set` (~3277), `crossing_partner_lookup_set_for_result` (~3334), `crossing_partner_lookup_set_for_route` (~3355). At least two of these are genuinely, defensibly different (`lidar_route_window_partner_lookup_set` computes a bounding box from a net's *source/target state* before a route exists, to scope a pre-search attempt; `lidar_route_result_partner_lookup_set` computes a bounding box from an *actual route's cells*, after a route exists, for post-search validation) -- but several others look like they could collapse into one parameterized helper (a single "partner set within a bounding box, computed from either two states or a route's cells" function) rather than five-plus near-copies. This has *not* been confirmed as safe to do yet -- it is a hypothesis from reading, not from a full call-graph trace.
- Observation: `crossing_allowed_partner_set` (~3201) itself internally branches on `allow_only_expected_pairs`: when true, it returns the topology-declared allowed-partner set (the real "expected pairs" mode); when false (i.e., in `lidar-pure` mode), it returns *every currently-committed net in the entire obstacle map*, unfiltered by any spatial window at all -- a single function serving two conceptually unrelated callers with two different meanings of "allowed partner," selected by a mode flag buried inside the function body rather than being two separate, clearly-named functions.
- Observation: `lidar_pure_owner_lookup_partner_set` (~3220) has a doc comment that states the repository owner's own mental model explicitly and is worth quoting in full, since it is the clearest evidence of what the *intended* design already is for at least this one function: "In LiDAR-pure this is not a whitelist of nets the route is allowed to cross. It is only the centerline lookup database for owners that A* discovers from actual dynamic obstacle cells while expanding moves." This confirms the reactive, collision-driven design is the documented intent, at least here -- the open question this plan exists to answer is whether every other partner-lookup function and call site is actually honest to that same intent, or whether some of them (particularly the *windowed*, pre-search ones like `lidar_route_window_partner_lookup_set`) are a different, more restrictive mechanism that predates or was bolted on alongside the reactive one, rather than being part of the same coherent design.
- Observation: at least three independent, hand-written blocks decide "what partner set should this net's collision-crossing attempt use" (all found via `grep -n "let collision_partner_ids"`), and they do not all agree:
  1. `route_single_net_and_commit_native` (~line 5386's function, decision block ~line 5466): `if self.use_collision_crossing_routing && !lidar_pure_crossing { crossing_partner_lookup_set_for_route(...) } else { empty }` -- in `lidar-pure` mode, this expression alone yields an *empty* set (the `!lidar_pure_crossing` guard excludes it), but the *same function*, later in its own body (~line 5617-5636), has a *second*, separate "deferred" attempt specifically for the `lidar_pure_crossing` case, using `lidar_pure_owner_lookup_partner_set` (the full, unwindowed set) instead. So this one function tries collision-crossing up to twice, via two different code paths with two different partner-set sources, depending on mode.
  2. `route_single_net_and_commit_repair_native` (~line 5913's function, decision block ~line 5988): `if self.use_collision_crossing_routing { if lidar_pure_crossing { lidar_pure_owner_lookup_partner_set(...) } else { crossing_partner_lookup_set_for_route(...) } }` -- in `lidar-pure` mode, this function uses the full unwindowed set *immediately*, as its one and only attempt; it does not have the two-stage windowed-then-full structure the sibling function above has.
  3. Inside `route_many_with_repair_and_commit` itself (~line 8530's function, decision block ~line 10527), a *third*, independently-written version of this same decision, in a probe/repair-candidate-selection context, not yet read in detail as of this entry.
  These three sibling/near-sibling functions (`route_single_net_and_commit_native`, `route_single_net_and_commit_repair_native`, and whatever the third site's enclosing logic actually needs) look like they grew independently rather than sharing one canonical "decide the collision partner set for this net" helper, which is exactly the kind of duplication-with-silent-divergence this plan exists to eliminate. Confirming whether the two known-divergent behaviors (two-stage vs. single-stage) are an intentional difference for a real reason, or an accidental drift, is Milestone 0 work, not yet done.
  Also not yet checked: whether `route_single_net_and_commit_native_with_repair_keepout` (~6207), `route_single_net_and_commit_repair_native_with_repair_keepout` (~6293), `route_single_net_and_commit_orthogonal_native_with_repair_keepout` (~6448), and `route_single_net_and_commit_native_with_optional_orthogonal_repair_keepout` (~6509) -- four more, similarly-named "route a single net and commit" functions found via an earlier session's `grep` for this family -- also each have their own copy of this decision, delegate to one of the two functions above, or don't touch collision-crossing at all. This is exactly the kind of question Milestone 0's full call-graph trace must answer before any consolidation is designed.
  This exact tangle is where `.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md`'s bug (`try_route_with_collision_crossings_using_primitives` accepting a route with zero real crossing events) lived -- that function is called from multiple of these sites, each with a different partner set, and the bug's fix (require at least one real crossing event) applies uniformly regardless of which caller's partner set was used, but the *callers'* own disagreement about what partner set to try was never itself examined or fixed by that plan, only the shared acceptance-criteria bug downstream of all of them.
- Observation: the "Python orchestrates, Rust internals stay stable" architectural decision recorded in `.agent/execplans/2026-08-19-restructure-port-endpoint-correction.md`'s Decision Log does **not** apply here the way it did there. That decision was about *Python-side* orchestration of pipeline stages; this plan's whole subject is internal Rust logic inside a single stage (native routing/repair), so touching Rust directly is expected and necessary here, not an exception requiring special justification the way it was for the endpoint-correction plan's Milestone 0.5.

- Observation (2026-08-19, Milestone 0, confirmed by reading all seven functions in full plus an exhaustive `grep` of every call site): the seven functions' real shape and call graph. **`crossing_allowed_partner_set(net_id)`** (line 3201) is the true base case: if crossing is disabled, empty set; if `allow_only_expected_pairs=true`, the topology-declared allowed partners (filtered to ones with committed cells); if `false` (i.e. lidar-pure, or collision-crossing-with-expected-pairs -- see the next observation), *every currently-committed net in the entire obstacle map*, completely unwindowed. **`lidar_pure_owner_lookup_partner_set(net_id)`** (3220) is a pure one-line passthrough to `crossing_allowed_partner_set` with an explanatory doc comment; it adds zero behavior of its own -- genuinely redundant as an *implementation*, but arguably not as a *name*, since it gives lidar-pure callers a semantically honest label ("owner lookup database", not "allowed partners") for what is, underneath, the same full-map call. **`lidar_route_window_partner_lookup_set`** (3234) and **`lidar_route_result_partner_lookup_set`** (3277) are a genuine, defensible pre-/post-search pair -- both compute a bounding box (with a caller-supplied margin) and filter `obstacle_map.net_route_entries()` to nets with any cell inside it, but one derives the box from a *source/target state pair* (before a route exists) and the other from an *actual route's cells or compressed waypoints* (after one exists); their bbox-and-filter bodies are near-identical, unshared code, a real, low-risk candidate for a shared "cells within bbox" helper. Both have exactly one call site each, both purely internal to their respective wrappers. **`crossing_partner_lookup_set_for_route`** (3355) and **`crossing_partner_lookup_set_for_result`** (3334) are the actual public-facing pair every real caller uses: each computes the same crossing-margin formula from `CrossingConfig`, then delegates to the matching windowed function above (in lidar-pure mode) or straight to `crossing_allowed_partner_set` (otherwise) -- itself a second, legitimate pre-/post-search pair mirroring the layer beneath it. **`lidar_probe_partner_lookup_set`** (3264) stands apart from all six others: it does not compute a bounding box against `net_route_entries()` at all, but instead queries the obstacle map's actual dynamic cell ownership near a *probe route* (`dynamic_owners_for_native_route`) and filters to owners already present in a caller-supplied `final_routes` map -- a fundamentally different mechanism (real cell-level ownership, not a coordinate-bbox test), and it has exactly one call site, inside `route_many_with_repair_and_commit`'s victim-selection logic (see below), where it is used to decide whether a collision found by a probe route is a legal crossing partner (not a rip-up victim) rather than to seed a search attempt. Conclusion for Milestone 1: the seven functions are not seven independent near-duplicates needing a single merge -- they are two real pre-/post-search pairs (states-vs-route-cells) sharing unshared bbox-filter code worth extracting, one pure-naming passthrough, one true base case, and one structurally distinct probe/ownership mechanism that should stay separate.
- Observation (2026-08-19, Milestone 0, confirmed by reading `crossing_allowed_partner_set`'s body directly): the plan's original Context and Orientation section describes only two crossing modes (`allow_only_expected_pairs` and `lidar-pure`), but the code actually supports a third combination: `use_collision_crossing_routing=true` *and* `allow_only_expected_pairs=true` simultaneously -- "collision-crossing search mechanics, but restricted to the topology-declared allowed-partner set" (visible in `route_single_net_and_commit_native`'s own `used_collision_crossing_attempt`/`require_crossing_compliant_route` guards, lines 5461-5471). It is not clear yet whether this third combination is ever actually exercised by a real benchmark or CLI flag combination, or whether it is dead-in-practice plumbing left over from an earlier design; not yet checked, worth resolving during Milestone 1 rather than silently assuming either answer.
- Observation (2026-08-19, Milestone 0, divergence audit, `route_single_net_and_commit_native` read in full lines 5386-5650ish): the "two-stage vs. single-stage" divergence the plan's Surprises & Discoveries already flagged is real, and reading the full function body explains its *shape*, though not yet its full *rationale*. `route_single_net_and_commit_native`'s actual flow in lidar-pure mode is: (1) the first `collision_partner_ids` computation (line 5466) is *always empty* in lidar-pure mode (guarded by `!lidar_pure_crossing`), so the first `crossing_attempt` block never fires a collision-crossing search for lidar-pure nets at all; (2) it then unconditionally tries a *plain, non-crossing* A* search first (lines 5510-5616) and returns immediately if that commits successfully; (3) only if the plain search fails or fails to commit does it fall through to a *second*, separate collision-crossing attempt (line 5617-5634) using `lidar_pure_owner_lookup_partner_set` (the full, unwindowed set). In other words: for a fresh (non-repair) net in lidar-pure mode, this function tries the cheap non-crossing path first and only pays for a full-map collision-crossing search when that fails -- a plausible, defensible ordering (crossings are the exception, not the default, so try the cheap case first), not obviously accidental.
- Observation (2026-08-19, Milestone 0, divergence audit, `route_single_net_and_commit_repair_native` read in full lines 5913-6100ish): the repair variant's `collision_partner_ids` (line 5988) is computed *once*, and in lidar-pure mode goes straight to `lidar_pure_owner_lookup_partner_set` (the full set) -- there is no windowed pre-attempt at all here, unlike the native function's skipped-in-lidar-pure first stage. The resulting `crossing_attempt` (full-set collision-crossing) is tried *first*; only if it fails does the function fall through to a plain A* search (lines 6107-6139), with an explicit comment directly above that fallback: "Keep collision-crossing as a preferred fast path, but fall back to ordinary A* when the local legal crossing candidate is rejected" (lines 6102-6103). So the two functions try the same two things in *opposite order*: native tries plain-then-crossing, repair tries crossing-then-plain. `git blame` on both decision blocks traces the lidar-pure-specific branches in *both* functions to the *same single commit*, `0d023891` ("routing: stabilize lidar-pure dense routing", 2026-07-17), while the repair function's "preferred fast path" comment itself predates that commit by four days (`56e8a1d6`, 2026-07-13, when the repair function's collision-crossing-first structure already existed for the *non*-lidar-pure case, and `0d023891` extended it to lidar-pure without changing that ordering). This is evidence the divergence was a *deliberate, same-commit* choice, not independent accidental drift across unrelated edits -- but `0d023891`'s own commit message ("keep LiDAR-pure crossing search owner-driven while preserving local crossing legality checks... release routed foreign keepout cells... add long-straight congestion guidance") is a broad, 1099-line, six-file commit that does not spell out *why* repair specifically should prefer crossing-first while native prefers plain-first. A plausible, unconfirmed rationale: repair only runs after a net has already failed to route normally (via rip-up or congestion), so the context already suggests congestion/crossings are likely, making crossing-first a reasonable prior specifically for repair -- but this is this plan's own inference, not confirmed by the commit message or by asking the repository owner (who is also the commit's author). Flagging as an open question for Milestone 1 rather than assuming either "confirmed intentional, keep as-is" or "unexplained, unify them" without asking.
- Observation (2026-08-19, Milestone 0, step 1, the `route_many_with_repair_and_commit` divergence site turns out to be five sites, not one): this function (`src/py_router.rs` line 8530, spanning to beyond line 11127 -- over 2500 lines) contains *five* distinct places that compute or consume a crossing-partner set, a materially larger and more varied terrain than the plan's original Surprises & Discoveries entry ("a third, independently-written version of this same decision... not yet read in detail") anticipated:
  1. Line ~9241-9289 (main per-job loop, guarded by `lidar_pure_crossing_enabled() && use_collision_crossing_routing`): computes `crossing_partner_lookup_set_for_route` (the windowed pre-search set), filters it to partners already present in `final_routes` (i.e. nets already fully committed within this same repair batch), then -- unlike every other call site, which passes a whole partner set to one search attempt -- iterates the resulting candidates *one partner at a time*, calling `try_route_with_collision_crossings` separately per candidate. This is a structurally distinct *strategy* (sequential single-partner probing), not just a different partner-set *source*.
  2. Line ~9393-9424 (victim-selection, after a probe route is found): computes `allowed_crossing_partners` to distinguish "this collision is with a legitimate potential crossing partner" from "this collision is with a genuine rip-up victim." Branches explicitly on the same two modes: `crossing_allowed_partner_set` (filtered to `final_routes`) for expected-pairs mode, `lidar_probe_partner_lookup_set` for lidar-pure mode -- the one and only call site of that seventh function, confirming its actual purpose is victim-set filtering, not search-attempt seeding.
  3. Line ~10527-10534 (deep inside victim-rerouting/guided-repair logic, inside a `victim_job` context): computes `crossing_partner_lookup_set_for_route` (the windowed set, same function as site 1, but used here to seed one `try_route_with_collision_crossings_with_loss` call for a displaced victim net, not a per-partner probing loop).
  4/5. Two more call sites at `crossing_allowed_partner_set` (~9411, inside site 2's own expected-pairs branch, already counted above) and the three-line block at 9241-9245 (already counted as site 1) -- listed separately here only because the earlier `grep` pass counted raw call-site line numbers before this narrative pass grouped them by actual usage context; five *usage contexts*, not five more functions.
  None of these five have been git-blamed yet for their own individual history; that is the remaining Milestone 0 work.
- Observation (2026-08-19, Milestone 0, step 4, PyO3 boundary confirmed): none of the seven partner-lookup functions, nor `route_single_net_and_commit_native`, nor `route_single_net_and_commit_repair_native`, carry a `#[pyo3(...)]` attribute or appear in `translation/*.py`/`python/` (confirmed via exhaustive `grep`, zero matches) -- all are safely Rust-internal, as the plan's Interfaces and Dependencies section expected. `route_many_with_repair_and_commit` itself *is* a `#[pyo3(signature=...)]`-annotated public method (the batch repair entry point Python calls), but this plan's scope is its *internal* partner-set decisions, not its own signature, so this does not block Rust-internal restructuring of what happens inside it.
- Observation (2026-08-19, Milestone 0, step 3, dead-code check on the four `_with_repair_keepout` variants the plan flagged as not-yet-checked): none are independent decision sites. All four (`route_single_net_and_commit_native_with_repair_keepout`, `route_single_net_and_commit_repair_native_with_repair_keepout`, and the two orthogonal variants) are confirmed, by reading their bodies directly, to be thin wrappers that retry `route_single_net_and_commit_native`/`route_single_net_and_commit_repair_native` (the two already-mapped functions) with progressively filtered opened-cell sets to route around a repair keepout region; none call any of the seven partner-lookup functions directly. They do not add a fourth/fifth/sixth independent divergence risk beyond the two already-analyzed functions they delegate to -- they are pure call-count multipliers on the same underlying logic (all four are called from within `route_many_with_repair_and_commit`, plus one from a currently-unidentified caller at lines 6539/6556, not yet traced).

## Decision Log

- Decision: this restructuring pass starts with the crossing-partner-discovery/collision-crossing-routing architecture specifically, not a general dead-code audit of the whole Rust codebase and not a different area, given a direct choice between the two.
  Rationale: this area was found concretely, in this same session, while diagnosing and fixing a real bug (`2026-08-19-fix-collision-crossing-zero-event-acceptance.md`) that lived inside it; the repository owner's own question ("can this not be simplified... A* just routes and when it collides it asks which net is it") independently arrived at the same place reading real code arrived at, which is strong signal this is a genuine, not imagined, problem, and the terrain is already partially mapped (see Surprises & Discoveries) rather than needing to be found from scratch.
  Date/Author: 2026-08-19, repository owner (via direct choice) and Claude, recorded here.
- Decision: this plan's Milestone 0 must be a full, systematic characterization (complete call graph of every partner-lookup function and every `route_single_net_and_commit*` variant, git-blame history for when each diverged, and a confirmed dead-or-live determination for each) before any redesign is specified in implementation-level detail.
  Rationale: this session's own reading already found two confirmed-divergent implementations of "the same" decision (see Surprises & Discoveries) via a partial, non-exhaustive read; per `.agent/PLANS.md`'s guidance against over-specifying implementation before source inspection has made the design clear, and given how easily a partial read has already produced surprising, load-bearing findings here, a full audit is required before committing to a specific target shape, not optional groundwork that can be skipped in favor of moving faster.
  Date/Author: 2026-08-19, Claude, recorded here.
- Decision: unlike the endpoint-correction restructuring plan, this plan does not treat "touching Rust" as requiring a special exception or owner sign-off -- it is the plan's whole subject matter.
  Rationale: see the corresponding Surprises & Discoveries entry; the prior plan's "Python orchestrates, Rust stays stable" decision was scoped to Python-side pipeline orchestration specifically, and does not generalize to "never restructure Rust internals," which would make this plan's stated purpose impossible.
  Date/Author: 2026-08-19, Claude, recorded here.

## Outcomes & Retrospective

(To be filled in as this plan's milestones complete.)

## Context and Orientation

This section assumes no knowledge of any prior conversation or prior ExecPlan. Every fact needed to understand and execute this plan is repeated here.

### The two crossing modes

This repository's router can require or discover "crossings" -- points where two nets' waveguides physically cross, which is legal only under specific geometric conditions (perpendicular enough, enough straight run on both sides, etc., configured via `CrossingConfig` in `src/py_router.rs`). Two modes exist, controlled by `crossing_context.config().allow_only_expected_pairs`:

- **`allow_only_expected_pairs = true`** ("expected pairs" mode): before routing, some upstream process (topology analysis) decides which specific pairs of nets are *allowed* to cross, and routing must either achieve exactly those crossings or fail. `crossing_allowed_partner_set` returns this pre-decided set in this mode, and `try_route_through_expected_crossing_partner` (`src/py_router.rs`, ~line 5211) is the search entry point built around it.
- **`allow_only_expected_pairs = false`**, combined with `use_collision_crossing_routing = true` ("lidar-pure" mode, gated by the helper `lidar_pure_crossing_enabled()` at ~line 3228): crossings are *discovered*, not pre-decided -- a net's route is allowed to cross whichever other already-committed nets it happens to run into, subject to the same legality checks. This is the mode this repository's actively-tracked benchmarks (`benes_4x4`, `multiportmmi_8x8`, `multiportmmi_16x16`) use via `--crossing-mode lidar-pure`. `try_route_with_collision_crossings` (and its wrappers, `..._with_loss` and `..._using_primitives`) is the search entry point for this mode -- see `.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md` for the bug this session already fixed inside it.

### Why "partner set" functions exist at all

A* cannot discover "which other nets exist nearby" purely from a cell coordinate during search -- to validate whether a specific physical crossing is *legal*, the search needs the specific candidate partner net's own committed waypoints (to check angle and margin against). So before or during a search attempt, something has to answer "which other nets' waypoints should I even have on hand to check against." That answer-computing step is what all of the partner-lookup functions listed in Surprises & Discoveries are for. The repository owner's own simpler mental model -- "A* just routes, and when it collides it asks which net is it" -- is *architecturally close* to at least one of these functions' documented intent (`lidar_pure_owner_lookup_partner_set`'s own comment), but this plan's job is to confirm (or fix) whether the rest of the code actually behaves that simply, given the divergences already found.

### Relevant files

Everything in scope is in `src/py_router.rs` (a single, very large file -- 17,000+ lines as of this session). No Python-side files are expected to need changes for this plan's subject matter, though Milestone 0's call-graph trace should confirm nothing in `translation/route_rust*.py` depends on Rust-side function names or signatures this plan might change (the PyO3 boundary is the `#[pymethods]`-annotated public surface; purely-internal Rust helper functions like the ones this plan concerns are not part of that boundary and are safe to rename/consolidate without any Python-side change, but this should be confirmed, not assumed).

## Plan of Work

### Milestone 0: exhaustive characterization

Before any redesign, build a complete, verified picture:

1. **Full call graph**: for every function named in Surprises & Discoveries (the seven partner-lookup functions, the three collision-crossing search entry points `try_route_with_collision_crossings*`/`try_route_through_collision_partner_set`/`try_route_through_expected_crossing_partner`, and every `route_single_net_and_commit*` variant), list every call site, which mode(s) it's reachable under, and what partner set (if any) it passes downstream. Confirm or refute the hypothesis that some of the seven partner-lookup functions are true near-duplicates safe to consolidate.
2. **Divergence audit**: for the three confirmed-independent `collision_partner_ids` decision blocks (Surprises & Discoveries), determine via `git blame`/`git log -p` whether the two-stage-vs-single-stage difference between `route_single_net_and_commit_native` and `route_single_net_and_commit_repair_native` was an intentional design choice (and if so, why a "repair" context wouldn't also want the two-stage behavior, or vice versa) or accidental drift from independent edits over time. Do the same for the third, not-yet-read site inside `route_many_with_repair_and_commit`.
3. **Dead code check**: for every function this milestone maps, confirm via exhaustive `grep` (matching this repository's established practice from the unify-port-access-region-computation plan) whether it has zero real callers (fully dead), exactly one (a candidate for inlining), or genuinely serves multiple distinct call sites (a candidate for staying a shared helper). Do not assume a function is dead or duplicate from a partial read -- this session's own partial read already produced two surprising, load-bearing findings that a shallower pass would have missed.
4. **Confirm the PyO3 boundary**: verify (via `#[pymethods]` annotations and a search of `translation/route_rust*.py` for direct calls into any function this plan might touch) that none of this plan's likely-in-scope functions are part of the Python-visible surface, so this plan's changes can stay Rust-internal.

Write the findings into this plan's own Surprises & Discoveries as they're confirmed, replacing today's "hypothesis, not yet confirmed" framing with definite answers. Only after this milestone is genuinely complete should Milestone 1's target shape be locked in.

### Milestone 1: design and land the consolidated partner-discovery mechanism

Not yet specified in detail, per `.agent/PLANS.md`'s guidance against over-specifying before source inspection justifies the design -- Milestone 0 must inform this. Expected shape, to be confirmed or revised once Milestone 0 completes: one clearly-named, single implementation of "given a net (and either its planned source/target or an already-found route), return the set of other nets it should check crossing legality against," parameterized over the bounding-box-vs-full-map distinction rather than duplicated, with the `allow_only_expected_pairs` and `lidar-pure` cases kept as two clearly-separate code paths (not silently branching inside one function, unlike today's `crossing_allowed_partner_set`) since they represent two different intents, not two configurations of the same intent.

### Milestone 2: consolidate the three `collision_partner_ids` decision sites

Once Milestone 1's shared helper exists, migrate `route_single_net_and_commit_native`, `route_single_net_and_commit_repair_native`, and the third site inside `route_many_with_repair_and_commit` to call it, resolving whatever the Milestone 0 divergence audit found (either by making all three sites intentionally consistent, or by keeping a confirmed-intentional difference but naming and documenting *why*, rather than leaving it as silent, undocumented drift).

### Milestone 3: broaden test coverage

Add or extend Rust unit tests (in `src/py_router.rs`'s own `#[cfg(test)] mod tests`, matching this repository's existing convention) that pin down the consolidated partner-discovery behavior directly -- both modes, both the pre-search (states-only) and post-search (route-cells) variants, and the previously-divergent two-stage-vs-single-stage behavior's resolved, intentional form. This closes the same kind of gap the endpoint-correction restructuring plan closed for its own subject: relying only on full-benchmark runs to catch a regression in this area, rather than having a fast, focused unit test for the specific behavior.

### Milestone 4: broad validation

Full validation ladder: `cargo test --lib`, `PYTHONPATH=. .venv/bin/pytest -q`, `benes_4x4`, `multiportmmi_8x8` under both bare CLI defaults and its documented stable-baseline config, `multiportmmi_16x16` under its documented stable-baseline config. Given this session's own experience validating `.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md` (a fix in this exact area that looked narrow but had real, benchmark-visible consequences on both `multiportmmi_8x8` and `multiportmmi_16x16`), do not skip either multiportmmi benchmark or assume a change here is benchmark-neutral without checking directly.

## Concrete Steps

All commands run from the repository root, `/home/benjamin/Documents/Repositories/working/TUMPhotonicRouter`, using the required toolchain override for any Rust build:

    RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu PYO3_PYTHON="$PWD/.venv/bin/python" cargo test --lib 2>&1 | tail -10
    RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu PYO3_PYTHON="$PWD/.venv/bin/python" .venv/bin/python -m maturin develop --release
    PYTHONPATH=. .venv/bin/pytest -q

`benes_4x4` end to end (small, fast; **do not** use `--debug-svgs` unless the repository owner has asked for it in the moment -- this session's owner explicitly flagged unwanted SVG generation partway through, so default to omitting the flag even though this benchmark is small enough that it was previously described as "safe"):

    rm -rf build/routes build/verification
    PYTHONPATH=. .venv/bin/python routing_flow.py benes_4x4

`multiportmmi_8x8` under bare CLI defaults and its documented stable-baseline config, and `multiportmmi_16x16` under its documented stable-baseline config: see `.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md`'s own Concrete Steps for the exact commands (identical flags apply here). Read every verdict from `build/verification/*.json` directly, per `.agent/WORKFLOW.md`'s Routing Verification Gate.

For isolated bisection/comparison against a specific prior commit without disturbing the main checkout, use a `git worktree` (this session used this successfully for exactly this purpose while validating the zero-event-acceptance fix) rather than checking out files in place in the main working tree.

## Validation and Acceptance

To be defined precisely once Milestone 0 completes and Milestone 1's target shape is locked in. At minimum: every partner-lookup function this plan touches has a clear, single, documented purpose (no function silently serving two different intents based on a mode flag); the three `collision_partner_ids` decision sites either agree or have a documented, intentional reason not to; `cargo test --lib` and the full validation ladder (Milestone 4) show no regressions; and the specific bug class this plan grew out of (a shared acceptance-criteria function called with inconsistent, undocumented partner sets from different callers) has real unit-test coverage pinning down the now-consolidated behavior.

## Idempotence and Recovery

Milestone 0 is read-only (no code changes) and safe to redo or interrupt at any point. Milestones 1-2 change real Rust internals and should each land as one focused, reviewable commit per this repository's established practice, not split into a partially-consistent intermediate state. Do not run `--debug-svgs` with no value on `multiportmmi_8x8` or `multiportmmi_16x16` at any point in this plan's execution, and default to omitting `--debug-svgs` entirely unless explicitly asked for in the moment.

## Artifacts and Notes

The concrete evidence trail for the bug this plan generalizes from is in `.agent/execplans/2026-08-19-fix-collision-crossing-zero-event-acceptance.md`'s own Surprises & Discoveries; it is summarized, not repeated, here.

## Interfaces and Dependencies

All in-scope functions are internal to `src/py_router.rs` and not part of the `#[pymethods]` PyO3-exposed public surface, pending Milestone 0's explicit confirmation of that claim (see Milestone 0, step 4). No Python-side interface changes are expected. Depends on nothing else being in flight in this same file concurrently -- confirm `git status` is clean and no other plan is mid-edit in `src/py_router.rs` before starting Milestone 1's actual changes.
