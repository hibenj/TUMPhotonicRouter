# Analyze and repair the forced-90-degree route degradation

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This document must be maintained in accordance with `.agent/PLANS.md`.

## Purpose / Big Picture

The repository owner's prioritized finding (2026-09-01, with GDS screenshots): large parts of the multiportmmi layouts route as 90-degree Manhattan geometry where 45-degree diagonals are obviously better -- nested rectangular detours around the dense fan-in groups (32x32, and visibly also 16x16), and even the FIRST-layer fanout's upper routes, which commit early into empty space. "Die 90 grad routen verschlechtern die route qualitaet. das wuerde ich analysieren und reparieren." The reference router (LiDAR, ~/Documents/Repositories/working/LiDAR) routes the identical multiportmmi_32x32 input clean with wide diagonal fans (1257 s, DRV 0, 50 crossings) under the SAME perpendicular-crossing rule (verified in its `drc/drcmanager.py`: `(check_ori + nType) % 180 == 0`), so the degradation is our engine's behavior, not a law of the topology. LiDAR is the reference for detecting problems -- explicitly NOT a template to copy (owner direction).

After this plan, routes are diagonal wherever geometry allows, 90-degree only where genuinely necessary; measurably: the Manhattan-excess of multiportmmi_16x16 drops, the first-layer fanout in the owner's screenshot routes as symmetric diagonals, and `multiportmmi_32x32` progresses past its current `n_108` collapse (net 109/447) -- ideally to clean completion, closing the "32x32 under lidar-pure is a baseline must" goal.

## Progress

- [x] (2026-09-01) Plan created from the day's evidence (below). SVG artifacts of the mm32 failure state exist (`scratchpad/svg_mm32/` of session afcc7efa: n_108 attempt174 probe / attempt176 repair_failed_net / FAILED.txt).
- [x] (2026-09-01) Milestone 0, first pass (coarse, from the existing `--verbose-routes` log of multiportmmi_16x16; shape proxy = fractional length remainder, sqrt2 => contains diagonals): engine x shape over all 223 nets: **simple 57 ORTH / 65 DIAG, astar 19 ORTH / 82 DIAG.** The simple fast path produces 57 of the 76 orthogonal routes and commits them with ZERO bend penalty (cost == length on every simple net -- the catalog, not the cost model, decides the shape). Pattern in the first-layer fanout tree: outer edges route DIAG, inner edges ORTH (e.g. n_3/n_4 at exactly 146.00), and the entire 14-net heater feeder series is simple-ORTH at exactly 252.00. Also one asymmetry case: n_6 routes astar with a 48-unit bend penalty and 52 um longer than its mirror branch. Remaining for the fine pass: exact bend counts from centerline records and per-net octile lower bounds; classify the owner's screenshot nets pixel-precisely.
- [ ] Milestone 0 (attribution, fine pass -- original scope): for every routed net of `multiportmmi_16x16`, join (a) which engine path produced it (simple fast path / plain A* / lidar_pure_probe_commit / repair buckets / orthogonal fallback) with (b) a shape metric (bend-penalty per um from cost-length; better: 90-vs-45 bend counts from the realized centerline records). Existing sources: `--verbose-routes` per-net lines carry engine + length + cost (yesterday's `rb_multiportmmi_16x16.log` already has them); centerline-based bend counting needs the record-capture pattern from `.agent/execplans/2026-08-31-...` (CLI-vs-run_routing_flow caveat: only shape statistics, not failure points, may come from direct calls). Deliverable: a table "engine path x shape quality" naming the dominant 90-degree sources, and the specific nets of the owner's first-layer screenshot classified.
- [x] (2026-09-01) Milestone 1, Fix #1 LANDED: the orthogonal repair fallback is disabled by default (`orthogonal_repair_fallback_enabled()`, re-enable via `PHOTONIC_ROUTER_ENABLE_ORTHOGONAL_REPAIR_FALLBACK`). Fine attribution had convicted it beyond doubt: the 14 repair-path nets of multiportmmi_16x16 carried 46 90-degree bends, ZERO 45s, octile excess 1.20-1.31; with the fallback off the same paths measure excess 1.03/1.07 with 16/8 45-degree bends and 3->0 / 2->1 staircases, and the "safety net" case never fired -- the diagonal-capable chain converts every time (more attempts, same outcome). Validation: full ladder clean (mm8x8 23.5 s, mm16 105.4 s, benes8 36.2 s, benes16 218.5 s, heater 8.9 s -- all verifications 0/0), pytest byte-identical 10-failure baseline after re-pinning the fanin-boundary counts to attempts=51/failures=14 (derivation in the test comment), **owner visual sign-off on multiportmmi_16x16 ("sieht jetzt gut aus")**, and **multiportmmi_32x32 progresses from failing at net 109 to grinding at net 156 of 447** (nets through 155 route briskly, 0.7 s each near the end; net 156 -- a long-diagonal into the next dense group -- is the new front, 60-min timeout).
- [x] (2026-09-01) Net-156 analysis (the new multiportmmi_32x32 front after Fix #1): its diag shows ZERO repair activity -- the net sits in a single plain A* search that ran 40+ minutes to the timeout. Source (1461,1467) -> target (1846,893), a 385x574-cell span into the second dense multiport column. The owner traced an obvious direct path in the GDS (green sketch) crossing ~8-12 horizontal feeders; at the search loss of 200 um-equivalent per crossing that path carries 1600-2400 um of penalty on ~1000 um of physical length, so admissible A* must exhaust every detour up to that penalty band first -- effectively the whole window, hence the state explosion. This is a SEARCH-TIME cost-model problem, distinct from the (result-insensitive) E4 finding. LiDAR contrast: no per-crossing g-price; a per-node escalating crossing budget (try 0 crossings, then allow up to 10), i.e. iterative deepening over crossing COUNT -- which is why it shows no explosion on the same input.
- Decision (owner, 2026-09-01): try OPTION 1 first -- lower the crossing search loss (A/B at 50 on net 156 in flight; if it rescues the search, measure the crossing-count trade across the green ladder at 50 vs 200 before touching the default `DEFAULT_COLLISION_CROSSING_SEARCH_LOSS_UM=200` in `translation/route_rust_crossing_plan.py`). FALL BACK to OPTION 2 -- LiDAR-style budget escalation over crossing count (a generic mechanism, not topology knowledge, hence baseline-legitimate) -- if option 1 fails the search or inflates crossings unacceptably.
- (2026-09-01) **Option-1 A/B RESULT: the simple price thesis is REFUTED.** With SEARCH_LOSS=50 (a ~500 um penalty band for the ~10-crossing direct path) net 156 still never returns (30-min timeout; nets 153-155 route in <1 s each). The blocker is not primarily the crossing price. Leading replacement hypothesis: the direct path may not be LEGAL at all -- each crossing needs its min-straight window and spacing between the tightly-pitched feeders; if the room is missing, admissible search runs an unbounded impossibility proof across growing window retries. Diagnosis run started: same net with PHOTONIC_ROUTER_ASTAR_TIMEOUT_MS=60000 so each search attempt fails after 60 s and the attempt structure (windows, expansions, error text, FAILED.txt) becomes visible (logs scratchpad/mm32_diag156*.log). Option 2 (budget escalation) stays queued but must wait for this diagnosis. Two co-equal hypotheses (owner, 2026-09-01) the diagnosis must distinguish -- the owner confirms the corridor is geometrically wide open ("da ist unfassbar viel platz und ein route ist zu 100% moeglich"), and a committed vertical net already crosses the SAME feeders legally:
  (H1) a legality/bookkeeping rule wrongly eats the free room (candidates: the crossing reservation window's no-third-net requirement -- the existing vertical crosser sits near every plausible new crossing site --, oversized keepouts/halos, stale reservations). Discriminator: the timeout-run's attempts fail fast with "no legal ... crossing route" and the crossing trace shows candidates rejected en masse with window/legality reasons.
  (H2) sheer crossing-state explosion: each potential crossing multiplies search states (extension bookkeeping: crossed_mask, pending windows, straight-run counters), so with ~10+ crossable partners the state space blows up and the search simply never reaches the target. Discriminator: expansions hit the ceiling with FEW rejections and many accepted-candidate states; per-window attempts time out rather than fail with legality errors.
  Diagnosis attempt 1 RESULT (2026-09-01): the PHOTONIC_ROUTER_ASTAR_TIMEOUT_MS=60000 run ALSO hit the outer 40-min timeout at net 156 with no FAILED.txt and no attempt SVGs -- the per-search wall clock either does not reach the native batch's searches for this net, or the attempt cascade (window expansions x strategies) stacks many 60-s slices beyond 40 min. Either way the structured-failure route to a diagnosis is closed for now; treat "does ASTAR_TIMEOUT_MS actually bind in route_many_with_repair_and_commit's searches?" as an open tooling question (env read is at py_router.rs ~2539 into astar_cfg -- verify it reaches the batch's per-search configs).
  NEXT CONCRETE STEP for the successor session: run net 156 with the LIVE crossing trace instead -- `PHOTONIC_ROUTER_TRACE_CROSSING_NET=156` (plus TRACE_CROSSING_CANDIDATES / TRACE_CROSSING_PENDING as needed, see the heuristics inventory) streams candidate-level accept/reject reasons to stderr unbuffered DURING the search, so H1 (mass legality rejections: reservation-window/not_perpendicular/min_straight) vs H2 (few rejections, state count explosion) is decidable without the call ever returning. Command shape: the mm32 stable invocation with --debug-stop-after-route 157 and a ~10-min outer timeout, stderr to a file, then classify the rejection histogram.
- IN FLIGHT at plan handoff (2026-09-01, session afcc7efa): (a) superseded, see the option-1 A/B result above; (b) benes_32x32 FIRST run with Fix #1, RESULT: 60-min timeout at net 59 of 320. The old net-57/58 cliff falls (and much faster than pre-fix, which needed 90 min just to reach 59), but net 59 grinds in the same pending-straight repair signature (victim=57, counts ~7450). Moderate progress only -- benes's interstage layers add the mandatory-crossing dimension, so the same H1/H2 diagnosis below applies to it; re-probe after the net-156 diagnosis lands a fix. Logs scratchpad/b32_fix*.log. Artifacts for the owner: `build/routed_multiportmmi_32x32_partial_net155.gds` (155 nets, the state the owner reviewed), `build/routed_benes_32x32_partial_net56.gds`.
- Known tooling gap (still open): per-net attempt SVGs are only exported when the native routing call RETURNS; a net grinding inside repair produces none (benes net-57 SVG attempts failed twice, once even with PHOTONIC_ROUTER_ASTAR_TIMEOUT_MS=30000, which does not reach the repair-internal searches). If failure-state SVGs are needed for grinding nets, export must be added inside the native repair loop.
- [ ] Milestone 1 (continued): remaining sources, in evidence order. Known suspects, each with its mechanism already located:
  1. **Orthogonal repair fallback** (`orthogonal_repair_primitives`, `src/py_router.rs:7147` -- a primitive set with ALL diagonals removed), triggered via `prefer_orthogonal_local_retry = !probe.probe_realized_crossing_violations.is_empty()` (py_router.rs ~8917) and in the victim-reroute path. Self-reinforcing: every orthogonal commit adds axis-aligned material that, under the perpendicularity rule, blocks the next net's diagonals, whose probe then also fails -> Manhattan cascade. Options for the owner (repair-strategy changes are decision-gated): remove the fallback (escalate via victims/keepout variants instead); keep it but mark orthogonal commits as preferred ripup victims; retry the 45-capable search with relaxed keepout before falling back.
  2. **Simple-route fast path** (`enable_simple_routes`, 122/223 mm16 nets route as `simple`): if its candidate shapes are orthogonal-dominant, it explains systematic early 90-degree commits in EMPTY regions (the owner's first-layer case, where the fallback cannot be the cause). Check `src/simple_routes.rs` candidate shapes; if orthogonal-only, add diagonal candidates or defer to A* when a diagonal would be shorter.
  3. **Keepout overreach** in the probe-retry (the localized keepout can sever exactly the diagonal corridor, leaving only orthogonal legality).
- [ ] Milestone 2 (acceptance): shape metric improves on multiportmmi_16x16 with unchanged clean verifications; the first-layer fanout screenshot region routes diagonally; `multiportmmi_32x32` re-run -- target: past n_108, ideally clean; full ladder + pytest baselines hold. benes_32x32 re-probed after (same cascade class suspected).

## Surprises & Discoveries

- (2026-09-01) The long-documented "direct `run_routing_flow(...)` fails at a different net than the real CLI under nominally identical flags" mystery (2026-08-26 target-side-stubs plan, Decision Log) is explained: `main()` applies the benchmark's `STABLE_ROUTING_ENV` via `os.environ.setdefault` BEFORE routing; a direct `run_routing_flow` call skips that, so e.g. `PHOTONIC_ROUTER_LONG_STRAIGHT_CONGESTION_WEIGHT=0.05` and `FANOUT_STUB_BEND_DEGREES=90` are silently absent and multiportmmi_16x16 then fails at n_113 (observed in this plan's attribution harness; CONFIRMED: with the two env vars set, the direct call routes the benchmark cleanly and matches the CLI). Scripts that call `run_routing_flow` directly must set the benchmark's STABLE_ROUTING_ENV themselves.

- (Carried from 2026-08-31/09-01 analysis) The perpendicularity rule does NOT forbid diagonals per se (owner's correction: diagonal x diagonal crossings are legal and occur); it forbids mixed-angle pairs. Manhattan geometry emerges where the crossed material is axis-aligned -- and our own mechanisms (orthogonal fallback commits, port-adjacent axis segments, possibly simple routes) keep producing exactly that material.
- LiDAR comparison facts (reference only): same perpendicularity rule; its 32x32 GDS has only 3.27 um^2 total waveguide overlap (clean by our overlap standard) and no tight bundling (densest slice: 49.25 um edge gaps); it uses a per-node escalating crossing budget instead of our 200 um search loss, and 10 ripup rounds with history.

## Decision Log

- Decision (owner, 2026-09-01): this problem is the current priority; analyze then repair; LiDAR serves for problem detection, our fixes must fit our architecture ("wir sollten nicht unsere version umschreiben um genau wie lidar zu sein").

## Outcomes & Retrospective

Not started.

## Context and Orientation

Terms: the *simple-route fast path* is a pre-A* shortcut (`enable_simple_routes`, `src/simple_routes.rs`) that commits common easy shapes without a full search. The *probe/lidar_pure_probe_commit* path routes with dynamic obstacles ignored and commits after realized-crossing validation. The *orthogonal fallback* is described in Milestone 1.1. The perpendicularity rule lives in the crossing legality (`not_perpendicular` rejections). Shape metrics can be computed from per-net `--verbose-routes` lines (cost minus length = bend-penalty units, 12 per 45-degree eighth-pair, 24 per 90-degree bend) or exactly from captured `corrected_centerline_um` records (count 45-vs-90 vertex classes as in the 2026-08-31 heater analysis).

## Plan of Work / Concrete Steps / Validation and Acceptance

As in Progress; every landed fix runs the standard ladder (multiportmmi_8x8/16x16, benes_8x8/16x16, heater_s_mod, pytest 10-failure baseline) plus the mm32x32 progression check. Repair-strategy changes are presented as options, not landed unilaterally.

## Idempotence and Recovery

Source edits only; `git checkout` restores. All measurement artifacts live in the session scratchpad and are re-creatable from the commands recorded here and in the repository state.
