# Restructure port endpoint correction into one readable, well-tested stage

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This document must be maintained in accordance with `.agent/PLANS.md`, which is checked into this repository at that path and describes the required structure and editing discipline for ExecPlans in general.

## Purpose / Big Picture

This repository (`TUMPhotonicRouter`) routes optical waveguides on a discrete grid, then "corrects" the very start and end of each route so its centerline lands exactly on the real, physical port it connects to (a port's physical center is an arbitrary micrometer coordinate set by the chip layout, not something constrained to a router grid point, so a small correction at each end is normal, not a sign of a bug). Today, "correct this net's endpoints" is not one thing: it is three separate top-level passes over the routed nets (`_apply_checked_endpoint_corrections_for_net_ids`, `_apply_checked_fanout_stub_endpoint_corrections_for_net_ids`, `_apply_crossing_aware_endpoint_corrections_for_net_ids`, all in `translation/route_rust.py`), run one after another, each deciding independently which nets it is and is not responsible for, with duplicated logic between them and, in the crossing-aware path specifically, a silent fallback chain that can produce a wrong-but-plausible-looking answer instead of a clearly-flagged failure -- this is exactly what happened in a real bug fixed the day before this plan was written (`.agent/execplans/2026-08-18-crossing-aware-endpoint-correction-direction-sequence.md`), where a valid correction was silently discarded and the code fell back, twice, to a worse answer that missed the real port by `2.0014` micrometers before a downstream verification check finally caught it.

After this plan is complete, there will be exactly one place a reader goes to understand "how does this net's endpoints get corrected, and why": one entry point, called once per routing run, that reads top-to-bottom as a short, obvious sequence (classify each net's situation, then apply the one correction strategy that situation calls for), backed by focused unit tests for each distinct situation rather than relying only on full-benchmark verification to catch a regression. A person can see this working two ways: (1) reading the new orchestration code and being able to answer "which of the three (now unified) treatments does net X get, and why" without tracing through duplicated boilerplate in three different functions; (2) running the exact regression scenario from the `2026-08-18` bug fix as a new, fast, focused test (no full benchmark needed) and seeing it fail on the old code shape and pass on the new one.

## Progress

- [x] (2026-08-19) Milestone 0, part (a): re-read all three passes fresh (`translation/route_rust.py`, `_apply_checked_endpoint_corrections_for_net_ids` line 4485, `_apply_checked_fanout_stub_endpoint_corrections_for_net_ids` line 4651, `_apply_crossing_aware_endpoint_corrections_for_net_ids` line 5109, all called in that order from `_RouteNetsRustSession.run()` lines 6923-6947 over `list(self.route_bookkeeping.route_order)`, i.e. every routed net id) and pinned down the exact classification table, including the fanout-stub-both-sides answer Milestone 0 asked for. See Surprises & Discoveries for the full table and the confirmed answer. Not yet backed by a runtime characterization test -- that is part (a)'s remaining work, tracked below along with part (b) (the `2026-08-18` regression test) and the `except Exception: crossing_net_ids = set()` history check.
- [x] (2026-08-19) Milestone 0, confirmed via `git log -S`/`git blame` that the `except Exception: crossing_net_ids = set()` swallow at `translation/route_rust.py` lines 4525-4526 and 4693-4694 was introduced verbatim by the Phase 2 closures-to-methods restructuring commit (`9925249`, 2026-08-18) with no prior or later touch, and no test, log, or diagnostic anywhere in the repository references it ever having fired. No evidence found that it has ever legitimately caught anything real. Per the plan's own Milestone 0 instruction, this is now the basis for Milestone 2 removing the swallow (let a real error propagate) rather than preserving or hardening it.
- [x] (2026-08-19) Milestone 0 complete. Both remaining test deliverables landed via a delegated implementation pass (see Decision Log), reviewed and independently re-run by the orchestrator: `tests/test_port_alignment_diagnostics.py::test_compatible_terminal_direction_sequence_accepts_extra_segment_at_far_end` (direct unit test of `_compatible_terminal_direction_sequence`, pins the `20aab29` fix at the unit level with both an accept and a discriminating reject case) and `tests/test_route_rust_opened_cells.py::test_apply_checked_fanout_stub_endpoint_corrections_skips_both_sides_fanout_net` (proves the both-fanout-stub-sides guard at `translation/route_rust.py:4705` fires via spies that fail the test if control flow reaches port resolution). The implementer additionally investigated, and confirmed via direct code trace, that a genuine end-to-end both-fanout-stub-sides net cannot currently be built from a real layout at all (`_build_static_fanout_anchors` only ever reads `source_port_specs_by_instance`, so the only way to get a fanout anchor on a net's target side is to reuse that exact port as a different net's source, which the router's own dynamic occupancy model rejects as a routing conflict) -- worth remembering for Milestone 1: this category is real in the *code's guard-clause logic* but may be unreachable in practice today, which affects how much test/behavior weight it deserves versus just being named for completeness. Both new tests independently re-run and pass; the delegated agent stalled during its own final full-suite validation step (a stream/watchdog issue unrelated to the test content -- its diff was already complete and correct at that point), so the orchestrator ran that validation directly instead: `PYTHONPATH=. .venv/bin/pytest -q tests/test_port_alignment_diagnostics.py tests/test_route_rust_opened_cells.py` shows the same 2 pre-existing failures with and without today's changes (confirmed via `git stash`/`git stash pop`, not assumed) -- `test_checked_no_bump_endpoint_correction_rejects_middle_static_contact` and `test_route_nets_rust_static_stub_fanout_uses_virtual_source_anchor`, both pre-existing and unrelated to this plan. Full suite: `21 failed, 318 passed, 1 skipped` (previously documented baseline was `23 failed, 314 passed, 1 skipped`; the 2-test improvement beyond the 2 new tests added is not yet explained -- likely minor drift/flakiness in the existing baseline rather than anything today's changes caused, since today's changes are purely additive new test functions, but flagged here rather than silently assumed).
- [x] (2026-08-19) Milestone 0.5, first half of "close the gap between very-likely-the-cause and confirmed-the-cause": reproduced `n_196`/`n_197` `cross_net_waveguide_overlap` on `multiportmmi_16x16` under its documented stable-baseline config (identical evidence to the prior session: `error_count=1`, `overlap_area_um2=50.60074900000001`, `overlap_bbox_um=[5865.656, 1470.927, 5982.176, 1587.447]`, read directly from `build/verification/multiportmmi_16x16_photonic_verification.json`). Mapped `n_196`/`n_197` to their integer net ids by reading `benchmarks/multiportmmi_16x16.build_schematic()().netlist.routes` dict order directly (`n_196` is dict position 196, `n_197` is dict position 197; `translation/route_rust.py` line 6322-6344 assigns `net_id = next_net_id` starting at 1 and incrementing once per `(net_name, links)` pair in that same dict-iteration order with no reordering, confirmed by reading the loop, so `n_196` -> `net_id=197`, `n_197` -> `net_id=198`). Re-running with the repository's pre-existing `PHOTONIC_ROUTER_TRACE_ENDPOINT_BUMP_NETS=197,198` env var (a tracing hook already present at `src/py_router.rs` lines 7335-7343/7064-7069, unrelated to this plan, built for exactly this kind of investigation) to identify which correction strategy actually ran is in progress -- see next entry once it completes.
- [x] (2026-08-19) Milestone 0.5, second half: identified the exact mechanism, which refines (and partially supersedes) the plan's earlier framing -- see the new Surprises & Discoveries entry below for the full trace. Short version: `n_196`/`n_197` have zero crossing events (confirmed via `build/verification/multiportmmi_16x16_crossing_verification.json`'s `crossings` list, 67 entries, none naming either net), so pass 3 (crossing-aware) never touches them; they go through pass 1/2's "checked" corrector, `route_port_corrected_centerline_checked_and_commit_native` (`src/py_router.rs:6852`). That function is not uniformly obstacle-blind: when `full_straight_offset_bump_candidates_rs` returns real candidates, each is validated strictly (`commit_route_with_clearance_overlap`, `src/obstacle_map.rs:397`, no exceptions) -- confirmed safe. But when it returns no candidates (confirmed via the `PHOTONIC_ROUTER_TRACE_ENDPOINT_BUMP_NETS=197,198` trace producing zero `endpoint_bump_trace` lines for either net, meaning neither reached that loop), it falls back to the unrestricted `route_to_port_corrected_centerline_with_options` and then, at lines 6946-6963, scans the resulting candidate's own core cells for any other net's dynamic ownership and unconditionally adds every such net to an `allowed_overlap_nets` set passed into `commit_route_with_clearance_and_allowed_core_overlap_cells` (`src/obstacle_map.rs:436`) -- which is otherwise a correctly-scoped, general-purpose mechanism (its own doc comment: "used as a first crossing-aware baseline: search may temporarily open expected partner routes, and commit validates that any resulting dynamic core overlap belongs only to those expected partners"), but this specific caller populates the allow-list from whatever already occupies the candidate's own cells rather than from any independently-known legitimate partner, so it self-authorizes exactly the collision the check exists to catch. The fix location is therefore narrower and more surgical than the plan's original "thread obstacle-awareness into the geometry-construction strategies" framing: the strategies genuinely have no obstacle awareness during construction (still true, still worth noting), but the caller-side check that runs immediately after construction is not absent, it is neutralized by this self-authorizing allow-list. Reported to the repository owner mid-session as a course correction from the plan's original text before implementing.
- [x] (2026-08-19) Milestone 0.5, fix implemented: `src/py_router.rs`'s `route_port_corrected_centerline_checked_and_commit_native`, `candidates.is_empty()` branch (around line 6946 pre-fix), no longer builds `allowed_overlap_nets`/`allowed_overlap_core_keys` by scanning the corrected candidate's own cells for whoever already occupies them. Confirmed by reading every caller (`route_port_corrected_centerline_checked_native` at line 7291, used only by `apply_checked_endpoint_corrections`, which Python's `_apply_checked_endpoint_corrections_for_net_ids`/`_apply_checked_fanout_stub_endpoint_corrections_for_net_ids` both call only after excluding any net_id in `crossing_net_ids`) that this branch never legitimately needs an expected-partner allowance, so the fix switches both the `commit_to_router` and check-only calls from `commit_route_with_clearance_and_allowed_core_overlap_cells` to the strict `commit_route_with_clearance_overlap` (no allowed overlap at all) -- the same strict function the case-4 bump candidate loop already used safely. `cargo check --lib` clean, no unused-import warnings. Rebuilt via `maturin develop --release`. Validation: `cargo test --lib` unchanged at `320 passed, 1 failed` (the one pre-existing, deliberately-untouched `collision_crossing_route_without_event_is_not_accepted` bug, unrelated); full Python suite unchanged at `21 failed, 318 passed, 1 skipped`, confirmed identical failure set before/after via `git stash`-and-rebuild on `src/py_router.rs` alone and a sorted `diff` of both `FAILED` line lists (empty diff). Added a new focused regression test, `tests/test_port_alignment_diagnostics.py::test_checked_no_bump_endpoint_correction_rejects_other_net_dynamic_overlap`: a diagonal net (net 7, matching the existing `_checked_no_bump_diagonal_test_router_and_route` fixture's geometry) whose corrected centerline passes through grid cell (5, 5), with a different net (8) pre-occupying exactly that cell via a route that is never committed for net 7 first (so the only occupant is genuinely the other net, not net 7's own prior geometry) -- confirmed this test fails with "ACCEPTED (bug present)" against the pre-fix code (verified directly via `git stash push -- src/py_router.rs`, rebuild, rerun, `git stash pop`, rebuild again -- not assumed) and passes (raises `RuntimeError` matching `dynamic_overlap`) against the fix.
- Observation (2026-08-19, Verifier Veto triggered): re-running `multiportmmi_16x16` after the `src/py_router.rs` fix alone still showed `success: false` with the *exact same* `cross_net_waveguide_overlap` numbers for `n_196`/`n_197` (`area=50.60074900000001`, byte-identical bbox) -- plus two new `missing_corrected_centerline` errors (`n_196`, `n_203`), confirming the Rust fix genuinely now rejects the unsafe correction rather than silently accepting it. Per `.agent/WORKFLOW.md`'s Verifier Veto rule, treated the previous fix as insufficient rather than declaring success, and traced why the identical overlap persisted instead of guessing. Root cause: a *third*, previously-unexamined call site. `translation/route_rust_realization.py:_physical_port_centerline` (lines 37-101), used by geometry realization for the actual GDS/verification polygons (not the endpoint-correction pipeline stage), falls back to calling the plain, unrestricted `router.route_port_corrected_centerline(..., allow_unchecked_bumps=True)` -- the same construction-with-zero-obstacle-awareness function, confirmed by this plan's earlier Surprises & Discoveries entry -- whenever `record.corrected_centerline_um` is empty *and* `record.endpoint_correction_error is None`. That guard never fired for `n_196` because `translation/route_rust.py`'s `_apply_checked_endpoint_corrections_for_net_ids` (lines 4586-4606 pre-fix) only recorded `endpoint_correction_error` `if not self.enable_crossings` -- the *global* run flag, not whether this specific net has a crossing -- and `multiportmmi_16x16`'s stable-baseline config always runs with `--crossings true`. Confirmed this guard is unconditionally wrong, not merely too broad: every net_id reaching that point in the function was already excluded from the crossing set earlier in the same function (`if self.enable_crossings and int(net_id) in crossing_net_ids: continue`), so any net still present is guaranteed non-crossing regardless of the global flag, and there is no later pass that will still process it. Confirmed by contrast that `_apply_checked_fanout_stub_endpoint_corrections_for_net_ids`'s equivalent error-handling block has no such guard and records `endpoint_correction_error` unconditionally -- the asymmetry was the tell that pass 1's conditional was the outlier, not an intentional design choice. Fixed by removing the `if not self.enable_crossings:` guard in both of pass 1's two error-handling blocks (empty note: pass 1 has two near-duplicate error blocks per the plan's own earlier `crossing_net_ids` duplication finding), so the record now always gets `corrected_centerline_um=()` and a real `endpoint_correction_error` on rejection. This makes `_physical_port_centerline`'s existing guard fire correctly, which makes the net's realized region empty instead of silently unsafe, which surfaces as `translation/photonic_verification.py`'s existing `empty_route_geometry` issue code -- an honest, visible failure instead of a silent overlap, matching this plan's stated purpose (a wrong answer should be clearly flagged, not silently produced). Full pytest suite re-run after this second fix: identical failure set to before (confirmed via sorted `diff` of `FAILED` lines, empty diff, `21 failed, 318 passed, 1 skipped`).
- [x] (2026-08-19) Milestone 0.5 complete, stopped at the repository owner's explicit direction after presenting the tradeoff. Final re-run of `multiportmmi_16x16` under the stable-baseline config with both fixes, verdict read directly from `build/verification/*.json` per `.agent/WORKFLOW.md`'s Routing Verification Gate: **crossing verification is now fully clean** (`build/verification/multiportmmi_16x16_crossing_verification.json`: `status=complete, success=true, error_count=0, warning_count=0`) and, critically, **the `cross_net_waveguide_overlap` between `n_196` and `n_197` is completely gone** -- the original, real physical design-rule violation this milestone set out to fix is resolved. Photonic verification: `status=complete, success=false, error_count=6` (`build/verification/multiportmmi_16x16_photonic_verification.json`), all six issues concentrated on exactly the two nets whose checked correction now correctly rejects an unsafe candidate with no safe alternative to fall back to: `n_196` (`endpoint_correction_error`: "dynamic_overlap=58 owners=[198]" -- net_id 198 is `n_197`, confirming this is the exact same pair, now caught before commit instead of after realization; `missing_corrected_centerline`; `target_port_not_connected`) and `n_203` (same three issue codes, `owners=[203]`, a different, previously-unknown collision pair this same fix also caught). **Verdict: FAIL** for the full benchmark as a whole, but this is a categorically safer failure than before: a silent, physically-invalid overlap replaced by two honestly-flagged, individually-diagnosable missing connections, out of 223 nets. Presented this exact tradeoff to the repository owner -- extending the correction algorithm so these 2 nets can find a real collision-free placement (the `candidates.is_empty()` branch has no alternative candidates to try once its one deterministic construction collides, unlike the case-4 bump path) is real additional Rust algorithm scope beyond this milestone's original "stop self-authorizing" fix -- and the owner chose to stop here rather than expand scope further. Recorded as an explicit, open follow-up below rather than silently left implicit.
- Follow-up (not started, deliberately out of this milestone's now-closed scope): make `n_196` and `n_203` (and any other net with the same shape) actually route successfully instead of being honestly rejected. The `candidates.is_empty()` branch of `route_port_corrected_centerline_checked_and_commit_native` (`src/py_router.rs:6852`) falls back to a single deterministic construction (`route_to_port_corrected_centerline_with_options`'s strategy chain: straight/diagonal/shift/45-degree-delta/absorb-into-existing-axis-run) with no alternative candidates to try when that construction collides, unlike `full_straight_offset_bump_candidates_rs`'s multi-candidate case-4 bump path. Making this branch collision-*avoiding* (not just collision-*rejecting*) would need either extending it to produce and try alternative placements the way the bump path already does, or feeding obstacle awareness earlier into the construction itself so it avoids the collision rather than discovering it after the fact. Two known reproduction cases now exist (`n_196`×`n_197`, `n_203`×net_id-203) if this is picked up later.
- [x] (2026-08-19) Milestone 1 complete. First corrected the plan's own Context and Orientation section: pass 1 and pass 2 are each called from three places, not the one the plan originally described (see the new Context and Orientation note and the Surprises & Discoveries entry below) -- discovered while designing the classification function, since it needs to work correctly no matter which call site is asking. Added `translation/route_rust_types.py`'s `EndpointCorrectionCategory` (a new `enum.Enum` with five named members: `ALREADY_CORRECTED_NO_OP`, `UNRESTRICTED`, `FANOUT_STUB_SOURCE_ONLY`, `FANOUT_STUB_TARGET_ONLY`, `CROSSING_AWARE`) and `NetEndpointCorrectionClassification` (a frozen dataclass carrying the category plus the raw `has_crossing`/`source_has_fanout_stub`/`target_has_fanout_stub` booleans), matching this repository's existing `@dataclass(frozen=True)` convention in that same shared-types module (no prior `enum.Enum` usage precedent existed in this codebase; chose it anyway as the standard, explicit-contract idiom for a small closed category set, over inventing a string-literal convention). Added two new methods on `_RouteNetsRustSession` in `translation/route_rust.py`: `_endpoint_correction_crossing_net_ids()` (the crossing-event-derived set, now computed once and shared, replacing the identical logic previously duplicated verbatim in both pass 1 and pass 2) and `_classify_net_for_endpoint_correction(net_id, *, crossing_net_ids)` (mirrors every existing per-net guard condition exactly -- including the previously-unnamed edge case where a fanout-anchor net without an existing `corrected_centerline_um` falls through to `UNRESTRICTED` rather than being deferred, preserved deliberately, not treated as a bug, per this milestone's additive framing). Rewired pass 1 and pass 2's per-net loops to filter on the shared classification's `category` instead of their own inline conditionals; deliberately left pass 3 (crossing-aware) untouched, since it uses a second, independently-derived crossing-net-id source (`_current_crossing_points_by_net_id()`, carrying actual points, not just event membership) that Milestone 1's additive scope does not unify with the first (flagged as an open question in Surprises & Discoveries, not resolved here). Added a new direct unit test, `tests/test_route_rust_opened_cells.py::test_classify_net_for_endpoint_correction_covers_every_category`, exercising all five categories plus the missing-record (`None`) case and the not-yet-precorrected edge case, via the same minimal-session-construction pattern Milestone 0's characterization test already established. Validated: full `pytest -q` shows the identical `21 failed` set before and after (confirmed via sorted `diff` of `FAILED` lines, empty diff; `319 passed` now, one more than the `318` baseline from the new test), and `benes_4x4` end to end shows **Verdict: PASS** (`build/verification/benes_4x4_{crossing,photonic}_verification.json` both `status=complete, success=true, error_count=0`, read directly).
- [x] (2026-08-19) Milestone 2 complete, both sub-tasks. (a) Removed the `except Exception: crossing_net_ids = set()` swallow from `_endpoint_correction_crossing_net_ids` (now the single, Milestone-1-consolidated place this logic lives, so there was only one site to fix instead of the original two) -- a real failure in `router.crossing_events()` now propagates instead of being silently treated as "no crossings." (b) Made the crossing-aware path's fallback chain visible instead of silent: `_spliced_crossing_endpoint_centerline` (`translation/route_rust_endpoint_correction.py`) now accepts an optional `fallback_notes` list it appends a short, specific note to whenever the source-prefix or target-suffix region has to fall back from the rich correction to the absorbed-terminal solver, or (weaker still) all the way to the raw uncorrected baseline; `_apply_crossing_aware_endpoint_correction_to_record` threads this through its `_candidate`/`candidate_modes` retry loop and sets a new record field, `RoutedNetRecord.endpoint_correction_fallback_note` (`translation/route_rust_types.py`), on the winning candidate. Deliberately a *separate* field from `endpoint_correction_error`, not a reuse of it: a fallback can still produce a valid, in-tolerance route, so it must not flip the same signal `photonic_verification.py` and downstream tooling already treat as a hard failure. Scoping note, a deliberate deviation from the plan's original literal text: the plan asked for this to land "at minimum in the diagnostics already written to `build/routes/*_diagnostics.txt`," but tracing `_write_route_diagnostics`/`_route_attempt_diagnostics` (`translation/route_rust.py`, lines 2930/3179) found they run per-route-attempt during the A* search loop, *before* the endpoint-correction pipeline stage exists at all -- there is no live per-net text file left to append to by the time a fallback could be known. Surfaced it instead through `translation/photonic_verification.py`'s existing structured issue mechanism instead, as a new `endpoint_correction_fallback_used` issue with `severity="warning"` (confirmed via `PhotonicVerificationIssue.error_count`/`warning_count`, lines 42/46, that a warning cannot affect the pass/fail verdict) -- this is the same `build/verification/*.json` evidence this repository's own `.agent/WORKFLOW.md` Routing Verification Gate already requires reading for every routing verdict, arguably a *better* fit than a per-net text file that a full pipeline run would need to separately go collect. Validated on a real, dense benchmark, not just fixtures: `multiportmmi_8x8` under its bare CLI defaults stays fully clean (`success=true, error_count=0`) but now reports `warning_count=2`, naming `n_42` and `n_108` as having used a fallback -- confirming both that real fallback usage exists in this repository's own benchmarks today (not just a hypothetical) and that surfacing it does not change the pass/fail verdict. `benes_4x4` unaffected (`warning_count=0`, no fallback needed there). Full `pytest -q`: identical `21 failed` set (sorted `diff`, empty). New/extended tests: `tests/test_port_alignment_diagnostics.py::test_crossing_aware_endpoint_correction_absorbs_both_split_terminal_sides` extended with assertions on the new field (this was already the one existing test whose whole point is exercising the absorbed-terminal fallback on both terminal regions).
- [x] (2026-08-19) Milestone 3 complete. Added two new `_RouteNetsRustSession` methods in `translation/route_rust.py`, right after pass 3's own definition: `_apply_unrestricted_and_fanout_stub_endpoint_corrections_for_net_ids` (pass 1 + pass 2 together -- safe to call with any unfiltered `net_ids` list, since both passes already self-filter by classification after Milestone 1) and `_apply_all_endpoint_corrections_for_net_ids` (the first, plus pass 3, for nets involved in a crossing). All three of the call sites found during Milestone 1 (`run()`'s final pass, and the two mid-repair call sites inside the rip-up/reroute repair methods) now make exactly one call each instead of two or three: the two mid-repair sites now call `_apply_unrestricted_and_fanout_stub_endpoint_corrections_for_net_ids` (replacing their identical two-call boilerplate with one, unchanged since they never called pass 3 to begin with), and `run()`'s final pass now calls `_apply_all_endpoint_corrections_for_net_ids` (replacing its three separate, independently-ordered calls with one). Scoping decision, recorded in both new methods' own docstrings so it is not mistaken for an oversight: this milestone does *not* attempt a full classify-once-then-bucket-and-dispatch restructuring (where net ids would be pre-sorted into five lists and each pass called with only its own bucket) -- doing that safely would require changing pass 1/pass 2's internal self-filtering contract to trust a pre-filtered caller instead, which would break the two mid-repair call sites' ability to call them directly and safely with an unfiltered net-id list. The two new methods are deliberately thin, correctness-preserving wrappers around the existing three passes, not a merge of their internals; the underlying Rust calls and correction algorithms did not change at all, matching this milestone's own stated scope. Validated: full `pytest -q` shows the identical `21 failed` set (sorted `diff`, empty); `benes_4x4` end to end **Verdict: PASS** (`error_count=0, warning_count=0` on both crossing and photonic verification); `multiportmmi_8x8` under bare CLI defaults **Verdict: PASS**, byte-identical to Milestone 2's own validation (`error_count=0, warning_count=2`, same two nets `n_42`/`n_108` naming the same fallback), confirming the mid-repair call-site consolidation changed nothing observable; `multiportmmi_8x8` under its documented stable-baseline crossing config (`--crossings true --crossing-mode lidar-pure --fanout-access-mode static-stubs --routing-window-scale 0.35 --foreign-port-keepout-cells 0`, plus `PHOTONIC_ROUTER_LONG_STRAIGHT_CONGESTION_WEIGHT=0.05`/`PHOTONIC_ROUTER_FANOUT_STUB_BEND_DEGREES=90`, exercising the mid-repair call sites and pass 2 far more heavily than the bare-defaults run) also **Verdict: PASS** (`error_count=0`, one new warning naming `n_43` as having used a fallback -- consistent with, not a regression from, the pre-session baseline recorded in `.agent/REPOSITORY_STATE.md` of this exact configuration being "fully clean": that baseline predates Milestone 2's warning mechanism, so it could only ever have reported zero *errors*, not zero *fallback usage*, which was always happening silently for this net).
- [ ] Milestones 4-5: not started.

## Surprises & Discoveries

- Observation: the actual scope here is bigger, and more architecturally justified, than "endpoint correction has 5 duplicate rules that should collapse into 1." Reading the real call sites (`translation/route_rust.py`, around line 6923-6947, where `_RouteNetsRustSession.run()` calls all three passes in sequence) shows the three passes exist because there are two genuinely independent, real constraints on a net's endpoints, not accidental duplication: (a) whether the net's route crosses another route (a net involved in a crossing cannot have its endpoint freely re-corrected by the "unrestricted" native corrector, because that corrector is free to move geometry anywhere along the route, including the region right around the crossing point that must stay fixed -- see the comment at `translation/route_rust.py` line 4707-4712 explaining exactly this for the fanout-stub case), and (b) whether the net's source or target is a "fanout stub" (a pre-corrected adapter used for dense multi-port fan-out, which must not be re-corrected on that side). These two axes combine into the three passes: plain nets get the unrestricted corrector directly (`_apply_checked_endpoint_corrections_for_net_ids`); fanout-stub nets get a partial version of the same unrestricted corrector, skipping the already-corrected side (`_apply_checked_fanout_stub_endpoint_corrections_for_net_ids`); and any net involved in a crossing, regardless of fanout-stub status, is excluded from both of those and handled entirely by the separate, restricted, splice-based mechanism (`_apply_crossing_aware_endpoint_corrections_for_net_ids`, backed by `translation/route_rust_endpoint_correction.py`). This does not mean the current code is fine as-is -- see the next two observations -- but it does mean the restructuring must preserve this real, two-axis classification, not naively merge everything into one code path that ignores it.
- Observation: the two passes that call the "unrestricted" native corrector (`_apply_checked_endpoint_corrections_for_net_ids` and `_apply_checked_fanout_stub_endpoint_corrections_for_net_ids`) each independently re-derive the same `crossing_net_ids` set from `self.router.crossing_events()`, with identical boilerplate, including an identical `except Exception: crossing_net_ids = set()` that silently swallows any error and proceeds as if no crossings existed at all (`translation/route_rust.py`, around lines 4512-4526 and 4680-4694). This is a second instance of exactly the class of problem the `2026-08-18` plan already found once in the crossing-aware path: a failure that should be visible (crossing detection genuinely failed) is instead silently treated as a normal, unremarkable case (no crossings). Confirm during this plan's Milestone 0 whether this has ever actually been observed to fire in practice (it may never have been hit, in which case removing the swallow entirely and letting a real error propagate is likely the right fix, not adding more defensive code around it).
- Observation: there is already substantial, real focused test coverage for pieces of this area -- `tests/test_port_alignment_diagnostics.py` has roughly 25 tests directly exercising `_absorbed_terminal_centerline`, the crossing-aware correction path, and the "checked" batch corrector (`apply_checked_endpoint_corrections`/`apply_checked_endpoint_corrections_and_commit`), including tests with names like `test_crossing_aware_endpoint_correction_rejects_inserted_mid_access_geometry` and `test_crossing_aware_endpoint_correction_absorbs_both_split_terminal_sides`. This plan's own "add focused tests" work is not starting from zero, and should extend and reuse this file's existing fixtures and patterns rather than inventing a new, parallel test style. What is genuinely missing, confirmed by reading the diff of the `2026-08-18` bug fix directly (`git show 20aab29 --stat`), is a dedicated regression test for the *specific* bug class just fixed: a candidate correction whose newly-inserted segment lands at the position `_compatible_terminal_direction_sequence` does not expect, for the specific reason that the baseline's own port-adjacent segment already matched the port's facing direction. That fix's only verification was a full `multiportmmi_8x8` benchmark run; nothing in `tests/test_port_alignment_diagnostics.py` or elsewhere pins this exact scenario down at the unit level, so a future change could silently reintroduce it without any test noticing until the next full-benchmark run.
- Observation (2026-08-19, found while confirming the repository owner's own description of the intended correction algorithm against the real code -- see below): **none of the correction strategies inside `route_to_port_corrected_centerline_with_options` (`src/geometry_realization.rs`, line 1094) have any access to other nets' already-committed geometry.** Confirmed at the top level first -- that function's own signature is `(route: &RouteResult, primitives: &PrimitiveLibrary, grid: &GeometryGridSpec, source_port_um, target_port_um, allow_unchecked_bumps: bool)`, with no `obstacle_map: &ObstacleMap` parameter at all, even though `ObstacleMap` is used extensively elsewhere in the same file (for example `DenseOccupancyPrefix::from_obstacle_map`, lines 120-142) -- then confirmed for every individual strategy function in the chain by reading each one's own signature and body directly, not assumed from the top-level absence: `try_apply_full_straight_port_correction` (line 1515), `try_apply_full_diagonal_straight_port_correction` (line 1555), `try_apply_shared_axis_port_shift` (line 1479), `absorb_endpoint_delta_into_existing_axis_runs` (line 1945) and its `shift_*_existing_axis_run_{x,y}` helpers (lines 1985-2051) -- none take obstacle data. The two mechanisms capable of inserting genuinely *new* geometry (not just shifting/extending what already exists) are especially significant, and there turn out to be two independent implementations of "insert a bump," not one: `try_apply_full_straight_offset_bump_correction` (line 1597) -> `build_ordered_compact_offset_bump` (line 1675) -> `build_compact_four_bend_offset_bump` (line 1738), used only for a fully-straight-or-vertical centerline; and, inside the very last fallback `absorb_endpoint_delta_into_axis_runs` (line 1900), a *second*, separate bump path -- `insert_source_delta_bump`/`insert_target_delta_bump` (lines 2953, 2989) -- reached only after a third mechanism, "dogleg absorption" (`insert_source_delta_dogleg`/`insert_target_delta_dogleg`, lines 2735, 2774, using pre-identified slack already present in the route's own primitive structure, `DoglegAbsorber`), itself fails. All three of these geometry-inserting mechanisms were checked directly and confirmed to validate only internal geometric/kinematic consistency (`validate_bump_endpoint_tangents`, line 1891, checks tangent *direction* only) -- never spatial collision against any other net. The only thing in this repository that catches a resulting physical overlap is `translation/photonic_verification.py`'s `cross_net_waveguide_overlap` check, which runs once, at the very end of the entire pipeline, after every net has been routed and corrected -- the exact same "a real problem is only visible via a late, separate verification stage, not locally where it happens" shape already found once in the `2026-08-18` bug this plan generalizes from, now confirmed one level deeper (geometric collision safety, not just port-alignment accuracy). This is very likely the direct mechanism behind the `n_196`/`n_197` `cross_net_waveguide_overlap` finding recorded in `.agent/REPOSITORY_STATE.md` from the `multiportmmi_16x16` stable-baseline run the day before this observation (not yet traced to this exact call site with net-specific evidence -- that confirmation is part of this plan's own work, not assumed here).
- Observation (2026-08-19, Milestone 0 part (a), confirmed by direct re-read of all three passes as they exist today): the precise classification table, per net id in `list(self.route_bookkeeping.route_order)`. Define `source_has_fanout_stub = net_id in self.fanout_anchor_source_net_ids`, `target_has_fanout_stub = net_id in self.fanout_anchor_target_net_ids`, `both_fanout_stub = source_has_fanout_stub and target_has_fanout_stub`. Pass 1 (`_apply_checked_endpoint_corrections_for_net_ids`) applies unrestricted, both-sides correction to a net iff it is not the case that (`net_id in self.fanout_anchor_net_ids` and `record.corrected_centerline_um` is already truthy), and it is not the case that (`self.enable_crossings` and `net_id` is in the `crossing_net_ids` set that pass derives itself from `self.router.crossing_events()`'s `net_id`/`partner_net_id` fields), and at least one of `source_port`/`target_port` resolves to a real coordinate. Pass 2 (`_apply_checked_fanout_stub_endpoint_corrections_for_net_ids`) applies partial, non-stub-side-only correction iff `record.corrected_centerline_um` is already truthy (i.e. the net already went through fanout-stub pre-correction earlier in the pipeline, before any of these three passes run) and (`source_has_fanout_stub` or `target_has_fanout_stub`) and not `both_fanout_stub` (explicit `continue` at line 4705-4706) and the same crossing exclusion as pass 1 (its own, separately re-derived copy of the identical `crossing_net_ids` computation -- see the next observation). Pass 3 (`_apply_crossing_aware_endpoint_corrections_for_net_ids`) applies to a net iff `self.enable_crossings` and the net has a non-empty entry in `self._current_crossing_points_by_net_id()` -- note this is a *third*, independently-derived notion of "does this net have a crossing," distinct from the `self.router.crossing_events()`-based set passes 1/2 share; nothing in the current code proves these two sources agree on every net, which is itself a finding worth resolving during Milestone 1's consolidation (use exactly one canonical source), not just a documentation nit. Pass 3 runs for every crossing net regardless of fanout-stub status, passing `correct_source=not source_has_fanout_stub` and `correct_target=not target_has_fanout_stub` through to `_apply_crossing_aware_endpoint_correction_to_record`, so a crossing net with a fanout stub on one side still gets that side's crossing-region splicing/registration handled by pass 3, just with correction suppressed on the stubbed side. **The confirmed answer to Milestone 0's explicitly-asked question**: a net with `both_fanout_stub = True` and no crossing (not in any of the three passes' crossing sets) is skipped by all three passes -- pass 1 by the `fanout_anchor_net_ids`-and-already-corrected guard, pass 2 by its explicit both-sides `continue`, pass 3 by having no crossing points at all -- so it receives **zero** endpoint correction from this stage of the pipeline and keeps exactly the `corrected_centerline_um` that fanout-stub pre-correction (upstream of these three passes, not read in detail for this plan) already gave it. This is a real, narrow, currently-silent category with no name anywhere in the code; Milestone 1's classification function must give it an explicit name (for example `already_corrected_no_op`) rather than let it remain an implicit fall-through of three unrelated guard clauses, which is precisely the kind of hard-to-read hazard this plan exists to eliminate.
- Observation (2026-08-19, Milestone 0, git history check): the `except Exception: crossing_net_ids = set()` swallow (`translation/route_rust.py`, lines 4525-4526 and 4693-4694) was introduced verbatim by commit `9925249` (the Phase 2 closures-to-methods restructuring, 2026-08-18) and has not been touched since; `git log -S "crossing_net_ids = set()"` shows no other touching commit, and no test, log message, diagnostics field, or `.agent/` doc anywhere references it ever having actually fired. Milestone 2 should remove the swallow and let a real failure in `self.router.crossing_events()` propagate, per the plan's own instruction to do so if no evidence of legitimate firing is found.
- Observation: the repository owner independently described, from memory/intuition rather than reading the code, an algorithm for endpoint correction that matches the real strategy chain closely: (1) if an existing segment oriented off the port's own axis is available, adjust its length to absorb the cross-axis delta and separately correct the along-axis delta with a small straight -- matches `absorb_endpoint_delta_into_existing_axis_runs`; (2) if no such segment exists and the needed correction is uniform at both ends, shift the whole route rigidly -- matches `try_apply_shared_axis_port_shift`/`try_apply_full_straight_port_correction`; (3) if no such segment exists and the correction is not uniform, insert a "bump," trying multiple candidate locations, and check it does not collide with other waveguides -- matches the *intent* of `build_ordered_compact_offset_bump`/`insert_source_delta_bump`/`insert_target_delta_bump` exactly, except (per the observation above) the real code does not actually do the collision check the described intent calls for; (4) for a crossed net, apply the same logic independently to source-to-first-crossing and last-crossing-to-target -- matches `_spliced_crossing_endpoint_centerline`'s existing structure. The one piece not explicitly named in that description, the dogleg-absorption mechanism (using slack already present in the route's own existing primitive structure before falling back to constructing a brand-new bump), is best understood as a refinement of case (3) rather than a missing fifth case -- confirm this reading during Milestone 0 rather than assume it, since "reuses existing route slack" and "constructs new detour geometry" have different risk profiles for the same missing-collision-check reason.

- Observation (2026-08-19, Milestone 1, found while designing the classification function so it would work correctly at every call site, not just the one the plan's Context section originally described): pass 1 and pass 2 are each called from three places in `translation/route_rust.py`, not the single final-pass call site the plan's Context and Orientation section originally claimed -- `grep` across the whole file for both function names shows 3 call sites each (the final pass in `run()`, plus two separate mid-repair methods around lines 5638-5645 and 6019-6028 that apply both passes to just-repaired nets immediately after a rip-up/reroute round succeeds, before `run()`'s own final pass ever runs), while pass 3 (crossing-aware) has exactly 1. This did not require any change to the classification function's design (a net's category depends only on its own crossing/fanout-stub status, not on which call site is asking), but it does mean Milestone 3's "collapse into one entry point" has three places to collapse for pass 1/2, not one, and the relationship between the mid-repair calls and the final pass (does the final pass re-process a net the repair call sites already corrected, and if so is that intentional or wasted work?) needs to be traced during Milestone 3, not assumed. The plan's Context and Orientation section has been corrected in place to describe this accurately for a fresh reader.

## Decision Log

- Decision: Python orchestrates each pipeline stage as a separate, explicit call (obstacle map building, routing with rip-up/reroute, endpoint correction, geometry realization), rather than pushing more of the pipeline into one large Rust function.
  Rationale (from the user's own framing, confirmed in conversation before this plan was written): this matches `.agent/PROJECT_GOAL.md`'s already-stated "Future Architecture Initiative" target shape (each stage behind an explicit Python `Protocol`/`ABC` or Rust `trait` contract); it preserves the fast, Python-level diagnostic iteration loop (write a small script that monkeypatches one function, run it, read real intermediate values, adjust) that found and fixed every real bug in this repository's session history leading up to this plan, which would be far slower if the equivalent logic lived inside one Rust function requiring a full extension rebuild (`maturin develop --release`, tens of seconds) per iteration; and the performance cost of a few extra Python-Rust boundary crossings per net is negligible next to A* search itself, which this repository's own timing logs consistently show dominates total routing time by one to two orders of magnitude.
  Date/Author: 2026-08-18, decided in conversation with the repository owner, recorded here at the start of this plan.
- Decision: this first restructuring pass consolidates and adds focused tests (one function/module per concept, real unit tests replacing reliance on full-benchmark verification where practical); it does *not* extract a formal `typing.Protocol`/`abc.ABC` interface boundary yet.
  Rationale: the repository owner explicitly chose this depth over immediately building the full "Future Architecture Initiative" interface machinery, given via a direct choice between the two options. The heavier interface-extraction step remains a legitimate future goal (see `.agent/PROJECT_GOAL.md`) but is deliberately deferred until after this narrower, evidence-driven pass proves out the target shape on a concrete, already-understood piece of the pipeline.
  Date/Author: 2026-08-18, repository owner, recorded here.
- Decision: this plan was written and left for a future session to execute, rather than starting implementation in the same session that wrote it.
  Rationale: the session that wrote this plan had grown very long, with most of its history (a separate investigation into `multiportmmi_8x8`/`multiportmmi_16x16` benchmark findings, a Rust test-suite recalibration, process-document revisions) genuinely unrelated to this specific restructuring task and already fully captured in `.agent/REPOSITORY_STATE.md` and its own ExecPlans. Per `.agent/WORKFLOW.md`'s own stated purpose ("a future agent should be able to enter the repository, read the stable instructions and the active task plan, and continue without relying on hidden chat history"), a fresh session reading this plan and the current working tree has everything needed; carrying the unrelated history forward would only cost tokens without adding value. This plan was written carefully enough, per `.agent/PLANS.md`'s self-containment requirement, to make that handoff safe.
  Date/Author: 2026-08-18/19, repository owner and Claude, recorded here.
- Decision: add Milestone 0.5 ("give bump/dogleg insertion real collision awareness") ahead of the consolidation milestones (1-4), and treat it as the one deliberate exception to this plan's "Python orchestrates, Rust internals stay stable" framing (see the first Decision Log entry above).
  Rationale: confirming the repository owner's own independently-described mental model of the correction algorithm against the real Rust source (see the `2026-08-19` Surprises & Discoveries entries) surfaced a genuine, structural correctness gap, not a readability problem: none of the geometry-inserting correction strategies (`build_ordered_compact_offset_bump`, `insert_source_delta_bump`/`insert_target_delta_bump`, the dogleg-absorption mechanism) check the candidate geometry against other nets' already-committed waveguides before accepting it. This is very likely the direct mechanism behind the `n_196`/`n_197` `cross_net_waveguide_overlap` finding already recorded in `.agent/REPOSITORY_STATE.md` from the `multiportmmi_16x16` stable-baseline run the day before this entry was written -- a real, physical design-rule violation (two different nets' waveguides overlapping in silicon), which is a more serious defect than the pipeline being hard to read. Sequencing it first, rather than after the readability consolidation, follows this repository's own established priority (fix real, verification-catchable bugs as they are found, per `.agent/WORKFLOW.md`'s Routing Verification Gate and this same plan's own `2026-08-18` precedent) and costs nothing in ordering: Milestone 0.5's Rust collision-awareness work is independent of the Python-side classification/dispatch consolidation in Milestones 1-4, so neither blocks the other. The "Python orchestrates, Rust stays stable" decision above was made to preserve a fast Python-level diagnostic iteration loop for the *consolidation* work specifically; it was never meant to forbid fixing a genuine missing-safety-check bug that can only be fixed where the unsafe geometry construction actually happens, inside Rust -- recorded explicitly here so a future reader does not mistake Milestone 0.5 for a violation of that decision rather than a deliberate, scoped exception to it.
  Validating `multiportmmi_16x16` specifically for this milestone (via Milestone 0.5's own Concrete Steps) does not contradict the repository owner's separate direction that "the 16x16 benchmarks are not important for that matter" for the *restructuring* work (Milestone 5's validation ladder, which correctly excludes it): that direction was about not requiring 16x16 to be clean as a gate for finishing the readability restructuring, not about refusing to fix a real bug that 16x16 happens to be the evidence for. If this reading turns out to be wrong, the fix belongs to this session's future -- flagging it here rather than silently assuming it.
  Date/Author: 2026-08-19, Claude, recorded here; **confirmed with the repository owner the same day**, after they independently spotted a visible loop/overlap in a `multiportmmi_16x16` GDS render and asked whether it should be fixed now or deferred until after the readability restructuring -- given an explicit choice between "fix Milestone 0.5 now" and "restructure first, revisit after," the owner chose to fix it now.

- Decision: dispatch Milestone 0's remaining test-writing (the fanout-stub-both-sides characterization test and the `_compatible_terminal_direction_sequence` regression test) to a general-purpose implementation subagent, with the classification table and exact regression scenario fully specified in the dispatch brief by the orchestrator, rather than have the orchestrator write the test code directly.
  Rationale: the classification logic (the "understanding" part of this work) is now fully derived and recorded above from a direct, complete source trace; writing conventional pytest test code that follows `tests/test_route_rust_opened_cells.py`'s existing dummy-schematic fixture pattern (for the characterization test) and a plain direct-call unit test (for the pure-function regression test, since `_compatible_terminal_direction_sequence` takes only tuples and has no session/router dependency) is mechanical once the scenario is fully specified, and delegating it keeps the orchestrator's own context available for reviewing the result and moving on to Milestone 0.5. This is not the Codex CLI delegation path (`.agent/CLAUDE_CODEX_FLOW.md`); no Codex CLI session was confirmed available in this session, so a Claude general-purpose subagent was used instead, with its diff reviewed directly by the orchestrator before being accepted (Reviewer role, performed by the orchestrator itself, explicitly labeled at the point it happens).
  Date/Author: 2026-08-19, Claude (orchestrator), recorded here.

## Outcomes & Retrospective

**Milestone 0.5 (2026-08-19)**: Fixed the real, physical `cross_net_waveguide_overlap` between `n_196`/`n_197` on `multiportmmi_16x16`'s stable-baseline config -- confirmed gone in the final structured verification JSON, not inferred. The investigation took three distinct turns, each caught by measuring rather than assuming the fix worked, matching this repository's established pattern (see the `2026-08-18-dense-port-runway-clearance-reach` plan's own "worked example of catching a wrong diagnosis via measurement"): (1) the plan's original hypothesis ("the geometry-construction strategies have zero obstacle awareness") was true but not precise enough to fix directly -- tracing the actual call path with the repository's pre-existing `PHOTONIC_ROUTER_TRACE_ENDPOINT_BUMP_NETS` instrumentation found the real, narrower mechanism: a caller-side self-authorizing overlap allow-list in `route_port_corrected_centerline_checked_and_commit_native`; (2) fixing that alone left the exact same overlap in place with byte-identical numbers, which the Verifier Veto rule (`.agent/WORKFLOW.md`) correctly flagged as insufficient -- tracing *that* found a second, independent bug (an overly-broad `if not self.enable_crossings` guard in `translation/route_rust.py` silently suppressing failure visibility, which let a *third*, previously-unexamined call site in `translation/route_rust_realization.py` quietly re-derive the same unsafe geometry); (3) fixing both together eliminated the overlap and produced the expected, honest tradeoff -- 2 nets now cleanly fail instead of silently colliding. The repository owner was consulted at two decision points during this arc (sequencing Milestone 0.5 before or after the readability work; whether to keep extending the fix to make the 2 remaining nets route cleanly) and chose to fix now and stop at the honest-failure state both times. Residual, explicitly out-of-scope work: making `n_196`/`n_203`'s correction succeed collision-free (tracked above as a follow-up), and Milestone 2's later "make failure visible" work should account for `endpoint_correction_error` now being recorded correctly in the crossing-enabled case, not just the non-crossing case it previously handled.

## Context and Orientation

This section assumes no knowledge of any prior conversation or prior ExecPlan. Every fact needed to understand and execute this plan is repeated here.

### What "endpoint correction" means and why it exists

The router finds paths on a discrete grid (`grid_size_um` micrometers per cell). A port's real, physical connection point is an arbitrary `(x, y)` coordinate in micrometers, set by the chip's component layout -- it is essentially never exactly on a grid cell boundary. "Endpoint correction" is the step, run once per net after routing and before the final GDS chip layout is written, that adjusts the very start and end of that net's centerline (the sequence of `(x, y)` points describing its physical shape) so it lands exactly on the real port, not just close to it. This is normal, necessary, and already mostly correct; this plan is about *how the code is organized*, not about changing the correction math itself except where a bug is found.

### The three current passes, and the two real constraints behind them

**Correction (2026-08-19, found starting Milestone 1, source re-read fresh rather than trusted from this paragraph's original draft): pass 1 and pass 2 are each called from three places, not one.** In addition to the final, full-net-set call in `_RouteNetsRustSession.run()` (`translation/route_rust.py`, around lines 6935-6957 as of this correction, guarded by `if self.enable_checked_endpoint_correction:`), both are also called, as a pair (never pass 3), from inside two separate mid-repair methods -- around lines 5632-5645 and lines 6018-6028 -- immediately after a rip-up/reroute repair round successfully re-routes a batch of victim nets (`repaired_net_ids`/`repaired_records`), before `run()` itself ever reaches its own final pass. These two repair call sites appear to be two different repair code paths (their surrounding methods differ in shape -- one builds `repaired_records` and a separate `repaired_net_ids` list, the other builds `repaired_net_ids` directly and also collects `endpoint_correction_failed_net_ids` into the attempt diagnostics -- not yet fully traced to their calling repair strategies since that is outside this plan's scope, but both indisputably call the identical two functions with the identical two-pass pattern immediately after a repair round, which is what matters for this plan). Pass 3 (crossing-aware) is called from exactly one place, the final pass in `run()`, confirmed via `grep` across the whole file (3 call sites each for pass 1 and pass 2's function names, 1 for pass 3's). This matters directly for Milestone 1 (the classification logic must work correctly no matter which of the three call sites is asking about a given net, not just the final one) and even more for Milestone 3 (there are three places to collapse into a readable sequence for pass 1/2, not one, and the repair call sites' relationship to the final pass -- does the final pass re-process a net the repair call sites already corrected? -- needs to be traced and preserved, not just assumed safe to unify). The three passes are called from `_RouteNetsRustSession.run()` in `translation/route_rust.py`, in this exact order, around lines 6935-6957 (guarded by `if self.enable_checked_endpoint_correction:`), for the *final* pass over the full net set:

1. `_apply_checked_endpoint_corrections_for_net_ids` (`translation/route_rust.py`, line 4485): the default pass. Builds a batch of `(net_id, route_obj, opened_candidate_cells, clearance_exempt_cells, source_port_um, target_port_um)` tuples for every net that is not a fanout-stub net and not involved in a crossing, then calls a single Rust batch method, `self.router.apply_checked_endpoint_corrections(...)` (line 4564), which does the actual geometric correction work in Rust.
2. `_apply_checked_fanout_stub_endpoint_corrections_for_net_ids` (`translation/route_rust.py`, line 4651): for nets where exactly one side (source or target, never both, per the `if source_has_fanout_stub and target_has_fanout_stub: continue` check at line 4705-4706) is a "fanout stub" -- a pre-corrected endpoint adapter used for dense multi-port fan-out patterns, which must not be touched again -- this pass calls the *same* Rust method, `apply_checked_endpoint_corrections`, but only requests correction for the non-stub side (`source_port = None if source_has_fanout_stub else ...`, line 4716-4720). It also excludes any net involved in a crossing, with an explicit comment (line 4707-4712) explaining why: the unrestricted corrector could move geometry on the crossing-protected side before the crossing-aware pass gets a chance to splice it correctly.
3. `_apply_crossing_aware_endpoint_corrections_for_net_ids` (`translation/route_rust.py`, line 5109): handles every net excluded from the first two passes -- anything involved in a crossing. This is the only pass that does *not* call the simple batch Rust method directly; instead it calls `_apply_crossing_aware_endpoint_correction_to_record` (`translation/route_rust_endpoint_correction.py`, line 1053), which is where the `2026-08-18` plan's bug lived. That function's job is to get the *same* rich, unrestricted correction the other two passes use (it does call `route_port_corrected_centerline`, the identical underlying Rust correction, via `_spliced_crossing_endpoint_centerline`, line 682) but then slice out only the portion of that correction from the route's start to the first crossing point (or the last crossing point to the route's end), so the crossing-protected middle region is never touched. If that sliced portion fails a compatibility check (`_compatible_terminal_direction_sequence`, line 621 -- the function fixed in the `2026-08-18` plan), it falls back to a second, weaker, Python-only solver (`_absorbed_terminal_centerline`, line 439) limited to a small "guard window" near the crossing point; if *that* also fails, it silently falls back a third time to the raw, uncorrected route for that region (`translation/route_rust_endpoint_correction.py`, lines 786-787 and 807-808).

Both of the first two passes independently re-derive the same `crossing_net_ids` set (which nets are involved in any crossing) from `self.router.crossing_events()`, with near-identical boilerplate including an identical `except Exception: crossing_net_ids = set()` (lines 4512-4526 and 4680-4694) -- see Surprises & Discoveries for why this specific duplication is itself worth fixing, separately from the deeper crossing-aware fallback bug already fixed.

### The `2026-08-18` bug, as a concrete example of the pattern to eliminate

`.agent/execplans/2026-08-18-crossing-aware-endpoint-correction-direction-sequence.md` (already complete, commit `20aab29`) fixed one specific instance of a broader pattern: a correction strategy inside the crossing-aware path silently failed and fell through to a worse fallback, without that failure ever being visible until a much later, separate verification stage caught the final, wrong result. The specific bug was in `_compatible_terminal_direction_sequence`: it assumed the one newly-inserted correction segment always sits at a fixed position in a list of directions, when in the common case (the baseline's own port-adjacent segment already matches the port's facing direction) the real, correct new segment lands at the *other* position instead -- rejecting an already-correct answer and triggering two further silent fallbacks, the last of which returned the raw, uncorrected route for that region, landing `2.0014` micrometers from the real port against a `2.0` micrometer tolerance. Read that plan's own Surprises & Discoveries section in full for the exact evidence trail; it is not repeated here, but the general shape -- "a correction strategy can fail, and when it does, the code keeps trying progressively worse fallbacks without ever surfacing that a fallback happened at all" -- is the central problem this restructuring plan exists to eliminate, not just patch one instance of.

### Relevant existing test coverage

`tests/test_port_alignment_diagnostics.py` (about 1050 lines) already has substantial, real, focused coverage of pieces of this area: tests calling `_absorbed_terminal_centerline` directly with hand-constructed centerlines (for example `test_crossing_aware_endpoint_correction_keeps_required_port_straight`, line 867, and `test_crossing_aware_endpoint_correction_does_not_modify_bend_samples`, line 882), tests of the crossing-aware path's rejection behavior (`test_crossing_aware_endpoint_correction_rejects_inserted_mid_access_geometry`, line 803; `test_crossing_aware_endpoint_correction_rejects_unanchored_source_prefix`, line 846), and tests of the "checked" batch corrector directly via the Rust binding (`test_batch_checked_endpoint_correction_returns_per_net_metadata`, line 544; several `test_checked_case4_bump_*` tests around lines 392-544). This plan's testing work should read this file first and extend it, not duplicate its patterns from scratch.

## Plan of Work

### Milestone 0: characterize current behavior before changing anything

Before any restructuring, write characterization tests (tests that record and pin down the *current*, real behavior of the system as it exists today, including any warts, so later milestones can be validated against "did this change on purpose" rather than discovering a difference only by re-running a full benchmark) for: (a) which of the three passes a net with a given combination of (has a crossing: yes/no, has a fanout stub on source: yes/no, has a fanout stub on target: yes/no) actually gets routed through today -- this is the classification logic this plan's later milestones will consolidate, and it needs to be pinned down precisely, including edge cases like a net with fanout stubs on *both* sides (currently skipped by the fanout-stub pass entirely per the `continue` at line 4705-4706 -- confirm what actually happens to such a net today, since it is excluded from pass 2 but may or may not also be excluded from pass 1 depending on its crossing status, and Milestone 0 should determine and record the real, current answer rather than assume one); and (b) a new, focused regression test for the exact `2026-08-18` bug scenario (a correction whose new segment lands at the position `_compatible_terminal_direction_sequence` does not expect for `allow_extra_at_start=True`), confirmed to fail if the fix from that commit were reverted and pass with it in place -- this closes the gap identified in Surprises & Discoveries. Also confirm, empirically, whether the `except Exception: crossing_net_ids = set()` swallow at lines 4512-4526/4680-4694 has ever actually been observed to catch anything (search git history and any logged/recorded evidence; if none is found, plan to remove the swallow in a later milestone rather than preserve it).

### Milestone 0.5: give bump/dogleg insertion real collision awareness

This milestone was added after this plan's initial draft, once confirming the repository owner's own description of the intended correction algorithm against the real code surfaced a genuine correctness bug, not just a readability problem -- see the `2026-08-19` entry in Surprises & Discoveries for the full evidence. It is placed before the consolidation milestones (1-4) deliberately: a route insertion that can physically overlap another net's waveguide is a more serious problem than the pipeline being hard to read, and fixing it does not depend on the consolidation work below (or vice versa), so there is no reason to sequence it after.

First, close the gap between "very likely the cause" and "confirmed the cause": reproduce the `n_196`/`n_197` `cross_net_waveguide_overlap` finding (`multiportmmi_16x16` under its documented `STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` configuration -- see Concrete Steps in the `2026-08-18-dense-port-runway-clearance-reach.md` plan or this plan's own Concrete Steps for the general pattern, substituting `multiportmmi_16x16` and its own stable-baseline flags), then trace which specific correction strategy actually ran for `n_196` and/or `n_197` (the same kind of direct instrumentation -- monkeypatch the relevant Rust-call site or add a temporary Rust-side debug print, whichever is faster to iterate with -- used throughout this repository's recent session history; do not assume it was the bump path without checking, since `absorb_endpoint_delta_into_existing_axis_runs`'s existing-run-shift could, in principle, also cause an overlap if the shift is large enough, even though it is a lower-risk mechanism than constructing brand-new geometry).

Once confirmed, thread real obstacle awareness into whichever mechanism(s) need it. This is expected to require passing an `&ObstacleMap` (or an equivalent, cheaper-to-check representation -- read how `DenseOccupancyPrefix::from_obstacle_map`, `src/geometry_realization.rs` line 120, is already used elsewhere in this same file for exactly this kind of query before inventing a new representation) down through `route_to_port_corrected_centerline_with_options` and into the specific strategies that insert new geometry (`try_apply_full_straight_offset_bump_correction`/`build_ordered_compact_offset_bump`, and `insert_source_delta_bump`/`insert_target_delta_bump`), and checking each candidate bump placement against it before accepting one -- matching the existing "try several candidates, use the first that validates" structure already in `build_ordered_compact_offset_bump` (line 1675) and `first_viable_delta_bump_run` (called from `insert_source_delta_bump`, line 2953), which already try multiple placements/directions for purely geometric reasons; add the collision check as one more condition a candidate must satisfy before being accepted, not a separate pass. Decide, and record the reasoning, for `absorb_endpoint_delta_into_existing_axis_runs`'s existing-run-shift path too: does it need the same treatment, or is shifting an already-obstacle-validated run by a small amount low-risk enough to leave alone (with a recorded reason, e.g. a bound on how large such a shift can realistically be) -- do not silently skip this question.

This milestone is the one exception to this plan's "Python orchestrates, Rust internals stay stable" framing (see Decision Log): it requires a real Rust algorithm change, not just Python-side reorganization, because the missing safety check lives entirely inside Rust geometry construction. Validate with a Rust unit test reproducing the exact `n_196`/`n_197` geometry (once traced) as a fast, focused regression test, in addition to re-running the full `multiportmmi_16x16` stable-baseline benchmark to confirm the real overlap is gone.

### Milestone 1: consolidate net classification into one place, additively

Add one new function (exact name and location to be decided during implementation, once the current code is read fresh -- a reasonable starting point is a method on `_RouteNetsRustSession` or a small dataclass/enum-returning helper in `translation/route_rust.py`, but do not lock this in before reading the code, per `.agent/PLANS.md`'s own guidance against over-specifying before source inspection) that computes, once, for every net, which of the (now-named, not just implicit) correction categories it belongs to: no correction needed, plain/unrestricted, fanout-stub-source-only, fanout-stub-target-only, or crossing-aware. This subsumes the currently-duplicated `crossing_net_ids` derivation (compute it once, pass it in) and makes the fanout-stub-both-sides case (whatever Milestone 0 found the current real answer to be) an explicit, named outcome instead of an implicit fallthrough. This milestone is purely additive: the three existing passes keep running exactly as they do today, just reading their net-id sets from this new, shared classification instead of each re-deriving their own. Validate with Milestone 0's characterization tests: the classification for every net in a real benchmark run should match what Milestone 0 recorded as current behavior, cell for cell.

### Milestone 2: make failure visible

Replace the crossing-aware path's silent fallback-to-uncorrected-baseline (`translation/route_rust_endpoint_correction.py`, lines 786-787 and 807-808 in the state as of commit `20aab29`) with something that records, at minimum in the diagnostics already written to `build/routes/*_diagnostics.txt`/`*_FAILED.txt` for other routing decisions in this repository, that a fallback occurred and which one -- so a future case like the `2026-08-18` bug would be visible immediately (in per-net diagnostics, the same place a routing failure is already visible) rather than only surfacing much later via a separate photonic-verification failure, or worse, silently landing just inside tolerance and never being caught by anything except a full geometric audit. Apply the same principle to the two `except Exception: crossing_net_ids = set()` sites from Milestone 0, per what that milestone found (remove the swallow if it has never legitimately fired, or make it visible if it can).

### Milestone 3: collapse the top-level entry point into one readable sequence

Once Milestones 1 and 2 exist, restructure `_RouteNetsRustSession.run()`'s three sequential calls (lines 6923-6947) into one call to a single, clearly-named orchestration function that, read top-to-bottom, tells the whole story: classify nets (Milestone 1's function), then for each category, apply the one correction strategy that applies, using the now-visible-on-failure mechanics from Milestone 2. This is the milestone that delivers the user's stated goal directly ("one file where you can really read the workflow from") -- the underlying Rust calls and correction algorithms should not need to change at all for this milestone; it is purely about how the dispatch is organized and named.

### Milestone 4: broaden test coverage

Extend `tests/test_port_alignment_diagnostics.py` with focused tests for the new classification function (Milestone 1) and the failure-visibility mechanism (Milestone 2), reusing that file's existing fixture patterns. Do not remove or weaken any existing test in that file without independently confirming, the same way this repository's other recent plans have, that the property it protects is preserved by the restructuring, not just that it happens to still pass.

### Milestone 5: broad validation

Run the full validation ladder: `PYTHONPATH=. .venv/bin/pytest -q` (expect no new failures beyond whatever this plan intentionally changes); `benes_4x4` and `multiportmmi_8x8` end to end, reading `build/verification/*.json` directly for both crossing and photonic verification (per `.agent/WORKFLOW.md`'s Routing Verification Gate -- a passing `pytest` run does not satisfy this on its own). Critically, given a discovery made the day this plan was written: `multiportmmi_8x8` and `multiportmmi_16x16` both have a documented "stable crossing-router baseline" configuration recorded as a comment in `benchmarks/multiportmmi_8x8.py` and `benchmarks/multiportmmi_16x16.py` (`STABLE_ROUTING_ENV`/`STABLE_ROUTING_FLAGS` -- two environment variables, `PHOTONIC_ROUTER_LONG_STRAIGHT_CONGESTION_WEIGHT=0.05` and `PHOTONIC_ROUTER_FANOUT_STUB_BEND_DEGREES=90`, plus `--crossings true --crossing-mode lidar-pure --fanout-access-mode static-stubs --routing-window-scale 0.35 --foreign-port-keepout-cells 0`), which differs substantially from the router's own bare CLI defaults on three of those four flags. Validate `multiportmmi_8x8` under *both* its bare defaults and this documented stable-baseline configuration, since they exercise meaningfully different code paths (confirmed: `multiportmmi_8x8` was independently found to be fully clean under both, the day this plan was written) and a change to endpoint correction could plausibly behave differently under `fanout-access-mode static-stubs` specifically, since that mode is what actually produces the fanout-stub nets this plan's classification work is centrally concerned with. Per the repository owner's own direction the day this plan was written, `multiportmmi_16x16` is *not* required for this validation (it currently has its own, separate, already-tracked real bug -- a physical waveguide overlap between `n_196` and `n_197` under the stable-baseline configuration -- unrelated to endpoint correction and out of scope here); keep `multiportmmi_8x8` as the running benchmark check throughout this plan's milestones, not `multiportmmi_16x16`.

## Concrete Steps

All commands below are run from the repository root, `/home/benjamin/Documents/Repositories/working/TUMPhotonicRouter`, using `.venv/bin/python`, with `PYTHONPATH=.` set for direct module imports.

Full Python test suite, before and after each milestone:

    PYTHONPATH=. .venv/bin/pytest -q

Focused endpoint-correction test file:

    PYTHONPATH=. .venv/bin/pytest -q tests/test_port_alignment_diagnostics.py -v

`benes_4x4` end to end (small, fast, safe to run with `--debug-svgs` since it has few nets):

    rm -rf build/routes build/verification
    PYTHONPATH=. .venv/bin/python routing_flow.py benes_4x4 --debug-svgs

`multiportmmi_8x8` end to end under bare defaults (no debug flags -- safe, does not generate any SVGs; never run `--debug-svgs` with no value on this benchmark, per this repository's standing safety rule about resource use on this machine):

    rm -rf build/routes build/verification
    PYTHONPATH=. .venv/bin/python routing_flow.py multiportmmi_8x8

`multiportmmi_8x8` end to end under the documented stable-baseline configuration:

    rm -rf build/routes build/verification
    PHOTONIC_ROUTER_LONG_STRAIGHT_CONGESTION_WEIGHT="0.05" PHOTONIC_ROUTER_FANOUT_STUB_BEND_DEGREES="90" \
    PYTHONPATH=. .venv/bin/python routing_flow.py multiportmmi_8x8 \
      --crossings true --crossing-mode lidar-pure --fanout-access-mode static-stubs \
      --routing-window-scale 0.35 --foreign-port-keepout-cells 0

After either `multiportmmi_8x8` run, read the verdict directly, per `.agent/WORKFLOW.md`'s Routing Verification Gate (do not infer success from console output alone):

    python3 -c "
    import json
    for name in ['crossing_verification', 'photonic_verification']:
        with open(f'build/verification/multiportmmi_8x8_{name}.json') as f:
            d = json.load(f)
        print(name, {k: d.get(k) for k in ['status','success','error_count','issues']})
    "

No Rust rebuild should be needed unless a milestone ends up touching `src/py_router.rs` or `src/geometry_realization.rs` directly (not expected for Milestones 0-4, which are Python-side reorganization; if it does become necessary, use this machine's required toolchain override):

    RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu PYO3_PYTHON="$PWD/.venv/bin/python" .venv/bin/python -m maturin develop --release

## Validation and Acceptance

Each milestone's own description above states what it must preserve or newly prove. Overall acceptance for this plan: `_RouteNetsRustSession.run()`'s endpoint-correction section reads as one clear, short call sequence instead of three independently-derived passes; every net's correction category is decided in exactly one place; a correction strategy falling back to a worse answer is visible in per-net diagnostics, not just eventually catchable by a separate downstream verification stage; the exact `2026-08-18` bug scenario has a dedicated, fast regression test that does not require a full benchmark run to catch a reintroduction; and the full validation ladder (pytest, `benes_4x4`, `multiportmmi_8x8` under both configurations) shows no regressions, with every verdict read from the actual structured JSON reports, not inferred from console output or a passing exit code alone.

## Idempotence and Recovery

Milestones 0, 1, and 4 are additive (new tests, new functions not yet wired into the real call path) and safe to land independently even if later milestones are paused or reconsidered, matching the pattern this repository's other recent plans have used successfully. Milestone 3 (collapsing the three passes into one entry point) changes real behavior in how corrections are dispatched and should be committed as one focused commit, not split partway, for the same reason the `2026-08-18-dense-port-runway-clearance-reach.md` plan gave for its own equivalent milestone: splitting it would recreate a mutually-inconsistent intermediate state. Do not run `--debug-svgs` with no value on `multiportmmi_8x8` or any comparably large benchmark at any point in this plan's execution.

## Artifacts and Notes

The exact evidence trail for the `2026-08-18` bug this plan generalizes from is preserved in full in `.agent/execplans/2026-08-18-crossing-aware-endpoint-correction-direction-sequence.md`'s own Surprises & Discoveries and Artifacts and Notes sections; it is summarized, not repeated, in this plan's Context and Orientation.

## Interfaces and Dependencies

To be determined during Milestones 1-3, after reading the current code fresh (per `.agent/PLANS.md`'s guidance against over-specifying implementation details before source inspection justifies them). At minimum, expect: one new classification function/type in `translation/route_rust.py` (Milestone 1); a change to how fallback outcomes are recorded in `translation/route_rust_endpoint_correction.py` (Milestone 2); and a single new top-level orchestration function replacing the three current call sites at `translation/route_rust.py` lines 6923-6947 (Milestone 3). No change to the underlying Rust correction functions (`route_port_corrected_centerline`, `apply_checked_endpoint_corrections`, or their Rust-side implementations in `src/py_router.rs`/`src/geometry_realization.rs`) is expected; this plan is about Python-side orchestration, per the "Python orchestrates each stage" decision recorded above.
