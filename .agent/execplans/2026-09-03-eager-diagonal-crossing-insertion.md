# Eager insertion of perpendicular diagonal-x-diagonal crossings (replace the defer)

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. This document must be maintained in accordance with `.agent/PLANS.md` (repository root).

## Purpose / Big Picture

Today the crossing-aware A\* kernel cannot reliably cross a fan of parallel 45-degree waveguides with a single straight 135-degree line, even though every such crossing is perpendicular, has ample margin, and is legal by the crossing ruleset. The concrete symptom: in `multiportmmi_32x32`, net `n_286` (Rust net id 287, `mmi0_multiport_2_4,o2 -> mol_array_1_mzi_16,o1`) must cross a fan of ~9 parallel diagonals spaced 22 um (11 grid cells) apart; the search crosses the top few and then the crossings stop being recorded, the search exhausts (`open_set_exhausted`, not a budget cap), and the net fails.

After this plan: a perpendicular crossing of a committed diagonal is recorded the moment the kernel detects it, even when the geometric intersection lies half a cell past the end of the current 1-cell move (the "X between cells" case). A unit test pins that behavior. `multiportmmi_32x32 --debug-stop-after-route 287` routes `n_286`, the full validation ladder stays at 0 verification errors, and pytest stays at its documented baseline. The stop-gap "defer" introduced on 2026-09-02 (commit `579aed2`) is removed, because deferring hands the crossing to the next move whose halo re-contacts the same partner in the same window and the crossing never matures.

## Progress

- [x] (2026-09-03) Milestone 0 done, with a decisive surprise (see Surprises): three tests written. `perpendicular_diagonal_x_crossing_is_recorded_eagerly` fails today as intended (approach move returns crossing_count 0). `consecutive_perpendicular_diagonal_crossings_on_one_line` (margin 2) PASSED today -- the two-partner geometry alone does not reproduce the wall. `consecutive_perpendicular_diagonal_crossings_with_benchmark_margin` (margin 5, the benchmark value) FAILED today with no route -- this is the real wall in a unit test.
- [x] (2026-09-03) Milestone 1 -- re-scoped by evidence. The wall is NOT the X-detection; it is the move AFTER an end-of-move crossing re-contacting the partner it just crossed and being rejected at three layers, plus an unpayable debt. Four small fixes in `src/astar.rs` (`crossing_move_outcome_with_segments` and `LiveCrossingHook::evaluate`), all uncommitted pending owner review: (1) partial pending consumption for a pure straight in the pending direction (4 + 1 pays a debt of 5; a move that would bend is still rejected); (2) skip a t=0 re-intersection with the pending partner (lidar-pure counterpart of the tracked-partner skip); (3) skip contact-without-intersection with the pending partner (same); (4) `CrossingMoveOutcome.contact_only_pending_partner`, set when every touched cell belongs to the pending partner, exempts the move from evaluate's "contact without recorded crossing" rejection. Result: the margin-5 consecutive test passes; cargo 421/422 (only the eager-X test still fails, see open decision); pytest 10 failed / 357 passed (baseline); ladder heater 8.9 s, mm8 16.5 s, benes8 37.8 s, mm16 85.1 s, benes16 180.4 s, all 0 verification errors. Eager extension through the X (the plan's original M1) was NOT needed for the wall and is now an open owner decision.
- [x] (2026-09-03) Milestone 1b (owner decision, Point 2): debt = half_size from the intersection, pure straights only, bends rejected while the debt is open (both hook paths). Four old-semantics tests re-pinned with derivations; new realized-level test added. cargo 423 passed; pytest 10 failed / 357 passed (baseline); ladder heater 8.6 s, mm8 16.0 s, benes8 36.2 s, mm16 63.3 s (was 85-105), benes16 173.1 s, all 0 verification errors.
- [ ] (2026-09-03) Milestone 3 blocked: Point 2 regressed the real net (see Surprises); root-causing with PHOTONIC_ROUTER_CHAIN_DIAG before any ladder/full run. Acceptance criterion restated by the owner: the route must cross n_280 and the parallels below on their DIAGONALS ((6464,3157)->(6695,3389) for n_280); a first-search trace must show accepts on those diagonals.
- [x] (2026-09-03) Milestone 2 done: defer path and `contact_intersection_lies_ahead` deleted (`grep -c defer_contact_intersection_ahead src/astar.rs` = 0); approach-move test inverted and un-ignored.
- [ ] Milestone 3: sequential Harness pass (cargo test, pytest baseline, five-benchmark ladder, then `multiportmmi_32x32` stop-287 and full run) recorded here with a verifier packet and a PASS/FAIL verdict.
- [ ] Milestone 4: retrospective; `.agent/REPOSITORY_STATE.md` and `.agent/ORCHESTRATOR.md` active-plan pointer updated.

## Surprises & Discoveries

- Observation (2026-09-03, Milestone 0): the two-partner perpendicular geometry with margin 2 routes today; with the benchmark margin 5 it does not. The plan's premise (the X-between-cells approach move being deferred/killed) is real but is NOT what walls the fan.
  Evidence: `consecutive_perpendicular_diagonal_crossings_on_one_line` passed unmodified; `..._with_benchmark_margin` failed with `search-failure kind=iteration_cap ... bestpath crossings=0` -- no node carrying a crossing ever entered the heap although the hook logged `accept_with_pending` for both partners.
- Observation: the real chain of rejections, found by instrumenting every `chain_completed = false` branch of the eager completion chain (temporary `PHOTONIC_ROUTER_CHAIN_DIAG`, removed again). Every break was `hook_none`. Layer by layer: (a) a crossing at the END cell of its move gets `missing_after = ceil(5 - 0) = 5`; the primitive library has straights of 1 and 4 cells only, and `reject_pending_initial_run` demanded one move cover the whole remaining debt -> a debt of 5 was unpayable (masked elsewhere because the search finds alignments where the crossing lands mid-move and the debt is <= 4; in the fan, each crossing's debt run lands exactly on the next partner 5.5 steps away, so the end-of-move case is systematic). (b) With partial consumption allowed, the debt-paying move starts ON the crossing cell and re-finds the same intersection at t=0; `crossing_move_outcome_with_segments` rejected it as a new intersection inside the pending zone (~line 7075) -- the existing t=0 skip only covered `track_crossed_partners` (require_all_partners) mode. (c) With that skipped, the same move's diagonal halo still touches the partner's neighbouring cells -> "contact without intersection" -> `reject_contact_unmatched`; the tracked-partner mode has a `continue` for this, lidar-pure did not. (d) With both inner skips in place the inner function returned `Some(crossing_count 0)` -- and `LiveCrossingHook::evaluate` then rejected it: `(!footprint_free || halo_contact) && crossing_count == 0 -> None`. This last rule is why the search statistics stayed byte-identical across the first three fixes: nothing ever survived it.
  Evidence: chain-break histogram `hook_none pending=5 step=1` (370x) etc.; the line before each break moved from `consume_pending_initial_run pending=5` to `reject_contact_unmatched partner=2` to `defer_contact_intersection_ahead` as each layer was removed; after fix (d) the margin-5 test passes and the ladder is green.
- Observation: the 2026-09-02 defer (`579aed2`, `contact_intersection_lies_ahead` -> `continue`) never let a move survive in the real kernel: its `Some(crossing_count 0)` result was always rejected by the evaluate rule in (d). Its only observable effect was which reject counter fired. Removing it is a pure cleanup (Milestone 2).
  Evidence: search stats identical with and without it; evaluate code at `LiveCrossingHook::evaluate`.
- Observation (owner, 2026-09-03, recorded verbatim in substance): the straight-after debt exists so that the crossing element itself is realizable (straight inside +-`crossing_half_size_cells`); the `bend_runout` part of `required_margin` accounts for a possible following bend, but a bend primitive brings its own straight arm. When cells are inserted eagerly, only what the crossing needs (half_size = 2) has to be inserted, not the full 5. Not implemented in this milestone (the wall is fixed without it); recorded as an open decision below because it changes after-side margin semantics for every crossing and needs its own ladder validation.
- Observation (2026-09-03, after Point 2 landed, `4c623ae`): REGRESSION on the real net that the ladder did not catch. `multiportmmi_32x32 --debug-stop-after-route 287`: net 287's first search now exhausts after only 1.1M iterations with explored bbox y in [1521..1631] (before Point 2: 8.7M iterations, floor 768, best path 14 crossings to (3400,950)); best path now 2 crossings ending at (3403,1550). The search no longer descends past n_284's horizontal run at y=1517, so the expected path (the owner's acceptance criterion: cross n_280's diagonal at (6464,3157)->(6695,3389) and the parallels below it on their diagonals) is never even reached.
  Evidence: `chaindiag`/`p2trace` runs. On the old descent column x=3104 the crossings of n_285 (y=1536) AND n_284 (y=1517) are accepted by the hook (`accept_with_pending route_before 7..9 pending 1..2`, 11.5k accepts on n_284 across x), and the first chain step from the crossing cell runs (`consume_pending_partial y=1517 ang=6 pend=2 run=1`, 3.5k) -- yet no state below y=1521 is ever expanded (the bbox counts both tiers). The nodes produced by those crossings therefore never reach the heap: the chain must abandon after its first step, or the final node is closed/cost-pruned. The second chain step is contact-free and goes through `crossing_no_contact_outcome`, which prints no pending trace, so its absence in the log proved nothing (a reasoning trap that cost time). A permanent env-gated diagnostic `PHOTONIC_ROUTER_CHAIN_DIAG` now names the abandoning branch and the final-node prune reason; the run is in flight.
  The isolated geometry routes: `vertical_route_crosses_two_horizontal_partners_with_benchmark_margin` (two horizontals 19 apart, margin 5) passes, so the blocker is contextual. The owner's read (2026-09-03): the top descent may be failing LEGITIMATELY now; finish that check briefly, then return to the acceptance criterion above.
- CORRECTION (2026-09-03, after adding a per-search sequence number to every diagnostic line): the previous observation below was contaminated by OTHER searches sharing stderr. With `seq` on the lines, net 287's failing search (seq=125) has ZERO Tier-2 pushes or pops below y=1521 -- every sub-1521 push/pop/prune I attributed to it came from earlier nets (seq 9, 95..99). So in the real search the eager chains at n_285/n_284 never complete; the reason is being read from seq-tagged CHAIN_DIAG lines now. Lesson recorded: shared-stderr diagnostics need a search id; `search_seq` is now printed on `search-failure`, `pop-diag`, `chain-break`, `chain-final` lines.
- (superseded, kept for the record) Observation (2026-09-03, chain/pop diagnosis of the Point-2 regression, facts only): all 60 net-287 searches use the full window (y 805..1631) yet expand nothing below y=1521; `open_set_exhausted` at ~1.1M iterations each. CHAIN_DIAG shows the eager chains DO complete far below 1521 (final states down to y=805, at every one of the five upper horizontals and deeper) but their final nodes are `cost_pruned` -- the key already holds an equal/lower g -- while the first arrival at each such key (which logs nothing and should push) never shows up as an expansion. Pop-diag (`PHOTONIC_ROUTER_POP_DIAG_BELOW_Y`) added to see whether those first arrivals are pushed and whether they are ever popped. Delivered meanwhile: `build/routed_multiportmmi_32x32_CURRENT2_net285.gds` (current code, routes 1-286, 286 attempts / 0 failures / 0 repairs -- the prefix is frictionless under Point 2).
- Observation (2026-09-03, probe cells, seq-scoped to net 287's real search): the wall is NOT static and NOT an unknown owner. On the old descent column x=3104: (3104,1536) `dynamic_owners=[286]` (n_285), (3104,1517) `dynamic_owners=[285]` (n_284), everything between them free; along y=1521 and y=1518 every probed cell is free (static only at x=3404/3405 = n_271's stub). Both owners are in the search's partner lookup set, so the lidar-pure contract (crossings discovered by collision, lookup DB = all committed routes, no whitelist) holds. Yet the search makes NO hook contact with 285/286 at all and its frontier stops at y=1521 in free space (n_285 has a lower run around y 1519-1520 west of x=3104 per the GDS bbox; not yet probed). A move into a free cell is being rejected before any crossing logic -- consistent with the new `evaluate` pending guard (rejects non-straight moves while a debt is open, no trace) or another Point-2 rule. Owner's framing (correct): it crossed before the changes, so the changes (or the prefix routing they altered: 286/0/0 vs 335/2/2) are the cause; unit tests cover H/V crossings and pass, so the real ingredient is missing from them. Bisecting empirically: env toggle `PHOTONIC_ROUTER_BISECT_NO_EVAL_PENDING_GUARD` (run in flight), then the other two Point-2 rules if needed. Whatever the bisect finds gets a unit test before the fix.
- Bisect (2026-09-03, env toggles, each a full stop-287 run, first net-287 search compared): bisect 1 = `evaluate` pending guard OFF -> byte-identical to Point 2 (iter 1095528, floor 1521, 2 crossings): the guard is NOT the cause. bisect 2 = all three Point-2 rules OFF (debt from required_margin, bends may pay, guard off) -> pre-Point-2 behaviour returns exactly (iter 8702955, floor 805, 14 crossings to (3400,950)): Point 2's rules ARE the cause. bisect 3 (only "bends may pay" restored, debt stays half_size) in flight; bisect 4 (only debt restored) next if needed. The removed defer cannot be involved: only `LiveCrossingHook::evaluate` (and the test wrapper) reach the inner function, and evaluate killed every deferred move.
- Bisect 3 (only "bends may pay" restored, debt stays half_size): byte-identical to Point 2. By elimination the cause is the DEBT BASIS (half_size instead of required_margin). Landing counters on probe cells (new: probe cells share the target-ring counter slots) in the Point-2 configuration, seq 125: the descent happens at the far west (x 3060-3080); at (3060,1521) 10 landings are accepted, at (3060,1520) all 54 landings are rejected by the hook with a blocked footprint, likewise 1519/1518/1517. Cell status there: n_285's LOWER run sits at (3060,1520) and n_284 at (3060,1517) -- only 3 cells apart (at x=3104 they are 19 apart: 1536 vs 1517). At x=3104 there are NO landings in the band at all, i.e. n_285 is never crossed at (3104,1536) either: under the half-size debt the search does not cross n_285 anywhere. No level1 line accompanies those rejections, so they occur before the intersection loop -- the pending block -- meaning the landing state carries pending > 0. Pending trace run in flight to see where that debt comes from.
- Observation (2026-09-03, fully seq-attributed traces + landing counters along the OLD descent path, net 287's real search seq 125): the old path is explored end to end -- east run, the 45-degree diagonal, down to (3104,1537) with accepted landings -- and then EVERY landing on n_285's cell (3104,1536) is rejected by the hook (54/54, likewise 1535..1531 for the 4-cell straights sweeping through it) with no pending / level1 / candidate line at all. Every `return None` after the witness scan is traced, so the rejection is in the witness scan; `unexpected_owner` is excluded (the start line prints `crossing_search.partners`, i.e. partners WITH centerlines, and 286 is listed). The only remaining silent path: the cell is blocked in the dense grid but carries NO core owner (`require_dynamic_owner_contact && dynamic_owner_count == 0`) -- a halo / non-core cell of n_285. HYPOTHESIS (being verified with a core-vs-halo probe map): under the Point-2 prefix (286 nets, 0 repairs -- different routes than before), n_285 runs as a diagonal staircase from (3060,1520) up to (3104,1536), exactly across the old descent corridor; a vertical route meeting a diagonal's halo has no core cell to intersect (and would be a 45-degree crossing anyway), so the kernel rejects it silently and correctly. The regression would then be INDIRECT (changed neighbour geometry), not a rule bug -- and n_286 needs a different descent (x > 3104 on n_285's horizontal, or a repair). The earlier n_285 crossings at (3060,1520) came from the probe searches seq 123/124, not from the real search.
- FINDING (2026-09-03, `PHOTONIC_ROUTER_MOVE_DIAG`, counter deltas per hook rejection, net 287's real search seq 125, landing cell (3104,1536) = n_285's core horizontal): 42 landings are bends -> `crossing_reject_non_straight` (correct: no crossing inside a bend); the 12 STRAIGHT landings (1-cell vertical from (3104,1537,6), straight_run 4-5, pending 0 -- i.e. a textbook perpendicular crossing with route_before >= 5) are ALL rejected with `crossing_reject_unexpected_owner`: a footprint cell's dynamic owner is not in `partner_index_by_id`, and the move dies silently before any intersection/trace. The core-vs-halo hypothesis is dead ((3104,1536) is core=true). This IS the lidar-pure violation the owner asked about: a collided committed cell whose owner the search cannot legalize. The dense owner grid (`DenseDynamicCoreOwnerGrid::from_obstacle_map`) assigns each cell the FIRST net (hash-map iteration order) whose `net_core_routes` list contains it; `partner_index_by_id` is built from `crossing.partners`, i.e. nets with a `committed_center_routes` entry. A net present in the obstacle map's per-net core lists but absent from `committed_center_routes` -- or a second, non-partner owner winning the "first owner" race on a shared core cell -- produces exactly this. The owner id is being read now (`unexpected-owner` trace at the reject site).
- KEY FINDING (2026-09-03): **under Point 2 the full lidar-pure search FINDS the owner's expected route for net 287 -- and post-search validation discards it.** The `unexpected_owner` rejections belonged to a later, single-partner repair search (`partners=1`, partner 272 only): seq 125, the first search to print a failure line, was NOT the full-map attempt. The full-map attempt (286 partners) printed no failure because it succeeded: `collision-crossing result net=287 ... waypoints=[(3040,1593),(3052,1593),(3072,1573),(3072,1211),(3341,942),(3341,907),...]` -- a vertical descent at x=3072 through n_285..n_281, then ONE straight 269-cell 135-degree diagonal (3072,1211)->(3341,942) across the whole fan, exactly the path the owner described; `events` lists 15 crossings (partners 272..286), `realized_violations=[]`. Then `collision-crossing validation ... satisfies=false` -> the route is dropped (`Ok(None)`) and the flow falls into repair with single-partner searches that cannot cross anything else. This is the sibling of the 2026-09-02 zero-event bug: a legal route killed after the search. `satisfies` = events non-empty AND `route_has_no_unresolved_grid_crossings` AND `crossing_events_satisfy_partner_constraints`; the trace now prints both sub-conditions plus the unresolved grid intersections (run in flight). Consequence for the whole diagnosis of the last hours: the "wall" at y=1521 was an artifact of analysing the wrong (single-partner) search; Point 2 did not regress the kernel, it changed which attempt fails first.
- ROOT CAUSE of the post-search rejection (2026-09-03, extended validation trace): `no_unresolved_grid_crossings=true`, `partner_constraints=false`, `realized_violations=[]`. `crossing_events_satisfy_partner_constraints` fails on `crossing_events_have_disjoint_reservations`: the found route crosses n_285 at (6125.5, 4603.125) and n_284 at (6125.5, 4597.125) -- 6 um = 3 cells apart on the x=3072 descent -- and their +-half_size (2) reservation boxes share cells. Physically correct: two crossing elements of 5 cells cannot sit 3 cells apart. The old `required_margin` = 5 silently encoded TWO rules in one number: (1) straight-after obligation for the crossing element (physically half_size = 2 -- Point 2 fixed this) and (2) minimum spacing to the NEXT crossing so the reservation windows stay disjoint (2*half_size + 1 = 5 -- the validation rule). Point 2 shortened the pending zone to 2 and thereby lost (2): the search now accepts crossings 3 cells apart that validation must reject. The unit test `vertical_route_crosses_two_horizontal_partners_three_cells_apart` encoded the wrong expectation (it asserts a route).
- Decision needed (owner): reconcile search and validation by separating the two rules in the kernel: `pending_after_crossing_cells` becomes the crossing-exclusion countdown = `2*half_size + 1 - distance_after`, decremented by every move; the pure-straight obligation applies only while `pending > half_size + 1` (the first half_size cells), which is exactly what the eager completion chain inserts before it stops; afterwards bends are allowed but no new intersection while `pending > 0` (the existing rule at ~7075). Tests: invert the 3-cells-apart test (search must not produce overlapping reservation windows; with both partners spanning the map the correct outcome is no route), add a "crossing -> 2 straights -> bend allowed -> no new crossing before 5 cells" test, and re-check the n_286 acceptance run (the search should then descend where n_285/n_284 are 19 cells apart, x >= 3092, as before).

## Rule audit (2026-09-03, owner request: "are the rules enforced everywhere exactly as intended, or is there mixing?")

Physical rules (ruleset): (R1) crossing perpendicular; (R2) both nets straight for `half_size` (2) cells on both sides of the intersection -- the crossing element; (R3) no third net inside the +-half_size reservation window; (R4) crossing elements never overlap -> consecutive crossings of one route >= `2*half_size+1` (5) cells apart, and windows of committed crossings are excluded; (R5) terminal bump distance half_size. Where each is enforced, with the number actually used:

| rule | kernel (`crossing_move_outcome_with_segments`, astar.rs) | post-search / realized (py_router.rs) | verdict |
|---|---|---|---|
| R1 perpendicular | `grid_axes_are_perpendicular` | realized `not_perpendicular` | consistent |
| R2 route straight AFTER | debt = `half_size` = 2 pure straights (Point 2), bends only at debt 0 | realized `insufficient_straight_margin`, 2 on the realized centerline | consistent for 90-degree bends (arc fills the arm); **WRONG for 45-degree turns**: the fillet trims `R*tan(22.5)` = 1.24 cells BEFORE the grid corner, so a diagonal starting 2 cells after the crossing leaves only 0.76 cells of real straight -> realized violation. Obligation before a 45-degree turn = half_size + 1.24 -> corner at >= 4 cells. |
| R2 route straight BEFORE | `distance_before_on_segment >= required_margin` = 5, where `straight_run_cells` counts a preceding bend's second ARM as straight (`primitive_terminal_straight_run_cells`) | realized 2 | 5 = 2 + R silently compensates "arm counted as straight": exact for 90-degree (arm = arc), over-strict by 1 cell after 45-degree corners (real straight = counted - 1.24). Conflation of two numbers in one. |
| R2 partner straight | `partner_margin >= required_margin` = 5 on the committed GRID polyline | realized 2 on the centerline | same compensation: exact for 90-degree partner corners, over-strict by ~1.8 cells at 45-degree corners -- i.e. on the fan diagonals: crossings must stay 10 um from a diagonal's start instead of the physically needed ~6.5 um. |
| R3 third-net window | `crossing_reservation_window_is_clear` +-2, static + dynamic owners | `crossing_reservation_blockers` +-2; committed windows are `add_static_keys` | consistent |
| R4 own consecutive-crossing spacing | pending zone = debt = 2 (Point 2); before Point 2 = 5 by coincidence (2+R = 2*2+1) | `crossing_events_have_disjoint_reservations` -> >= 5 | **BUG (the n_287 loss)**: kernel builds crossing pairs 3 cells apart, validation must reject the whole route. |
| R4 own window re-entry | `active_local_reservation_keys` activated when the debt is paid; witnesses = footprint + halo | -- | with debt 2 the route is still on the window's edge cell when the window becomes active; a diagonal starting there has halo cell start+(dx,0) inside the window -> self-reject. Accidentally masks the 45-degree gap above by one cell; needs a kernel test, not a guess. |
| R5 terminal bump | half_size | -- | consistent |
| pending gate before the hook (astar.rs ~4728) | lets a bend ARM complete the debt (`pending_completed_by_primitive`), the hook then rejects it | -- | redundant second implementation with the superseded "arms pay" semantics; also drives window activation. Fold into one rule. |
| config `min_straight_cells_per_crossing` | passed from Python, validated, then IGNORED (`_min_straight_cells` in `crossing_required_margin_cells`) | -- | dead knob; wire it as the straight-obligation parameter or delete it. |
| lidar-pure partner set | full committed set (lookup DB) | -- | consistent; but the single-partner repair searches (`partners=[272]`) reject every other net as `unexpected_owner` and cannot succeed in a crowded region -- wasted time and the source of the mis-diagnosis. |

Root conflation: `required_margin = half_size + bend_runout(=R)` uses ONE number for three different things -- (a) the straight obligation (half_size), (b) the arc-tangent compensation for polyline measurements (R*tan(theta/2): R for 90 degrees, 0.41R for 45 degrees), (c) the crossing spacing (2*half_size+1). They agree only for R = half_size+1 = 3 and 90-degree corners, which is why the benchmark "worked before".

Proposed correct formulation (decision-gated): straight obligation each side = half_size + tangent(next/previous turn) with tangent(90) covered by the 90-degree primitive's own arm, tangent(45) = ceil(0.414*R); partner margin measured on the realized centerline (or polyline minus tangent per corner); crossing spacing = 2*half_size+1 as a separate countdown in the extension; window activation when the route has physically LEFT the window (distance > half_size), not when the debt is paid; delete the pre-hook arm-pays gate; wire or delete `min_straight_cells_per_crossing`.

### Audit correction + owner's simplification (2026-09-03)

- CORRECTION: the "45-degree gap after the crossing" row above is wrong -- `make_turn` (primitives.rs) gives EVERY turn primitive (45 and 90 degrees) arms of `radius_cells` on both sides of the corner, so the 45-degree fillet trim (0.414 R = 1.24 cells) lies inside the primitive's own arm. After the chain's `half_size` straights any following primitive is legal.
- Owner's framing: the GDS crossing size gives `half_size`; that fixes the element footprint and the straight cells; policy = insert the straight cells eagerly (chain) so every keyed state is debt-free. Everything reduces to ONE number and TWO predicates:
  - P1 straightness +-half_size on both nets, measured in REAL straight cells (arc cells never count). Today violated in three places: (a) carried `straight_run_cells` counts the previous turn's second arm (`primitive_terminal_straight_run_cells`); (b) a crossing INSIDE a turn primitive's arm counts arc cells as straight -- Point 2 opened this hole: 90-degree turn, crossing 3 cells before the corner -> old 5-3=2 debt -> rejected (rest of primitive not straight); new 2-3<0 -> no debt -> accepted although the arc starts at the crossing (real straight after = 0) -> realized `insufficient_straight_margin`; (c) `partner_margin` measured on the committed grid polyline counts the partner's arms. Fix: per-segment TRIM at every end that sits on a corner, `R*tan(theta/2)` (90: R = whole arm; 45: 1.24 -> ceil 2; ports/no corner: 0), same formula as the realizer (route_rust.py:1623); route-before = (carried real run if collinear without kink) + distance_on_segment - trim_start >= half_size; route-after = distance_after - trim_end >= half_size else debt -> chain (terminal segment only, otherwise reject as today); partner = min(u*len - trim_start, (1-u)*len - trim_end) >= half_size. Kernel then computes the same quantity the realized validator measures.
  - P2 reservation windows disjoint: the new crossing's +-half_size window contains no third-net/static cell (exists; committed windows are static) AND does not overlap the route's own earlier windows (missing -> the n_287 loss). Implement with the own-window keys already carried on the node chain (`active_local_reservation_keys`), i.e. literally the validator's predicate `crossing_events_have_disjoint_reservations`; no new counter, states stay clean. Subsumes the pending-zone no-intersection rule.
  - Delete: `bend_runout_cells`, `required_margin`/`capped_required_margin` (-> half_size), the pre-hook "arm pays" gate, the three bisect toggles, `min_straight_cells_per_crossing` (or make it the half_size override -- owner's call).
- Test list (kernel, H/V and diagonal): crossing inside the first/second arm of a 90-degree turn -> rejected; inside a 45-degree arm at 3/4/5 cells before the corner -> rejected/rejected/accepted; crossing right after a 90-degree turn with 1/2 straights -> rejected/accepted; partner corner 90 and 45 degrees at 2/4/5 cells -> per trim; inverted 3-cells-apart spacing test (no pair with overlapping windows); end-to-end realized validation on each accepted case.
- Order: (1) P2 + spacing test -> build -> net-287 acceptance run; (2) P1 trims + tests; (3) deletions, ladder, pytest baseline.

## Decision Log

- Decision: The crossing detection for diagonal-x-diagonal must insert the crossing cells eagerly -- no defer, no pending -- exactly the way the post-crossing straight is already inserted eagerly by the completion chain.
  Rationale: Repository owner, 2026-09-03: "bei der diagonalen crossing erkennung [hat man] dann eben nicht pending, sondern man muss diese Zellen einfuegen wie auch normalerweise, weil sonst evtl das Halo danach nochmal im selben Fenster kollidiert." Evidence that the defer is only a stop-gap: on net 287 the fan partners 280/279/278/277 now accept (16.9k/12.2k/11.5k/6.5k accepts) but 276 -> 1.7k, 275 -> 26, 274 -> 0 accepts with 371 defers and zero legitimate candidate rejects (no `partner_margin`, no `reservation_footprint`, no `not_perpendicular` candidate) -- the deferred crossing never matures.
  Date/Author: 2026-09-03 / repository owner, recorded by Claude.

- Decision: Unit test first. No implementation lands without a Rust unit test that fails before and passes after; benchmark runs are supporting evidence, not the acceptance criterion.
  Rationale: The 2026-09-02 defer commit (`579aed2`) shipped with no unit test and turned out to be partial. The repository's stated priority is readability and testing over benchmark passing.
  Date/Author: 2026-09-03 / repository owner, recorded by Claude.

- Decision: No subagents for complex tasks in this session; the orchestrator runs the Explorer, Implementation, and Harness roles sequentially in one session and labels each pass here (the fallback `.agent/ORCHESTRATOR.md` prescribes when real subagents are unavailable).
  Rationale: Repository owner, 2026-09-03: subagent lanes have repeatedly stalled ("die oft stehen geblieben sind").
  Date/Author: 2026-09-03 / repository owner.

- Decision: Consolidate findings into this plan before every further diagnostic run.
  Rationale: 2026-09-02 stacked ~10 diagnostic runs without consolidation and the owner had to call it out.
  Date/Author: 2026-09-03 / repository owner.

- Decision: Eager insertion means EXTENDING THE MOVE through the X (appending the crossing cell(s) and evaluating an ordinary in-move intersection), not recording a forward point and setting pending.
  Rationale: With a forward point + pending, the completion chain's first step contains the X at t=0.5 and is rejected by the existing "no new intersection inside the pending straight run" rule (`crossing_reject_pending_straight`, src/astar.rs ~line 7075) -- the crossing dies one step later. Normal shared-cell crossings avoid this because the X is inside the recording move; extending the move reproduces that shape. The diagonal halo must NOT be changed: for an X between cells the route and partner share no cell at all, so the halo firing one move early is the only thing that detects the crossing; shrinking it would make such crossings invisible (unrecorded physical crossing, caught only at commit). The reservation window is not involved (partner and self are allowed inside it; the just-recorded crossing's keys are pending, not active, when the chain starts).
  Date/Author: 2026-09-03 / repository owner ("die Zellen einfuegen wie auch normalerweise") + Claude code check.

- Decision (owner, 2026-09-03, "genau, mach das so"): NO eager extension on the approach move. Resolved as option (b): the approach move is rejected as contact without a crossing, the containing move records the X; the 2026-09-02 defer (`contact_intersection_lies_ahead`, `defer_contact_intersection_ahead`) is deleted; the test is inverted to `perpendicular_diagonal_x_approach_move_is_rejected_and_containing_move_accepts`. Original framing kept below for the record.
  Rationale: eager extension would be a second mechanism for a result the containing move already produces (proven by `crossing_move_detects_offset_diagonal_halo_contact` and the margin-5 consecutive test); the debt insertion (eager completion chain) is a different logic and already works.
  Date/Author: 2026-09-03 / repository owner.
- (record) OPEN DECISION as originally framed: eager extension through the X on the APPROACH move (the plan's original Milestone 1 and the still-failing test `perpendicular_diagonal_x_crossing_is_recorded_eagerly`). Evidence now says it is not needed for correctness: the move that CONTAINS the half-cell X records the crossing (`crossing_move_detects_offset_diagonal_halo_contact` proves it, and the margin-5 consecutive test routes the straight line through such an X). Options: (a) implement it anyway for robustness; (b) drop it, delete that test (or invert it to pin "approach move rejected, containing move accepts"), and remove the dead defer (Milestone 2). Claude's recommendation: (b) -- smaller kernel, no second mechanism, behaviour already correct and tested.
- Decision (owner, 2026-09-03): the straight-after debt is `crossing_half_size_cells` measured from the intersection (not `required_margin` = half_size + bend_runout), and it is paid ONLY by pure straight primitives in the pending direction (partially if shorter); a bend never pays and may only start once the debt is zero. Implemented in `crossing_move_outcome_with_segments` (`missing_after`, the pending block) and `LiveCrossingHook::evaluate` (no-contact path), `src/astar.rs`.
  Rationale (verified in code): `make_turn` gives every bend arms of exactly `bend_radius_cells`; the realized arc's tangent points sit `R*tan(theta/2)` from the corner -- for 90 degrees that is R, i.e. the arc fills the first arm completely and never reaches back into the preceding straight; so after the crossing element (+-half_size) a bend may follow immediately. `required_margin` stays correct where it compensates arcs counted as straight run: the PARTNER's margin (its corner may be a 90-degree bend whose arc eats `bend_radius` back) and `route_before` (a preceding bend's terminal arm is counted as straight but is arc). The realized validator already uses exactly this: `realized_crossing_margin_um = half_size * grid`. Letting a bend's arm pay the (now shorter) debt would put the arc on the crossing point, which that validator rejects (`insufficient_straight_margin`) -- hence pure straights only. Four older tests that pinned "bend arm pays the runout" were re-pinned with derivations (`crossing_pending_rejects_bend_while_debt_open`, `crossing_pending_rejects_bend_regardless_of_diagonal_arm_length`, `crossing_pending_after_keeps_largest_missing_runout` via half_size 5, `crossing_move_marks_n35_n32_short_after_runout_as_pending` 4 -> 2). New realized-level test `realized_crossing_accepts_bend_two_cells_after_crossing_and_rejects_one` (src/py_router.rs) pins the owner's claim: a 90-degree arc two cells after the crossing point is valid, one cell is not.
  Date/Author: 2026-09-03 / repository owner + Claude code verification.

## Outcomes & Retrospective

Not started.

## Context and Orientation

The router is a Rust library (`src/`) driven from Python (`routing_flow.py` -> `translation/route_rust.py`). Build and deploy the Rust side with, from the repository root:

    PYO3_PYTHON=.venv/bin/python cargo +stable build --release
    cp target/release/libphotonic_router.so python/photonic_router/_rust.abi3.so

(`rust-toolchain.toml` pins a Windows channel, hence `+stable`. The Python package loads the copied `.so`; forgetting the `cp` runs the old binary.)

Terms used below:

- **Grid / cell**: routing happens on a 2.0 um grid (`SCRIPT_GRID_SIZE_UM = 2.0` in `routing_flow_config.py`). A cell is `(x, y)` in grid units. Physical micrometers = `origin + (cell + 0.5) * 2.0`; the origin is NOT the layout bounding-box minimum (for `multiportmmi_32x32` it is about `(-19.5, 1562.125)`), so never hand-convert -- read `grid_centerline_um`/`realized_centerline_um` from the `committed-centerline-compare` diagnostic (`PHOTONIC_ROUTER_TRACE_PARTNER_NET=<id>`) or read the GDS directly with `klayout.db`.
- **Angle**: 0..7 in eighths of a turn; `crate::primitives::DIRECTIONS[angle]` gives the unit step. Angle 1 = `(1, 1)`, angle 7 = `(1, -1)`; these two are perpendicular diagonals.
- **Primitive**: one A\* move -- a straight of 1 or 4 cells, or a 45/90-degree bend -- with a `footprint` (cells swept) and `start_angle`/`end_angle`. See `src/primitives.rs` and `PrimitiveLibrary`.
- **Unified kernel**: `route_single_net_with_bounds_unified` in `src/astar.rs`. Tier-1 = dense, contact-free moves; Tier-2 = moves carrying crossing state (`CrossingExtension`: `pending_after_crossing_cells`, `straight_run_cells`, `crossed_mask`).
- **Diagonal halo**: for every diagonal segment of a move, the two "corner" cells beside each diagonal step are added as extra collision witnesses (`effective_collision_witness_offsets` -> `compact_diagonal_halo_cells`, `src/astar.rs`). Purpose (2026-08-27 rule, correct and required): two parallel diagonals in adjacent cells realize as physically overlapping waveguides, so adjacency must be detected even with no shared cell. Horizontal/vertical segments carry no halo.
- **Contact**: a witness cell owned by another committed net (`dynamic_core_owners.owner_at`). A contact sends the move to `crossing_move_outcome_with_segments` (`src/astar.rs`, ~line 6600 onward), which builds the move's route segments (`translate_primitive_path_segment`), intersects each with the contacted partner's committed centerline segments (`partner_segments[partner_idx]`, built from `committed_center_routes`) via `grid_segment_intersection_with_params`, and for every intersection checks, in order: perpendicularity (`grid_axes_are_perpendicular`), partner margin (`required_margin` cells of straight partner on each side of the point; 5 in the benchmarks), the reservation window (`crossing_reservation_window_is_clear`: a box of `crossing_half_size_cells` = 2 around the point must contain no third net), and the terminal-bump guard. A passing intersection is pushed to `route_intersections` as a `CrossingRouteIntersection`; downstream this increments `crossing_count`, sets `pending_after_crossing_cells` (the minimum straight run after the crossing) and the **eager completion chain** in the kernel (search for `EAGER_CHAIN_MAX_STEPS`) immediately appends straight primitives to pay that run off, legalizing any further crossings the run hits.
- **The gap this plan closes**: when the route (angle 7) and the partner (angle 1) are perpendicular diagonals, their grid paths form an X through a 2x2 block and the centerline intersection is at a half-cell point, e.g. `(3364.5, 898.5)`. The 1-cell move that ends one cell before the X already has a halo witness on the partner's cell, so contact fires -- but the intersection lies half a cell past the move's segment, `grid_segment_intersection_with_params` returns `None` for this move, and `route_intersections` stays empty for the partner. Before 2026-09-02 that branch hard-rejected the move ("contact without a same-move intersection" = grazing). Commit `579aed2` changed it to a defer: if `contact_intersection_lies_ahead(state, primitive, partner_segments, margin + 6)` (the partner's segment intersects the route's forward ray from the move end) the code `continue`s to the next contacted partner and the move survives without recording a crossing, on the assumption that the next move will contain the intersection and record it. That assumption fails in practice (see Decision Log evidence), which is what this plan fixes.

Relevant existing unit tests (pattern to copy), all in `src/astar.rs` `mod tests`: `crossing_move_detects_offset_diagonal_halo_contact` (~line 11319) builds an `ObstacleMap::new(800, 300)`, commits a diagonal partner from grid waypoints with `rasterize_waypoints_for_test` + `commit_route_with_clearance_and_allowed_core_overlaps`, builds a `CrossingSearchConfig { partners: vec![CrossingSearchPartner { net_id, waypoints, target_terminal_bump_guard: None }], ... }`, a one-cell angle-7 `Primitive { dx: 1, dy: -1, footprint: vec![(0,0),(1,-1)], ... }`, and calls `crossing_move_outcome(...)` directly. `crossing_move_accepts_perpendicular_shared_core_cell` (~line 11247) is the neighbouring accepted case where the X does share a cell.

## Plan of Work

**Milestone 0 -- pin the bug.** Add two tests to `src/astar.rs` `mod tests`, next to `crossing_move_detects_offset_diagonal_halo_contact`:

1. `perpendicular_diagonal_x_crossing_is_recorded_eagerly`: partner net 32 committed as the diagonal `(700,150) -> (760,210)` (angle 1, line `y = x - 550`; it passes through `(729,179)` and `(730,180)`). Route state `(728, 181, 7)`; the 1-cell angle-7 move goes to `(729, 180)`. The route line is `y = -x + 909`, so the centerline intersection with the partner is at `x = (909 + 550) / 2 = 729.5`, `y = 179.5` -- half a cell PAST the move's segment end `x = 729` (this is the "X between cells" case; `grid_segment_intersection_with_params` returns `None` for this move by construction). The move still makes contact through its diagonal halo: the halo of a diagonal step reaches one cell past the step end along the x direction (observed in the real failure: state `(3363,900,7)`, move end `(3364,899)`, witness `(3365,899)` = `end + (dx, 0)`), so here the witness `(730, 180)` is a cell of partner 32. The test must first assert that `effective_collision_witness_offsets` for this primitive, translated to the state, contains at least one cell owned by net 32 -- so that a different halo shape shows up as a fixture error, never as a false pass. Then: today `crossing_move_outcome` returns `Some` with `crossing_count == 0` (the defer keeps the move alive but records nothing); after Milestone 1 it returns `Some` with `crossing_count == 1`, exactly one `crossing_events` entry with `partner_net_id == 32`, its point within 0.6 cells of `(729.5, 179.5)`, and `pending_after_crossing_cells == min_straight_cells`. Use `min_straight_cells: 2`, `crossing_half_size_cells: 2`, and the `required_margin` the kernel derives from those (`crossing_required_margin_cells`). The partner's 60-cell diagonal gives it ample margin on both sides of the point, so the only thing that can fail is the recording itself.
2. `consecutive_perpendicular_diagonal_crossings_on_one_line`: two partners (32 and 33) as parallel angle-1 diagonals whose line offsets differ by 11 (so one X shares a cell and the next is between cells), crossed by a full unified search (`route_single_net_with_bounds_unified` via the same fixture style as `crossing_search_fixture`) from a source above-left to a target below-right on the perpendicular line. Assert: a route is found, `crossing_events.len() == 2`, one per partner, and `crossing_violations_for_realized_centerline`-style realized validation reports no violation (use the existing test helper the neighbouring kernel tests use for realized checks; if none fits, assert on `crossing_events` only and note it in Surprises).

Run `PYO3_PYTHON=.venv/bin/python cargo +stable test --release perpendicular_diagonal_x_crossing_is_recorded_eagerly` and expect the crossing-count assertion to FAIL before Milestone 1 (the test must be written so the pre-fix failure is the `crossing_count == 1` assertion, not a fixture error).

**Milestone 1 -- eager insertion by EXTENDING THE MOVE THROUGH THE X (not by recording a point beyond the move).** Design fixed 2026-09-03 with the owner (see Decision Log). Why not "record the forward point and set pending": the eager completion chain's first straight step would then contain the X at t=0.5, find the same intersection at distance 0.5 < pending, and `crossing_move_outcome_with_segments` rejects it at the `pending_before > 0.0 && intersection.distance_from_primitive_start < pending_before` check (~line 7075, `crossing_reject_pending_straight`) -- the chain dies exactly as the owner predicted ("das Halo kollidiert danach nochmal im selben Fenster"). A normal shared-cell crossing never has this problem because the crossing lies INSIDE the move that records it, so pending starts past the X and the next move contains no intersection. Eager insertion must reproduce that shape: extend the move by the cell(s) needed to pass the X, then evaluate the crossing as an ordinary in-move intersection of the extended move.

In `src/astar.rs`, `crossing_move_outcome_with_segments`, at the branch `if route_intersections.len() == intersection_count_before { ... }`:

- Replace the boolean helper `contact_intersection_lies_ahead(...) -> bool` with `eager_forward_crossing_intersection(state, primitive, partner_segments, lookahead) -> Option<ForwardIntersection>` (fields: `x, y, distance_beyond_move_end, partner_segment_idx, partner_u`), computed exactly as the boolean was (forward ray from the move end along `DIRECTIONS[primitive.end_angle]`, nearest hit via `grid_segment_intersection_with_params`).
- When it returns `Some(fi)`: the move is ELIGIBLE for eager extension only if the primitive ends straight in its own direction (`primitive.end_angle == primitive.start_angle` for straights; for a bend the diagonal arm must be the terminal arm) and `fi.distance_beyond_move_end <= 2.0` cells (the X is at most one full cell past the end; anything farther is a genuine "ahead" case the NEXT move will meet as a normal in-move intersection, so keep today's behaviour there -- but as a REJECT, not a defer). Build the extension: `k = ceil(fi.distance_beyond_move_end)` one-cell straight primitives in `primitive.end_angle` (`completion_straights_per_angle[angle]` already lists them, longest first; take the 1-cell one). Compose an extended primitive path = original footprint + k appended cells, and re-run THIS SAME function's segment/intersection/legality pipeline on the extended path (perpendicularity, partner margin, reservation window at the X, terminal bump guard). The simplest faithful implementation is to loop: append one cell, recompute `primitive_segments`/`primitive_crossing` for the extended path via the existing `primitive_crossing_metadata`, and let the normal in-move logic find and validate the X. Every appended cell must also pass the ordinary footprint/static/dynamic-owner checks the kernel applies to a move (static blocked → reject; third-net core cell → reject via the existing unexpected-owner path).
- If the extended move records the crossing: return the outcome for the EXTENDED move (its end state is k cells farther along; `primitive_steps`, `straight_run`, the reservation keys and `pending_after_crossing_cells` all derive from the extended path). The caller (the kernel's Tier-2 successor generation) must then place the successor state at the extended end and account cost for the extra cells -- check how the eager completion chain already spawns intermediate scaffolding nodes and reuse that shape (`UnifiedExtendedNode` chain with `parent` links; intermediate nodes un-keyed), so a novice implementer copies the chain's node-creation code rather than inventing a second mechanism.
- If the extended move does NOT legalize (any check fails): reject the original move (`return None`), as before the defer existed. Nothing is deferred anymore.
- If `eager_forward_crossing_intersection` returns `None`: keep today's reject (`classify_unresolved_crossing_contact` + `return None`).
- Verification inside this milestone (record in Surprises): after the extension, the chain's first step must NOT re-find the X. Assert it in `perpendicular_diagonal_x_crossing_is_recorded_eagerly` by running the outcome's chain one step (or by asserting `stats.crossing_reject_pending_straight == 0` across the full-search test `consecutive_perpendicular_diagonal_crossings_on_one_line`).

**Milestone 2 -- remove the defer.** Delete the `"defer_contact_intersection_ahead"` trace event and any code path that can `continue` past a contact without either a recorded crossing or a reject. `grep -n defer_contact_intersection_ahead src/astar.rs` must return nothing.

**Milestone 3 -- sequential Harness pass.** Run, in this order, from the repository root, and paste the results into Concrete Steps / Artifacts:

    PYO3_PYTHON=.venv/bin/python cargo +stable test --release
    timeout 580 .venv/bin/python -m pytest tests -q
    for b in heater_s_mod multiportmmi_8x8 benes_8x8 multiportmmi_16x16; do timeout 300 .venv/bin/python routing_flow.py $b > /tmp/claude-1000/.../tol_$b.log 2>&1; echo "$b exit=$?"; done
    timeout 580 .venv/bin/python routing_flow.py benes_16x16
    timeout 1500 .venv/bin/python routing_flow.py multiportmmi_32x32 --debug-stop-after-route 287
    (then the full 447-net run, detached with a status file)

Expected: cargo tests all green including the two new ones; pytest `10 failed, 357 passed` (the 10 are the documented pre-existing failures; if `test_multiportmmi_8x8_routes_cleanly_through_first_mmi_fanin_boundary` moves, re-pin its counts with a derivation comment as was done on 2026-09-02); every ladder benchmark `photonic error_count 0` and `crossing error_count 0` in `build/verification/<benchmark>_{photonic,crossing}_verification.json`; `multiportmmi_32x32 --debug-stop-after-route 287` exits 0 with 287 routed records (this is the acceptance test for the bug); full run result recorded honestly whatever it is.

Label the pass in Progress as "Harness role pass (sequential, in-session)" with verdict PASS / FAIL / BLOCKED / INCONCLUSIVE.

**Milestone 4 -- close out.** Fill Outcomes & Retrospective; update `.agent/REPOSITORY_STATE.md` (Current Snapshot, Next Engineering Step) and the active-plan pointer paragraph in `.agent/ORCHESTRATOR.md` (it still says "no single active ExecPlan (2026-08-28)").

## Concrete Steps

Working directory for every command: the repository root `/home/benjamin/Documents/Repositories/working/TUMPhotonicRouter`.

Build + test cycle:

    PYO3_PYTHON=.venv/bin/python cargo +stable build --release
    PYO3_PYTHON=.venv/bin/python cargo +stable test --release 2>&1 | grep -E "^test result|FAILED|panicked"
    cp target/release/libphotonic_router.so python/photonic_router/_rust.abi3.so

Expected `cargo test` transcript today (before this plan): three `test result: ok` lines (419 + 12 + 0 tests). After Milestone 0 and before Milestone 1: one `FAILED` naming `perpendicular_diagonal_x_crossing_is_recorded_eagerly`. After Milestone 1: all green, 421 in the first group.

Focused reproduction of the bug on the real benchmark (diagnosis only; the unit test is the acceptance criterion):

    PHOTONIC_ROUTER_TRACE_CROSSING_NET=287 PHOTONIC_ROUTER_TRACE_CROSSING_LEVEL1=1 \
      timeout 1000 .venv/bin/python routing_flow.py multiportmmi_32x32 --debug-stop-after-route 287 \
      > /tmp/claude-1000/<scratchpad>/l1.stdout.log 2> /tmp/claude-1000/<scratchpad>/l1.stderr.log
    for p in 280 279 278 277 276 275 274; do echo "partner $p accepts=$(grep -c "crossing-level1 net=287 partner=$p event=accept" .../l1.stderr.log)"; done

Before this plan that prints `280: 16905, 279: 12233, 278: 11528, 277: 6540, 276: 1682, 275: 26, 274: 0`. After Milestone 1 every fan partner the straight line crosses must show accepts, and the run must end with 287 routed (no `search-failure kind=` line for `source=(3040,1593`).

Note: `--debug-stop-after-route N` routes flow routes 1..N; Rust net id 287 is `n_286` and is flow route 287, so `287` INCLUDES the failing net (needed here) while a pre-failure GDS needs `286`.

## Validation and Acceptance

- `perpendicular_diagonal_x_crossing_is_recorded_eagerly` fails before Milestone 1 on the `crossing_count == 1` assertion and passes after.
- `consecutive_perpendicular_diagonal_crossings_on_one_line` passes after Milestone 1.
- `grep -c defer_contact_intersection_ahead src/astar.rs` prints `0` after Milestone 2.
- Ladder: five benchmarks with 0/0 verification errors; times within the 2026-09-02 range (heater ~8 s, mm8 ~16 s, benes8 ~35 s, mm16 ~95-105 s, benes16 ~180-210 s).
- `multiportmmi_32x32 --debug-stop-after-route 287` routes 287 records with 0 verification errors.
- pytest at baseline (10 failed / 357 passed) or re-pinned with a derivation.
- The full `multiportmmi_32x32` run's outcome (routed count, first failing net if any, wall time) is recorded in Outcomes whatever it is.

## Idempotence and Recovery

All build/test/benchmark commands are safe to repeat. Benchmark runs overwrite `build/routed_multiportmmi_32x32.gds` and `build/verification/*.json`; copy any GDS you want to keep to a distinct name first. To roll back the implementation: `git revert <commit>` then rebuild and re-copy the `.so`. Never leave a routing process running when you stop: `ps aux | grep "python routing_flow.py"` and kill by PID (do not `pkill -f` a pattern that matches your own shell).

## Artifacts and Notes

(To be filled with the failing-then-passing test transcript, the ladder table, and the stop-287 result.)

## Interfaces and Dependencies

In `src/astar.rs` (private, same module as the kernel):

    struct ForwardIntersection { x: f64, y: f64, distance_beyond_move_end: f64, partner_segment_idx: usize, partner_u: f64 }
    fn eager_forward_crossing_intersection(
        state: State,
        primitive: &Primitive,
        partner_segments: &[PartnerPathSegment],
        lookahead_cells: f64,
    ) -> Option<ForwardIntersection>;

`contact_intersection_lies_ahead` is deleted. `crossing_move_outcome_with_segments` keeps its signature but may return an outcome whose `primitive_steps`/end state describe an EXTENDED path (original primitive + k appended one-cell straights); the outcome type gains whatever field is needed to tell the kernel how many cells were appended (e.g. `extended_by_cells: i32`, default 0) so the successor state is placed at the extended end. Keep that field name in the plan when chosen. No Python-side or pyo3 interface changes. Dependencies: none new; uses `crate::primitives::DIRECTIONS`, `grid_segment_intersection_with_params`, `grid_axes_are_perpendicular`, `crossing_reservation_window_is_clear`, `terminal_bump_guard_satisfied`, `CrossingRouteIntersection`, all already in `src/astar.rs`.

---
Revision note (2026-09-03, Claude): plan created at the repository owner's direction after the 2026-09-03 audit of the 2026-09-02 session; it supersedes the defer introduced by commit `579aed2` and records the owner's eager-insertion design as the required shape.
